# 文档实现对照排查报告

更新时间：2026-05-31

## 1. 排查结论

结论：当前项目已经基本贴合 `docs/MVP范围说明.md` 定义的 P0 主业务闭环，后端、Worker、普通查询前端、管理后台和索引运维能力都已经不是骨架状态。本次已完成一轮文档/实现收口：OpenAPI contract-only 接口被明确标注阶段，setup JWT、配置管理、组织管理、架构分层和 P0 联调记录已按当前代码修正。

剩余不能称为“全部完善”的原因主要有三类：

- OpenAPI 仍有 9 个未挂载接口，但已全部标注为 P1/P2 contract-only gap，并由 `tests/unit/test_openapi_contract_routes.py` 跟踪。
- P0 主链路已经有 smoke/regression 脚本和真实执行记录，但 `tests/e2e`、`tests/contract` 仍未形成完整自动化目录，安全绕过、真实依赖故障注入和发布门禁还需要继续补强。
- OCR、复杂版式解析、query rewrite/expansion、Redis 热通知/缓存、完整审批、通用配置回滚和 `rag-admin` 运维 CLI 仍属于后续阶段。

置信度：高。依据来自当前代码、OpenAPI 差集、测试结果和 docs 原文。

## 2. 本次验证证据

### 2.1 OpenAPI 契约对照

当前 FastAPI 已挂载操作数：101。

`docs/contracts/openapi.yaml` 定义操作数：110。

实际已挂载但未写入 OpenAPI：0。

OpenAPI 已定义但后端未挂载：9。

```text
POST   /internal/v1/admin/roles
PATCH  /internal/v1/admin/roles/{role_id}
DELETE /internal/v1/admin/roles/{role_id}
GET    /internal/v1/model-catalog
GET    /internal/v1/model-health
POST   /internal/v1/model-chat-completions
POST   /internal/v1/model-embeddings
POST   /internal/v1/model-rerankings
GET    /internal/v1/permission-evaluations
```

判断：

- 接口新增治理较好：没有发现“代码新增接口但契约未登记”的情况。
- 已补齐普通用户知识库详情和文件夹列表接口：`GET /internal/v1/knowledge-bases/{kb_id}`、`GET /internal/v1/knowledge-bases/{kb_id}/folders`。
- 剩余契约占位已降级标注：角色创建/修改/删除为 P1；权限评估为 P1；模型网关 HTTP 服务端接口为 P2。
- `docs/contracts/权限矩阵.md` 已明确说明角色创建、修改、删除属于 P1 契约占位，因此 3 个 role 写接口不应算 P0 缺陷，但应继续保留追踪。

### 2.2 自动化测试结果

本次执行：

```text
make PYTHON=.venv/bin/python test
```

结果：

```text
ruff check passed
354 passed in 9.06s
```

判断：

- 当前单元测试和静态检查通过。
- 本轮还执行了 `npm run typecheck:admin`，通过。
- 已新增 setup JWT 签发审计单测、setup-state 字段名回归测试、contract-only 接口阶段标注测试。
- `tests/e2e`、`tests/contract` 当前仍只有 `.gitkeep`，`tests/integration` 目前主要是 Qdrant indexing opt-in 联调测试；这不满足 `docs/testing/测试计划.md` 对完整 E2E、安全绕过、真实依赖故障注入和发布门禁的最终要求。

## 3. 按模块对照

| 范围 | 当前实现情况 | 与 docs 贴合度 | 缺口判断 |
| --- | --- | --- | --- |
| 初始化与 setup | 已有 setup state、config validation、initialization、setup JWT 服务、启动检查、setup guard；setup-state 字段已改为 `setup_token_expires_at`；setup JWT 签发已写审计。 | 高 | 正式 `rag-admin setup-token issue/rotate/revoke` CLI 未实现，已标为 P1 运维增强。 |
| 认证与会话 | 本地登录、JWT、refresh rotation、当前用户、改密、登出、管理员密码操作已落地；手写 JWT 已替换为 PyJWT。 | 高 | 需继续用安全绕过测试覆盖 setup/refresh/access token 错用。 |
| 配置管理 | active config、版本列表、新建、编辑、校验、激活、归档已落地；管理后台已可视化管理；文档已同步当前 API 和 P1/P2 边界。 | 高 | 完整审批、通用回滚、scoped override、Redis 热通知仍未落地，按现有 MVP 可归 P1/P2。 |
| 组织、用户、角色绑定 | 部门、用户、角色读取、角色绑定、部门管理员边界、菜单权限隐藏和普通员工管理后台阻断已多轮补齐；组织文档已改为当前 `modules/admin` 实现边界。 | 高 | 角色 CRUD 仍是契约占位。 |
| Permission Service | 权限上下文、知识库/文档权限、权限策略校验、权限下推、权限收紧刷新任务已落地。 | 中高 | `GET /internal/v1/permission-evaluations` 已降级为 P1 诊断接口占位；权限缓存失效、旧索引清理、权限收紧 fail-closed 需要更强 E2E/安全回归记录。 |
| 知识库与文档 | 知识库 CRUD、可见性、默认文档权限、文件夹、文档列表/详情/版本/chunk、权限修改、批量重建索引 UI 已落地。 | 高 | public KB detail/folders 已补齐；普通用户侧 PDF 页内高亮/完整对象预览仍是增强项。 |
| 导入与 Worker | 上传/URL/metadata_batch、任务查询/取消/重试、worker claim、阶段推进、heartbeat 方法、解析切块、embedding、index/publish 已落地。 | 中高 | OCR、复杂版式、表格保真、dead-letter 运维入口、独立 heartbeat/崩溃接管集成测试仍未完善。 |
| 索引服务 | draft/active、发布、权限 payload 刷新、cleanup、collection health、snapshot/create/recover、重建任务和管理后台入口已落地。 | 高 | 自动修复策略仍是后续增强；发布失败、旧索引清理、权限 payload 刷新需要真实环境回归记录继续补齐。 |
| 查询与答案 | 非流式、SSE token 级流式、关键词+向量召回、RRF、rerank、相关性 gate、Context Builder、LLM、citation 校验、降级回答、query/model logs 已落地。 | 中高 | query rewrite/expansion 未实现；限流、查询缓存、Redis 缓存策略更多停留在设计/保守实现；业务评测集还需要扩展。 |
| 模型网关 | embedding/rerank/chat client 已作为内部 adapter 使用，支持 auth token、timeout、错误归一化和日志。 | 中 | OpenAPI 中的模型网关 HTTP 服务端接口未挂载；如果不计划开放 service-token 模型代理，应从 P0 契约中降级。 |
| 审计与可观测 | 审计日志、查询日志、模型调用日志、管理后台诊断页面、敏感字段收敛已落地。 | 中高 | 指标、告警、审计导出、完整安全门禁仍未达到 docs 中的生产级设想。 |
| 前端与管理后台 | 普通查询页支持登录、知识库选择、多轮显示、会话同步/删除、流式查询；管理后台功能覆盖较完整。 | 中高 | 仍需做发布前 UI smoke；普通用户源文档高亮/全文预览能力不完整。 |
| 部署与运维 | Makefile、README、docker-compose、迁移、worker 启动、smoke/regression 命令已整理；P0 setup JWT 获取方式已统一为 API 启动日志。 | 中高 | `rag-admin` 运维 CLI 仍未实现；发布检查清单中备份恢复、E2E、故障注入仍偏人工。 |

## 4. 文档偏差

### 4.1 本轮已整改的文档偏差

- `docs/架构设计文档.md` 已从早期 `org`、`data_model`、`ports` 描述调整为当前 `api/routes`、`api/schemas`、`api/presenters`、`modules/*`、`adapters`、`db`、`shared` 边界。
- `docs/modules/01-初始化服务设计实现文档.md`、`docs/modules/13-部署与运维设计实现文档.md`、`docs/modules/15-前端与管理后台API设计实现文档.md` 已统一 setup JWT 说法，并明确当前 P0 通过 API 启动日志获取 setup JWT，`rag-admin` 为 P1。
- `docs/modules/05-配置服务设计实现文档.md` 已同步当前配置版本新建、编辑、校验、激活、归档和管理后台可视化输入。
- `docs/modules/03-组织服务设计实现文档.md` 已同步当前 `modules/admin` 组织/部门/用户部门关系实现边界。
- `docs/testing/P0真实联调记录.md` 已记录真实 `smoke-p0-record` 和 `query-regression-p0` 通过证据。
- `docs/contracts/openapi.yaml`、`docs/contracts/权限矩阵.md`、`docs/contracts/审计事件字典.md` 已同步 setup JWT、P1/P2 占位和模型网关边界。

### 4.2 docs 设计高于当前实现的内容

- 完整 CLI contract：`rag-admin setup-token issue/rotate/revoke`，已标为 P1。
- 模型 Provider Adapter HTTP 服务端：catalog、health、embedding、rerank、chat completions。
- 完整 E2E/security/integration/contract 自动化门禁。
- Redis 级权限缓存、查询缓存、限流、配置热通知。
- query rewrite/expansion、企业词库、同义词、评测趋势分析。
- OCR、复杂版式解析、表格结构保真和解析质量评估。
- 审批、通用配置回滚、配置 diff 工作台和 scoped config override。

## 5. 优先级建议

### P0：必须先收口

1. 处理 OpenAPI 的 contract-only 接口。已完成本轮收口：
   - role create/patch/delete：继续标注 P1 占位即可。
   - public KB detail/folders：已补实现。
   - permission-evaluations：已降级为 P1 诊断接口占位。
   - model gateway HTTP endpoints：已降级为 P2 模型代理接口占位，P0 仅保留内部 provider client / adapter。
2. setup JWT 当前 P0 边界已正式写入 docs：API 启动日志输出一次；`rag-admin` 为 P1。
3. docs 状态源已完成第一轮更新：把已实现、P1/P2 占位、明确不做的内容分开。
4. 下一步仍需补 P0 安全/E2E 验收门禁最小集：setup JWT 越权、refresh/access token 错用、跨部门文档越权、draft/deleted/blocked 命中阻断、权限收紧 fail-closed、LLM/rerank/embedding 降级。

### P1：完善可靠性

1. 给权限刷新、索引 cleanup、snapshot/recover、collection rebuild 补真实环境回归记录。
2. 补 Worker heartbeat/崩溃接管集成测试和 dead-letter 运维入口。
3. 扩展 query regression 数据集，加入低相关、拒答、权限边界、citation 来源可验证样例。
4. 评估是否实现 Redis 限流/缓存；如果当前阶段不启用 final answer cache，应在 docs 中明确“安全优先，P0 不启用最终答案缓存”。

### P2：增强体验和质量

1. query rewrite/expansion、企业词库、同义词和可配置融合参数。
2. OCR、复杂 PDF、表格保真和解析质量评估。
3. 普通用户侧 PDF 页内高亮、对象源文件预览体验。
4. 审计导出、指标面板、告警和在线评测平台。

## 6. 下一步执行建议

建议下一步继续做“自动化验收收口”，而不是继续堆业务功能：

1. 为已存在的 smoke/query regression 增加 CI 友好的环境前置检查和失败摘要。
2. 在 `tests/contract` 中落地 OpenAPI response/schema 的关键路径测试，而不是只做路由差集。
3. 在 `tests/e2e` 中补最小 P0 security/e2e：setup JWT 越权、refresh/access token 错用、跨部门文档越权、draft/deleted/blocked 命中阻断、权限收紧 fail-closed。
4. 再继续增强 OCR、rewrite、缓存和运维自动修复。

这条顺序的原因是：当前代码已经能跑主闭环，真正影响后续验收的是“契约和实际不一致”以及“高风险场景缺少自动化门禁”，不是单个业务页面再多加一个功能。
