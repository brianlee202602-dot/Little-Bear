# 后端重构计划清单

更新时间：2026-05-31

## 目标

本清单用于跟踪后端代码冗余治理与模块化结构治理工作，重点处理会影响维护成本、权限一致性、错误响应一致性、接口安全边界和业务模块边界的问题。

本轮重构不以减少代码行数为唯一目标。public/admin DTO、列表/详情 DTO、不同业务模块的服务边界，如果承担不同权限暴露职责，不应简单合并。

## 重构原则

- 路由层只负责 HTTP 入参、依赖注入、响应组装，不重复实现认证、错误响应、request_id 获取等基础能力。
- Service 层按业务边界拆分，不把用户、部门、角色、知识库、文档、索引、权限操作长期堆在一个大类中。
- 公共 DTO 放到 common schema，业务 DTO 保持各自暴露边界。
- 废弃接口必须有删除计划，不能长期只标记 deprecated。
- 运行时依赖构造统一封装，避免配置读取、secret 读取、adapter 构造散落在路由层。
- 模块之间只能通过清晰的 service/runtime/schema 边界协作，避免 A 模块的路由直接依赖 B 模块的内部管理 service。
- 空模块、占位模块和未落地的架构层要么补齐职责，要么删除，避免误导维护者。
- 重构必须保持现有行为可验证，先补单元测试，再迁移实现。

## 当前结构判断

结论：

- 后端已经具备基础模块化骨架：`api/routes`、`api/schemas`、`modules/*`、`shared`、`db`、`adapters` 分层清楚。
- 模块化执行不彻底：最大问题集中在 `admin` 大模块、`query/setup/import_pipeline/indexing/config` 核心 service 职责过重、route 层公共能力未抽离、permissions 路由反向依赖 admin、公共 schema 位置不合理、部分空模块没有真实职责。
- P4 实迁后仍残留 `core.py` / 兼容导出层。它们是拆分过程中的中间产物，不能作为最终模块化结果。后续必须删除这些兼容入口，并把调用方改到真实 service、repository、runtime、mapper 或 writer。
- 不能把所有重复都合并。public/admin DTO、列表/详情 DTO 是安全边界的一部分，应保留隔离，只做命名和 presenter 层优化。

## P0 必须处理

### P0-1 统一路由层认证与 request_id 处理

现状：

- `apps/api/app/api/routes/admin.py`、`knowledge.py`、`config.py`、`query.py`、`audit.py`、`auth.py`、`permissions.py`、`import_pipeline.py` 中重复定义 `_authenticate`、`_extract_bearer_token`、`_request_id`。
- 重复实现会导致认证错误、缺失 token、request_id fallback 行为不一致。

改造：

- 新增 `apps/api/app/api/dependencies/auth.py`。
- 提供 `get_bearer_token`、`authenticate_required_scope`、`current_request_id`。
- 路由层通过统一 helper 获取 `AuthContext`。

验收：

- 全局只保留一份 bearer token 解析逻辑。
- 各路由不再定义重复 `_extract_bearer_token`。
- 原有认证相关单测全部通过。

产出：

- 新增 `apps/api/app/api/dependencies/auth.py`。
- route 层统一使用 `extract_bearer_token`、`authenticate_required_scope`、`current_request_id`、`current_trace_id`。
- route 内部不再重复定义 `_authenticate`、`_extract_bearer_token`、`_request_id`。
- 相关 route 单测改为 patch 公共认证 dependency。

状态：已完成（2026-05-30）

### P0-2 统一 API 错误响应构造

现状：

- 多个路由重复定义 `_auth_error_response`、`_database_error_response`、`_admin_error_response`、`_query_error_response` 等。
- 响应结构相似，但状态码、error_code、details 暴露策略分散。

改造：

- 新增 `apps/api/app/api/errors.py`。
- 统一提供：
  - `service_error_response(exc, stage)`
  - `database_error_response(exc, stage, error_code, message)`
  - `confirmation_required_response(stage, message)`
- 保留各模块业务 error_code，但响应结构由公共函数生成。

验收：

- 路由层不再重复构造 `JSONResponse` 错误体。
- 错误响应仍包含 `request_id`、`error_code`、`message`、`stage`、`retryable`、`details`。
- 不向普通用户接口泄漏数据库驱动细节。

产出：

- 新增 `apps/api/app/api/errors.py`。
- 统一提供 `structured_error_response`、`service_error_response`、`database_error_response`、`confirmation_required_response`。
- admin、audit、auth、config、import、knowledge、permissions、query、setup route 使用公共错误响应构造。

状态：已完成（2026-05-30）

### P0-3 统一 ServiceError 基类

现状：

- `AdminServiceError`、`QueryServiceError`、`KnowledgeServiceError`、`ImportServiceError`、`AuditServiceError` 等结构高度相似。
- `ConfigServiceError` 缺少 `status_code`，需要路由层额外做 `_config_status_code` 映射。

改造：

- 在 `apps/api/app/shared/errors.py` 或 `apps/api/app/modules/common/errors.py` 中新增 `ServiceError` 基类。
- 字段统一为 `error_code`、`message`、`status_code`、`retryable`、`details`。
- 各业务模块错误类继承该基类。

验收：

- 路由错误响应可以统一处理所有业务 ServiceError。
- `ConfigServiceError` 不再需要特殊状态码映射。
- 现有错误码不变。

产出：

- `apps/api/app/shared/errors.py` 新增 `ServiceError`。
- 各业务 `*ServiceError` 继承统一基类。
- `ConfigServiceError`、`AuditServiceError` 状态码映射下沉到错误类。
- 新增 `tests/unit/test_service_errors.py`。

状态：已完成（2026-05-30）

### P0-4 统一 ObjectStorage 运行时构造

现状：

- `admin.py` 和 `knowledge.py` 中都有 `_object_storage_or_none`。
- `import_pipeline/runtime.py` 中也重复读取 active_config、解析 MinIO 配置、读取 secret 并创建 `MinioObjectStorage`。

改造：

- 新增 `apps/api/app/modules/storage/runtime.py`。
- 提供：
  - `build_object_storage(session, *, required: bool = False)`
  - `build_object_storage_from_config(session, config, *, required: bool = False)`
- 路由和导入运行时统一调用该 runtime。

验收：

- MinIO 配置解析和 secret 获取只保留一套实现。
- citation/source 读取、admin 文档预览、导入 worker 行为一致。
- MinIO 配置缺失时，required 场景抛结构化错误，optional 场景返回 `None`。

产出：

- 新增 `apps/api/app/modules/storage/runtime.py`，统一 active_config、storage provider、MinIO 配置和 secret 读取。
- `admin.py`、`knowledge.py` 的 `_object_storage_or_none` 改为委托 `build_object_storage(..., required=False)`。
- `import_pipeline/runtime.py` 改为委托 `build_object_storage_from_config(..., required=True)`，导入场景继续使用结构化 `ImportServiceError`。
- 更新 `tests/unit/test_import_runtime.py` 以 patch 新的对象存储运行时入口。

状态：已完成（2026-05-30）

## P1 结构拆分

### P1-1 拆分 AdminService

现状：

- `apps/api/app/modules/admin/service.py` 约 7357 行。
- 同一个类混合用户、部门、角色、知识库、文档、索引运维、权限策略、审计写入。
- 这是当前后端最大的维护风险。

改造：

- 按业务边界拆分：
  - `admin/users_service.py`
  - `admin/departments_service.py`
  - `admin/roles_service.py`
  - `admin/knowledge_bases_service.py`
  - `admin/documents_service.py`
  - `admin/index_ops_service.py`
  - `admin/permission_admin_service.py`
- 保留一个轻量 `AdminFacade` 过渡，避免一次性改爆路由。

验收：

- 单个 service 文件建议控制在 1500 行以内。
- 用户/部门/角色改动不会影响知识库/索引相关服务。
- 高风险权限校验逻辑仍集中复用，不散落复制。

进度更新（2026-05-30）：

- 已完成第一轮边界拆分。
- 原 `admin/service.py` 移动为 `admin/core.py`，保留历史 SQL 和事务实现作为兼容核心。
- 新增 `admin/service.py` facade，`AdminService`、`AdminActorContext`、`RoleBindingInput`、`_merge_scopes`、`_is_high_risk_role` 原导入路径保持不变。
- 已拆出领域委托服务：
  - `admin/users_service.py`：用户、密码、解锁、用户部门关系。
  - `admin/departments_service.py`：部门列表、详情、新增、修改、删除。
  - `admin/roles_service.py`：角色、可分配角色、角色绑定。
  - `admin/knowledge_bases_service.py`：知识库列表、详情、新增、修改、删除。
  - `admin/folders_service.py`：文件夹列表、详情、新增、修改、删除。
  - `admin/documents_service.py`：文档列表、详情、版本、chunk、预览、文档修改和删除。
  - `admin/index_ops_service.py`：文档、知识库、collection 索引重建和清理任务。
  - `admin/permission_admin_service.py`：兼容旧 `AdminService` 权限策略方法；正式权限路由已使用 `modules.permissions.PermissionAdminService`。
- 当前 `admin/core.py` 仍保留大体量历史实现，后续如继续收敛，需要把各领域委托服务中的方法从 core 逐步实迁，而不是继续扩大 core。

后续实迁（2026-05-30）：

- 已完成部门域实迁：
  - `admin/departments_service.py` 从委托壳改为真实部门管理实现。
  - `admin/core.py` 已移除 `list_departments`、`list_department_options`、`get_department`、`create_department`、`patch_department`、`delete_department` 六个部门公开方法。
  - 部门域继续复用 core 中的共享能力：权限检查、`org_version` / `permission_version` 递增、审计写入、DTO mapper 和数据库错误包装。
- 已完成文件夹域实迁：
  - `admin/folders_service.py` 从委托壳改为真实文件夹管理实现。
  - `admin/core.py` 已移除 `list_folders`、`list_folder_options`、`get_folder`、`create_folder`、`patch_folder`、`delete_folder` 六个文件夹公开方法。
  - 文件夹域继续复用 core 中的共享能力：知识库准入、文件夹加载/父级校验、访问阻断、索引清理任务、审计写入、DTO mapper 和数据库错误包装。
- 已完成角色域实迁：
  - `admin/roles_service.py` 从委托壳改为真实角色和角色绑定管理实现。
  - `admin/core.py` 已移除 `list_roles`、`list_assignable_role_options`、`get_role`、`list_role_bindings`、`create_role_bindings`、`replace_role_bindings`、`revoke_role_binding` 七个角色公开方法。
  - 角色域继续复用 core 中的共享能力：角色加载、角色绑定插入、作用域校验、最后一个 `system_admin` 保护、`permission_version` 递增、审计写入、DTO mapper 和数据库错误包装；角色绑定读取已下沉到 `admin/role_binding_reader.py`。
- 已完成知识库域实迁：
  - `admin/knowledge_bases_service.py` 从委托壳改为真实知识库管理实现。
  - `admin/core.py` 已移除 `list_knowledge_bases`、`list_knowledge_base_options`、`get_knowledge_base`、`create_knowledge_base`、`patch_knowledge_base`、`delete_knowledge_base` 六个知识库公开方法。
  - 知识库域继续复用 core 中的共享能力：部门解析、知识库加载、知识库管理范围校验、默认文档权限边界校验、访问规则替换、资源策略/权限快照、访问阻断、索引删除任务、权限版本递增、审计写入、DTO mapper 和数据库错误包装。
- 已完成文档域实迁：
  - `admin/documents_service.py` 从委托壳改为真实文档管理实现。
  - `admin/core.py` 已移除 `list_documents`、`get_document`、`list_document_versions`、`list_document_chunks`、`get_document_preview`、`list_document_index_versions`、`patch_document`、`delete_document` 八个文档公开方法。
  - 文档域一并接管了文档预览 chunk 文本读取 helper；继续复用 core 中的共享能力：文档/知识库/文件夹加载、部门解析、文档权限边界校验、资源策略/权限快照、访问阻断、索引删除任务、权限版本递增、审计写入、DTO mapper 和数据库错误包装。
- 已完成索引运维域实迁：
  - `admin/index_ops_service.py` 从委托壳改为真实索引运维任务实现。
  - `admin/core.py` 已移除 `create_document_index_rebuild_job`、`create_index_rebuild_job`、`create_collection_index_rebuild_job`、`create_index_version_cleanup_job` 四个索引运维公开方法。
  - 索引运维域继续复用 core 中的共享能力：文档/知识库加载、索引任务入队、文档索引状态更新、审计写入和数据库错误包装；索引重建/清理 target 读取已下沉到 `admin/index_target_reader.py`。
- 已完成兼容权限策略域实迁：
  - `admin/permission_admin_service.py` 从委托壳改为真实兼容权限策略实现。
  - `admin/core.py` 已移除 `replace_knowledge_base_permissions`、`replace_document_permissions` 两个兼容权限公开方法。
  - 兼容权限策略域继续复用 core 中的共享能力：知识库/文档加载、权限管理校验、部门解析、知识库访问规则替换、资源策略/权限快照、权限刷新任务、审计写入和数据库错误包装；资源权限版本读取已下沉到 `admin/permission_version_reader.py`。
- 已完成用户域实迁：
  - `admin/users_service.py` 从委托壳改为真实用户管理实现。
  - `admin/core.py` 已移除 `list_users`、`get_user`、`create_user`、`patch_user`、`delete_user`、`reset_user_password`、`unlock_user`、`list_user_departments`、`replace_user_departments` 九个用户公开方法。
  - 用户域继续复用 core 中的共享能力：用户加载、角色/部门加载、默认角色和默认部门解析、部门/角色绑定写入、用户范围校验、最后一个 `system_admin` 保护、权限版本/组织版本递增、token 吊销、审计写入和数据库错误包装。
- 已完成 DTO mapper 与角色策略 helper 拆分：
  - 新增 `admin/mappers.py`，集中管理 admin 数据库 row 到 DTO 的转换，以及索引重建/清理 target 私有数据结构。
  - 新增 `admin/role_policy.py`，集中管理 `_merge_scopes`、高风险角色/权限判断和 `system_admin` 检测。
  - 各领域 service 不再从 `admin/core.py` 导入 mapper 与角色策略 helper；`admin/core.py` 仅在内部加载资源时复用这些模块。
- 已完成通用策略、事件和工具 helper 拆分：
  - 新增 `admin/policies.py`，集中管理知识库可见性、文档可见性、默认文档权限边界、知识库访问规则归一化和权限收紧/放宽判断。
  - 新增 `admin/events.py`，集中管理知识库、文件夹、文档更新时的审计事件名、动作和风险等级判断。
  - 新增 `admin/utils.py`，集中管理输入归一化、可选值解析、文件夹路径工具、用户名脱敏和数据库错误包装。
  - 各领域 service 不再为了纯函数 helper 依赖 `admin/core.py`；`admin/core.py` 继续保留资源加载、写入和权限校验 glue。
- 已完成访问控制 glue 拆分：
  - `AdminActorContext`、`RoleBindingInput` 已迁入 `admin/access_control.py`，`admin/service.py` 继续对外兼容导出。
  - 新增 `AdminAccessControlMixin`，保留历史 `AdminService._ensure_actor_can_*` 兼容入口，但实现不再放在 `admin/core.py`。
  - 知识库管理列表 ACL SQL、兼容权限 actor context 提升逻辑已迁入 `admin/access_control.py`。
  - 各领域 service 不再从 `admin/core.py` 导入权限上下文类型或访问控制纯函数；`admin/core.py` 只通过 mixin 获得兼容方法。
- 已完成资源读取 helper 拆分：
  - 新增 `admin/resource_loaders.py` 和 `AdminResourceLoaderMixin`。
  - 已迁出用户、知识库、文件夹、文档、用户角色、用户部门、部门解析、文件夹解析、角色解析等 `_load_*` / `_resolve_*` 方法。
  - `admin/core.py` 通过 mixin 保留历史方法名，现有领域 service 与测试 monkeypatch 路径保持兼容。
  - 新增 `admin/role_binding_reader.py`，迁出角色绑定列表、单条绑定和用户角色存在性读取。
  - 新增 `admin/permission_version_reader.py`，迁出资源权限版本读取。
  - 新增 `admin/index_target_reader.py`，迁出索引重建 target 与索引清理 target 读取。
  - `_load_role_bindings`、`_load_role_binding`、`_load_resource_permission_version`、`_load_index_*_targets*` 兼容 wrapper 已并入 `AdminResourceLoaderMixin`；`admin/core.py` 只负责初始化对应 reader。
- 已完成成员/角色绑定写入 helper 拆分：
  - 新增 `admin/role_binding_writer.py` 和 `AdminRoleBindingWriterMixin`。
  - 已迁出 `_insert_department_membership`、`_insert_role_binding`、`_ensure_role_binding_scope_exists`、`_ensure_not_last_system_admin`。
  - `admin/core.py` 通过 mixin 保留历史方法名，现有领域 service 与测试 monkeypatch 路径保持兼容。
- 已完成权限/资源策略写入 helper 拆分：
  - 新增 `admin/permission_writer.py` 和 `AdminPermissionWriterMixin`。
  - 已迁出 `_replace_knowledge_base_access_rules`、`_replace_resource_policy`、`_insert_permission_snapshot`、`_insert_access_block`。
  - `admin/core.py` 通过 mixin 保留历史方法名，知识库、文档、文件夹和兼容权限策略 service 的调用路径保持兼容。
- 已完成索引/权限刷新任务写入 helper 拆分：
  - 新增 `admin/index_job_writer.py` 和 `AdminIndexJobWriterMixin`。
  - 已迁出 `_enqueue_index_delete_job`、`_enqueue_permission_refresh_job`、`_enqueue_index_rebuild_job`、`_enqueue_index_rebuild_batch_job`、`_enqueue_index_version_cleanup_job`、`_mark_index_rebuild_targets_indexing`。
  - `admin/core.py` 通过 mixin 保留历史方法名，文档/文件夹/知识库删除、权限刷新和索引运维 service 的调用路径保持兼容。
- 已完成跨域状态写入 helper 拆分：
  - 新增 `admin/state_writer.py` 和 `AdminStateWriterMixin`。
  - 已迁出 `_bump_permission_version`、`_bump_org_version`、`_revoke_user_tokens`、`_insert_audit_log`。
  - `admin/core.py` 通过 mixin 保留历史方法名，用户、部门、角色、知识库、文档、索引运维和兼容权限策略 service 的调用路径保持兼容。
- 已完成权限边界 guard 与文件夹计数 helper 拆分：
  - 新增 `admin/permission_guard.py` 和 `AdminPermissionGuardMixin`。
  - 已迁出 `_ensure_default_document_permission_within_kb_access`、`_ensure_document_permission_within_parent_knowledge_base`。
  - 新增 `admin/folder_document_counter.py` 和 `AdminFolderDocumentCounterMixin`。
  - 已迁出 `_count_folder_documents`。
  - `admin/core.py` 现在仅保留 mixin 组合和依赖初始化，不再承载具体业务 SQL 或权限校验实现。
- 已完成 facade/core 命名与说明复核：
  - `admin/core.py` 主类改名为 `AdminCoreService`，并保留 `AdminService = AdminCoreService` 兼容旧直连导入。
  - `admin/service.py` facade 改为继承 `AdminCoreService`，说明已改为“领域 workflow 在专用 service，core 只保留兼容 helper 和运行时依赖”。
  - `admin/__init__.py` 保留历史 reader 导出，暂不做破坏性收紧；公共入口仍是 `admin.service.AdminService`。
- `admin/core.py` 行数从约 7162 行降至约 51 行。

验证：

- `ruff check apps/api/app/modules/admin apps/api/app/api/routes/admin.py tests/unit/test_admin_service.py tests/unit/test_admin_route.py` 通过。
- `pytest -q tests/unit/test_admin_service.py tests/unit/test_admin_route.py` 通过，101 passed。
- 后续实迁验证：
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_admin_service.py tests/unit/test_admin_route.py`：101 passed。
  - `make test`：349 passed。

状态：已完成；core 内部公开业务实现实迁完成（用户域、部门域、文件夹域、角色域、知识库域、文档域、索引运维域、兼容权限策略域已完成），core 私有 helper 归类拆分已完成（DTO mapper、角色策略、通用策略、事件、工具 helper、访问控制 glue、资源加载 helper、角色绑定读取、成员/角色绑定写入、权限/资源策略写入、索引/权限刷新任务写入、跨域状态写入、权限边界 guard、文件夹计数、资源权限版本读取、索引 target 读取已完成），facade/core 命名复核已完成。

### P1-2 拆分 admin route

原状：

- `apps/api/app/api/routes/admin.py` 约 2266 行。
- 同一个 route 文件包含管理后台大部分功能，DTO 映射函数也堆在文件底部。

改造：

- 已拆成：
  - `api/routes/admin_users.py`
  - `api/routes/admin_departments.py`
  - `api/routes/admin_roles.py`
  - `api/routes/admin_knowledge.py`
  - `api/routes/admin_documents.py`
  - `api/routes/admin_index_ops.py`
- 新增 `api/routes/admin_shared.py` 承载共享认证、错误响应、DTO mapper 和兼容 helper。
- `api/routes/admin.py` 已改为聚合入口，只负责 include 子路由并保留历史测试/替换点所需的 `AdminService`、`session_scope`、`build_index_ops_service`、`_object_storage_or_none` 导出。
- 子路由 URL 路径保持 `/internal/v1/admin/...` 不变，前端无需调整接口路径。
- 已完成子路由共享依赖收尾：
  - `admin_users.py`、`admin_departments.py`、`admin_roles.py`、`admin_knowledge.py`、`admin_documents.py`、`admin_index_ops.py` 不再使用 `from admin_shared import *`。
  - 每个子路由改为显式导入自身需要的认证 helper、错误响应 helper、presenter、schema 和兼容 helper。
  - `admin.py` 继续保留兼容导出，确保现有测试 monkeypatch 路径和外部旧引用不被破坏。

验收：

- admin 主路由文件不再承担所有后台接口。
- 每个 route 文件只包含一个功能域。
- URL 路径保持兼容，前端不需要因文件拆分改接口路径。

验证：

- `ruff check apps/api/app/api/routes/admin.py apps/api/app/api/routes/admin_shared.py apps/api/app/api/routes/admin_users.py apps/api/app/api/routes/admin_departments.py apps/api/app/api/routes/admin_knowledge.py apps/api/app/api/routes/admin_documents.py apps/api/app/api/routes/admin_index_ops.py apps/api/app/api/routes/admin_roles.py tests/unit/test_admin_route.py` 通过。
- `pytest -q tests/unit/test_admin_route.py` 通过，42 passed。
- `pytest -q tests/unit/test_admin_service.py tests/unit/test_admin_route.py` 通过，101 passed。
- `make test` 通过，349 passed。
- 后续依赖收尾验证：
  - `ruff check apps/api/app/api/routes/admin.py apps/api/app/api/routes/admin_shared.py apps/api/app/api/routes/admin_users.py apps/api/app/api/routes/admin_departments.py apps/api/app/api/routes/admin_knowledge.py apps/api/app/api/routes/admin_documents.py apps/api/app/api/routes/admin_index_ops.py apps/api/app/api/routes/admin_roles.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_admin_route.py tests/unit/test_admin_service.py` 通过，101 passed。

状态：已完成（路由文件已按领域拆分；DTO mapper 已在 P1-3 抽离到 presenter；子路由共享依赖已显式化）

### P1-3 抽离 DTO presenter/mapper

原状：

- `admin.py` 中存在大量 `_xxx_data` 函数。
- `knowledge.py`、`query.py`、`audit.py` 中也有类似 DTO 转换函数。

改造：

- 已新增：
  - `api/presenters/admin.py`
  - `api/presenters/knowledge.py`
  - `api/presenters/query.py`
  - `api/presenters/audit.py`
- `admin_shared.py` 只保留管理后台路由共享依赖、认证 helper、错误响应和兼容导出；管理后台响应 DTO 映射已迁入 `api/presenters/admin.py`。
- `knowledge.py`、`query.py`、`audit.py` 已删除本地 `_xxx_data` 字段映射函数，改为调用对应 presenter。
- `query.py` 中的公开 debug id 生成逻辑也随响应呈现逻辑迁入 `api/presenters/query.py`。

验收：

- route 文件明显变短。
- DTO 映射逻辑可独立单测。
- 列表 DTO 和详情 DTO 仍保持字段隔离。

验证：

- `ruff check apps/api/app/api/presenters apps/api/app/api/routes/admin_shared.py apps/api/app/api/routes/knowledge.py apps/api/app/api/routes/query.py apps/api/app/api/routes/audit.py apps/api/app/api/routes/admin.py apps/api/app/api/routes/admin_users.py apps/api/app/api/routes/admin_departments.py apps/api/app/api/routes/admin_knowledge.py apps/api/app/api/routes/admin_documents.py apps/api/app/api/routes/admin_index_ops.py apps/api/app/api/routes/admin_roles.py` 通过。
- `pytest -q tests/unit/test_admin_route.py tests/unit/test_knowledge_route.py tests/unit/test_query_route.py tests/unit/test_audit_route.py` 通过，59 passed。
- `make test` 通过，349 passed。

状态：已完成

### P1-4 解除 permissions 路由对 AdminService 的反向依赖

现状：

- `apps/api/app/api/routes/permissions.py` 直接导入 `app.modules.admin.service.AdminService`。
- 权限策略管理接口依赖 admin schemas 和 admin service，说明权限管理能力并没有真正沉到 `permissions` 模块。
- 这会让权限模块边界倒置：后续拆分 AdminService 时，permissions 路由会被 admin 内部实现牵连。

改造：

- 在 `apps/api/app/modules/permissions` 下补充管理侧服务，例如 `PermissionAdminService`。
- 将 `replace_knowledge_base_permissions`、`replace_document_permissions`、权限快照、权限版本递增等逻辑从 `AdminService` 中迁出或通过专用接口封装。
- `api/routes/permissions.py` 只依赖 `modules/permissions`、`modules/auth` 和必要的公共 schema，不再导入 `modules/admin`。

验收：

- `routes/permissions.py` 不再出现 `from app.modules.admin...`。
- 文档权限、知识库权限修改行为保持一致。
- 权限收紧后的索引 payload 刷新闭环不受影响。

产出：

- 新增 `apps/api/app/modules/permissions/admin_schemas.py`，定义权限管理写侧 actor、知识库权限策略、文档权限策略和访问规则结构。
- 新增 `apps/api/app/modules/permissions/admin_service.py`，承接知识库权限替换、文档权限替换、权限版本递增、资源策略归档/写入、权限快照、权限刷新任务和审计写入。
- `apps/api/app/api/routes/permissions.py` 已移除 `modules/admin` 相关导入，改为依赖 `PermissionAdminService`、`PermissionServiceError` 和权限模块 schema。
- `tests/unit/test_permission_route.py` 已改为 patch `PermissionAdminService`，避免路由测试继续绑定 `AdminService`。
- 新增 `tests/unit/test_permission_admin_service.py`，覆盖权限写服务的 scope 校验、知识库权限替换、文档权限替换关键路径。

验证：

- `.venv/bin/python -m ruff check apps/api/app/modules/permissions apps/api/app/api/routes/permissions.py tests/unit/test_permission_route.py --fix`
- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_permission_route.py tests/unit/test_permission_admin_service.py`
- `make test`：349 passed。

状态：已完成（2026-05-30）

### P1-5 建立模块化 route 注册层

现状：

- `main.py` 直接 import 并 include 所有 router。
- 目前还能接受，但随着 admin route 拆分，入口文件会继续增长。

改造：

- 在 `apps/api/app/api/routes/__init__.py` 提供 `include_api_routes(app)`。
- `main.py` 只调用统一注册函数。
- admin 拆分后的多个 router 在 routes 包内部聚合。

验收：

- `main.py` 不再直接维护全部 router 列表。
- 新增或拆分路由时，只修改 routes 包内部注册逻辑。
- FastAPI URL 路径保持不变。

产出：

- `apps/api/app/api/routes/__init__.py` 新增 `include_api_routes(app)`。
- `apps/api/app/main.py` 只调用统一路由注册函数。

状态：已完成（2026-05-30）

### P1-6 拆分 ConfigService

现状：

- `apps/api/app/modules/config/service.py` 约 1461 行。
- 模块已有 `cache.py`、`probe.py`、`validator.py`，说明配置模块不是完全没抽象。
- 但 `ConfigService` 本身仍混合了 active_config 读取、配置项列表、版本创建、版本编辑、发布激活、归档、依赖探测、bootstrap 状态持久化、审计写入。
- 这会导致配置读取侧和配置管理侧耦合过重。运行时模块只需要稳定读取 active_config，不应被配置版本管理复杂度牵连。

改造：

- 拆分为：
  - `config/reader.py`：读取 active_config、缓存、section 查询。
  - `config/version_service.py`：配置版本创建、编辑、查询、归档。
  - `config/publisher.py`：配置发布、active version 切换、旧版本状态处理。
  - `config/dependency_validator.py`：配置依赖探测和 bootstrap 校验。
  - `config/repository.py`：集中 SQL 读写。
- 保留 `ConfigService` 作为短期 facade，逐步迁移调用方。

验收：

- 运行时读取 active_config 的调用不依赖版本管理实现。
- 配置发布事务仍保持原子性。
- 初始化、配置新建、配置编辑、配置激活的校验结果保持一致。

产出：

- 新增 `apps/api/app/modules/config/repository.py`，集中 system_state、system_configs、config_versions 的 SQL 读写。
- 新增 `apps/api/app/modules/config/reader.py`，负责 active_config 快照读取、schema 校验和元数据/hash 校验。
- 新增 `apps/api/app/modules/config/dependency_validator.py`，负责 schema + bootstrap dependency 校验和 bootstrap 状态持久化。
- 新增 `apps/api/app/modules/config/version_service.py`，负责配置项列表、草稿、新建版本、编辑版本、归档版本。
- 新增 `apps/api/app/modules/config/publisher.py`，负责发布校验、active version 切换、旧版本失活和发布审计。
- `apps/api/app/modules/config/service.py` 收敛为 facade，保留原 public API 和必要 private helper 委托，避免现有调用方大面积改动。
- 新增 `apps/api/app/modules/config/constants.py`、`utils.py`，拆出风险等级常量和配置转换/审计辅助函数。

状态：已完成（2026-05-30）

### P1-7 拆分 ImportService

现状：

- `apps/api/app/modules/import_pipeline/service.py` 约 2507 行。
- 模块已有 `executors.py` 和 `runtime.py`，parser/cleaner/chunker 也有 Protocol，说明导入执行器层已有抽象。
- 但 `ImportService` 同时承担：
  - 上传校验和导入任务创建。
  - 文档、版本、权限快照写入。
  - worker claim、heartbeat、cancel、retry。
  - 导入阶段状态机推进。
  - parse/clean/chunk 阶段副作用。
  - 索引任务调用。
  - 审计日志写入。
- 这会让 API 侧导入创建和 Worker 侧执行推进强耦合。

改造：

- 拆分为：
  - `import_pipeline/command_service.py`：创建导入任务、取消、重试。
  - `import_pipeline/worker_service.py`：claim、heartbeat、advance、mark_failed。
  - `import_pipeline/stage_runner.py`：validate/parse/clean/chunk/embed/index/publish/cleanup 阶段执行。
  - `import_pipeline/document_writer.py`：文档、版本、chunk、对象 key 写入。
  - `import_pipeline/permission_guard.py`：导入权限、owner department、KB 可访问校验。
  - `import_pipeline/repository.py`：集中 SQL。
- `ImportService` 短期保留为 facade 或删除后由 route/worker 分别依赖专用 service。

验收：

- API 创建导入任务不依赖 worker 状态推进实现。
- Worker 状态机可以独立单测。
- 权限校验逻辑不再散落在 service 底部函数。
- 上传、URL、metadata_batch 三类导入行为保持兼容。

产出：

- `apps/api/app/modules/import_pipeline/service.py` 收敛为 facade，按 API 命令侧、重试侧、Worker 侧委托。
- 新增 `apps/api/app/modules/import_pipeline/core.py`，保留原导入 SQL、状态机和私有 helper 作为兼容内核，保证现有测试 patch 点和 worker 行为不变。
- 新增 `apps/api/app/modules/import_pipeline/command_service.py`，承接上传校验、导入创建、任务详情、任务列表和取消请求入口。
- 新增 `apps/api/app/modules/import_pipeline/retry_service.py`，承接普通导入重试和批量索引重建重试入口。
- 新增 `apps/api/app/modules/import_pipeline/worker_service.py`，承接 claim、advance、mark_failed、heartbeat 等 Worker 生命周期入口。
- 新增 `apps/api/app/modules/import_pipeline/stage_runner.py`，建立阶段执行边界；本阶段仍委托兼容 core，后续可继续把 parse/clean/chunk/embed/index/publish/cleanup 阶段逐步迁出。
- 新增 `apps/api/app/modules/import_pipeline/document_writer.py`，建立文档、版本、chunk、对象存储写入边界；本阶段保留 core 兼容。
- 新增 `apps/api/app/modules/import_pipeline/permission_guard.py`，建立导入 owner department、KB 访问和文档权限策略校验边界；本阶段保留 core 兼容。
- 保留 `app.modules.import_pipeline.service.build_indexing_service` 这个历史 monkeypatch 点，避免 worker 阶段测试和扩展点失效。

状态：已完成（2026-05-30）

### P1-8 拆分 IndexingService

现状：

- `apps/api/app/modules/indexing/service.py` 约 1677 行。
- 模块已有 `VectorIndexWriter` Protocol、`NoopVectorIndexWriter`、`runtime.py`、`ops_service.py`，说明索引模块已有部分抽象。
- 但核心 `IndexingService` 仍混合：
  - 索引目标加载。
  - draft index version 创建。
  - keyword index 写入。
  - vector draft point 写入。
  - ready index 发布。
  - active/archive 状态切换。
  - pending_delete 清理。
  - 权限 payload 刷新。
  - 审计日志写入。
- 这会让导入发布、重建索引、权限刷新、索引清理相互牵连。

改造：

- 拆分为：
  - `indexing/target_repository.py`：索引目标、chunk、ready version、pending delete 查询。
  - `indexing/draft_service.py`：draft index version 创建、keyword/ref/vector draft 写入。
  - `indexing/publish_service.py`：ready version 发布、active 状态切换。
  - `indexing/cleanup_service.py`：pending_delete 清理、向量删除。
  - `indexing/permission_payload_service.py`：权限 payload 刷新闭环。
  - `indexing/audit_writer.py` 或复用统一 AuditWriter。
- 保留 `IndexingService` facade，避免 import worker 和 admin 索引任务一次性大改。

验收：

- 导入 publish、后台重建索引、权限刷新、索引清理可分别测试。
- VectorStore 写入端口仍通过 Protocol 注入。
- Qdrant 运维相关能力继续留在 `ops_service.py`，不混入索引发布主流程。

产出：

- `apps/api/app/modules/indexing/service.py` 收敛为 facade，仅持有运行时参数、VectorStore writer 和主流程委托。
- 新增 `apps/api/app/modules/indexing/core.py`，保留原 SQL helper、VectorIndexWriter Protocol、Noop writer、payload/hash helper，作为本阶段兼容内核。
- 新增 `apps/api/app/modules/indexing/draft_service.py`，负责 draft index version 创建、keyword/ref 写入和 draft vector point 写入。
- 新增 `apps/api/app/modules/indexing/publish_service.py`，负责 ready index 发布、active/pending_delete 状态切换、文档和 chunk 激活。
- 新增 `apps/api/app/modules/indexing/cleanup_service.py`，负责 pending_delete 账本清理、keyword/ref 标记删除和向量物理删除。
- 新增 `apps/api/app/modules/indexing/permission_payload_service.py`，负责权限 payload 刷新、access block 释放和 vector payload 更新。
- `runtime.py`、import worker、admin 索引任务继续依赖 `IndexingService` facade，调用入口保持兼容。

状态：已完成（2026-05-30）

### P1-9 拆分 QueryService

现状：

- `apps/api/app/modules/query/service.py` 约 1899 行。
- 模块已有 `runtime.py` 和 `conversations.py`，但核心 `QueryService` 仍混合：
  - 查询主流程编排。
  - 权限上下文构建和候选准入。
  - active index version 查询。
  - keyword search SQL。
  - vector/rerank 融合。
  - citation 校验。
  - 降级策略和降级答案生成。
  - query log、model_call_log、audit_log 写入。
- 这会导致检索策略、权限策略、日志策略、降级策略互相牵连，后续优化 RAG 链路时容易扩大改动面。

改造：

- 拆分为：
  - `query/orchestrator.py`：查询主流程编排。
  - `query/repository.py`：active config/index、keyword search、候选元数据查询。
  - `query/retrieval_pipeline.py`：关键词、向量、融合、rerank 流程。
  - `query/citation_validator.py`：citation 校验和风险判断。
  - `query/degrade_policy.py`：降级原因归一、降级答案生成。
  - `query/log_writer.py`：query log、model_call_log、audit log 写入。
- 保留 `QueryService` facade，保证 route 和 stream plan 不一次性大改。

验收：

- 非流式查询和流式查询行为保持一致。
- query regression 和 smoke 测试通过。
- 权限拒绝、无结果、rerank 不可用、LLM 不可用、citation 缺失等降级场景仍有明确回答。

进度更新（2026-05-30）：

- 已完成第一轮结构拆分。
- 原 `query/service.py` 移动为 `query/core.py`，保留原查询编排作为兼容核心。
- 新增 `query/service.py` facade，`QueryService` 和 `QueryStreamPlan` 的原导入路径保持不变。
- 已拆出：
  - `query/orchestrator.py`：公开查询流程入口委托，隔离 route 与核心编排。
  - `query/repository.py`：active config、active index、keyword search、候选元数据、rerank 文本读取。
  - `query/retrieval_pipeline.py`：候选权限 gate 与 rerank 输入组装。
  - `query/citation_validator.py`：citation 校验、自动补引用、展示清理和风险等级。
  - `query/degrade_policy.py`：降级答案和降级原因中文描述。
  - `query/log_writer.py`：query log、model_call_log、query audit log 写入。
- facade 保留构造参数兼容：
  - `permission_service`
  - `vector_retriever`
  - `candidate_reranker`
  - `rerank_input_top_k`
  - `rerank_min_score`
  - `fusion_service`
  - `context_builder`
  - `answer_service`
- 当前仍保留 `query/core.py` 作为兼容编排层；后续如需要继续瘦身，可把非流式、流式 plan、流式 finalize 三段编排从 core 迁入 `orchestrator.py`。

验证：

- `ruff check apps/api/app/modules/query apps/api/app/api/routes/query.py tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过。
- `pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
- `make test` 通过，346 passed。
- 未执行真实环境 `query-regression` / `smoke`；后续验收阶段仍需补跑。

状态：已完成（代码拆分完成，真实环境回归待验收）

### P1-10 拆分 SetupInitializationService

现状：

- `apps/api/app/modules/setup/initialize_service.py` 约 1105 行。
- `SetupInitializationService` 混合：
  - setup payload 校验。
  - provider secret 写入。
  - bootstrap 检查。
  - 企业、部门、用户、凭证、角色初始化。
  - active_config 写入。
  - recovery setup。
  - system_state 更新。
  - audit 写入。
- 初始化链路属于高风险链路，当前实现可用，但职责过密，不利于后续增加初始化步骤或恢复流程。

改造：

- 拆分为：
  - `setup/payload_validator.py`：setup payload 规则校验。
  - `setup/secret_initializer.py`：初始化阶段 provider secret 写入。
  - `setup/organization_initializer.py`：企业、部门、用户、凭证、角色写入。
  - `setup/config_publisher.py`：首次 active_config 与 config_version 写入。
  - `setup/recovery_service.py`：恢复初始化。
  - `setup/repository.py`：集中 SQL。
- 保留 `SetupInitializationService` facade，初始化 route 不直接感知内部拆分。

验收：

- 首次初始化、恢复初始化、setup token 消费行为不变。
- bootstrap 失败时仍不会写入管理员和 active_config。
- 初始化失败审计仍完整记录。

进度更新（2026-05-30）：

- 已完成第一轮结构拆分。
- 原 `setup/initialize_service.py` 移动为 `setup/initialize_core.py`，保留原初始化事务编排作为兼容核心。
- 新增 `setup/initialize_service.py` facade，route 和测试仍通过原导入路径访问 `SetupInitializationService`。
- 已拆出：
  - `setup/payload_validator.py`：setup payload schema、组织、角色、密钥引用、缓存策略、中文检索 analyzer 校验。
  - `setup/secret_initializer.py`：初始化阶段模型 provider 明文 token 写入 Secret Store，并重写为 `secret://` 引用。
  - `setup/repository.py`：setup 初始化相关 SQL 集中封装。
  - `setup/organization_initializer.py`：企业、管理员、部门、内置角色和角色绑定写入。
  - `setup/config_publisher.py`：active config version、system config、初始化状态和恢复状态写入。
  - `setup/recovery_service.py`：恢复初始化入口。
  - `setup/initialization_audit.py`：初始化审计写入。
- facade 保留旧 patch/import 兼容点：
  - `SetupInitializationService`
  - `SetupInitializationError`
  - `SetupInitializationResult`
  - `SetupValidationResult`
  - `SetupStatus`
  - `ConfigSchemaValidator`
  - `SecretStoreService`
  - `_role_scopes`
- 当前仍保留 `initialize_core.py` 作为兼容编排层；后续如需要继续瘦身，可再把首次初始化和恢复初始化的 orchestration 从 core 中迁出。

验证：

- `ruff check apps/api/app/modules/setup tests/unit/test_setup_initialize_service.py tests/unit/test_setup_route.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py` 通过。
- `pytest -q tests/unit/test_setup_initialize_service.py tests/unit/test_setup_route.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py` 通过，24 passed。
- `make test` 通过，346 passed。

状态：已完成

## P2 公共 schema 与接口清理

### P2-1 抽离 PaginationData

现状：

- `apps/api/app/api/schemas/config.py` 定义 `PaginationData`。
- `admin.py`、`audit.py`、`import_pipeline.py` 从 config schema 引用分页模型。
- `knowledge.py` 又单独定义一份 `PaginationData`。

改造：

- 新增 `apps/api/app/api/schemas/common.py`。
- 移动 `PaginationData` 到 common。
- 所有 schema 从 common 引用。

验收：

- 不再从 `config` schema 引用通用分页结构。
- OpenAPI 中分页结构一致。

产出：

- 新增 `apps/api/app/api/schemas/common.py`。
- `PaginationData` 迁移到 common schema。
- admin、audit、config、import、knowledge、query 相关 schema/route 统一引用 common 分页模型。

状态：已完成（2026-05-30）

### P2-2 删除 public document preview 废弃接口

现状：

- `GET /internal/v1/documents/{doc_id}/preview` 已标记 `deprecated=True`。
- 前端查询页已经不依赖该接口。
- 该接口仍返回较长 preview 和 citations，是额外维护面和潜在暴露面。

改造：

- 删除 public preview route。
- 删除 public `DocumentPreviewData`、`DocumentPreviewResponse`。
- 保留 admin 文档 chunk 预览和 citation source 详情接口。

验收：

- 前端构建通过。
- OpenAPI 中不再出现 public document preview。
- citation 来源验证链路不受影响。

产出：

- 删除 `GET /internal/v1/documents/{doc_id}/preview` public route。
- 删除 public `DocumentPreviewData`、`DocumentPreviewResponse` API schema。
- 删除 `KnowledgeService.get_document_preview`、`AccessibleDocumentPreview`、`AccessiblePreviewCitation`。
- 保留 `GET /internal/v1/documents/{doc_id}/sources/{source_id}` citation source 详情接口。
- 更新 `docs/contracts/openapi.yaml`、`docs/contracts/权限矩阵.md`、`docs/modules/15-前端与管理后台API设计实现文档.md`，移除 public preview 入口。

状态：已完成（2026-05-30）

### P2-3 将配置列表分页下沉到 service

现状：

- `GET /internal/v1/admin/configs` 在 route 中调用 `list_config_items` 获取全部配置项，再用 Python 切片分页。
- 当前数据量小，风险低，但不符合“列表接口分页由后端服务处理”的一致性标准。

改造：

- 修改 `ConfigService.list_config_items(session, page, page_size)`。
- Service 返回 `items + total`。
- Route 不再做内存切片。

验收：

- route 不再出现 `items[start:start + page_size]`。
- 响应仍保持 `page`、`page_size`、`total`。

产出：

- `modules/config/schemas.py` 新增 `ConfigItemList`。
- `ConfigService.list_config_items(session, page, page_size)` 返回 `items + total`。
- `api/routes/config.py` 不再获取全量配置项后切片，改为只传分页参数并渲染 service 返回结果。
- 更新配置 route/service 单测。

状态：已完成（2026-05-30）

### P2-4 清理 admin/public schema 交叉引用

现状：

- `admin.py` route 从 `app.api.schemas.knowledge` 引用 `ChunkData`、`DocumentVersionData`、`DocumentVersionListResponse`。
- admin schema 从 config schema 引用 `PaginationData`。
- 这会让 public schema、admin schema、config schema 互相借用，模块边界不干净。

改造：

- `PaginationData` 统一迁移到 `api/schemas/common.py`。
- admin 专用响应模型保留在 `api/schemas/admin.py`，即使字段与 public schema 相似，也不要直接复用 public DTO。
- 如需减少重复，用 presenter 或内部 module schema 复用，不跨 API 暴露边界复用响应 DTO。

验收：

- `api/routes/admin.py` 或拆分后的 admin routes 不再从 `api.schemas.knowledge` 导入响应 DTO。
- `api/schemas/admin.py` 不再从 `api/schemas/config.py` 导入分页结构。
- OpenAPI 中 admin/public 模型命名清晰，不出现语义混用。

产出：

- `api/schemas/admin.py` 新增 `AdminDocumentVersionData`、`AdminDocumentVersionListResponse`、`AdminChunkData`、`AdminChunkListResponse`。
- `api/routes/admin.py` 的文档版本、chunk 列表接口改为使用 admin 专用响应 DTO，不再从 `api.schemas.knowledge` 导入响应模型。
- `CitationData` 下沉到 `api/schemas/common.py`，`query` 与 `knowledge` 不再互相借用 API schema。
- `docs/contracts/openapi.yaml` 中 admin 文档版本/片段响应改为 `AdminDocumentVersionListResponse`、`AdminChunkListResponse`。

状态：已完成（2026-05-30）

### P2-5 拆分 AuthService

现状：

- `apps/api/app/modules/auth/service.py` 约 911 行。
- `AuthService` 同时承担会话创建、会话刷新、token 生命周期、用户上下文加载、密码校验、密码更新。
- 登录、刷新和权限上下文是多个入口复用能力，继续集中在单 service 中会导致认证逻辑难以复用和测试。

改造：

- 拆分为：
  - `auth/session_service.py`：登录、刷新、登出会话编排。
  - `auth/token_service.py`：access/refresh token 生成、校验、撤销。
  - `auth/user_context_loader.py`：用户、角色、权限、部门上下文加载。
  - `auth/credential_service.py`：密码校验与密码更新。
- 保留 `AuthService` facade，逐步迁移 route 和其他模块调用方。

验收：

- 登录、刷新、登出、当前用户上下文行为保持一致。
- token 相关逻辑不散落在 route 或其他 service 中。
- 认证失败、权限不足、服务不可用错误码保持稳定。

产出：

- 新增 `apps/api/app/modules/auth/session_service.py`，负责登录和 refresh 编排。
- 新增 `apps/api/app/modules/auth/token_service.py`，负责 JWT 签发、解码、token row 校验、过期标记和会话吊销。
- 新增 `apps/api/app/modules/auth/user_context_loader.py`，负责用户、角色、部门和 scope 上下文加载。
- 新增 `apps/api/app/modules/auth/credential_service.py`，负责登录凭证读取、失败/成功登录记录、密码修改和改密后的 token 吊销。
- 新增 `apps/api/app/modules/auth/utils.py`，拆出 scope 合并、claim 校验、状态错误、UTC 时间、metadata JSON 等公共辅助函数。
- `apps/api/app/modules/auth/service.py` 收敛为 facade，保留原 public API 和测试/扩展依赖的 private helper 委托。

状态：已完成（2026-05-30）

### P2-6 拆分 KnowledgeService

现状：

- `apps/api/app/modules/knowledge/service.py` 约 840 行。
- `KnowledgeService` 同时承担可访问知识库浏览、文档读取、chunk/source 读取、权限包装、对象存储读取。
- public 查询页面与管理后台文档详情的读取职责容易混在一起，存在字段收紧和权限复核风险。

改造：

- 拆分为：
  - `knowledge/browser_service.py`：用户可访问知识库、文档列表浏览。
  - `knowledge/document_reader.py`：文档详情、版本、chunk 读取。
  - `knowledge/source_reader.py`：原文或对象存储内容读取。
  - `knowledge/repository.py`：knowledge 相关 SQL 查询。

验收：

- 列表接口只返回展示字段。
- 详情接口重新做权限检查。
- public 查询页不再依赖管理后台文档接口。

进度更新（2026-05-30）：

- 已完成第一轮结构拆分。
- 原 `knowledge/service.py` 移动为 `knowledge/core.py`，保留原实现作为兼容核心。
- 新增 `knowledge/service.py` facade，`KnowledgeService` 原导入路径保持不变。
- 已拆出：
  - `knowledge/access_control.py`：用户权限上下文构建、scope 校验、知识库可查询性校验。
  - `knowledge/browser_service.py`：用户可访问知识库列表、知识库下文档列表。
  - `knowledge/document_reader.py`：文档详情、文档版本、文档 chunk 预览读取。
  - `knowledge/source_reader.py`：citation source 原文读取、对象存储读取降级。
  - `knowledge/repository.py`：knowledge 相关 SQL 查询集中封装。
- facade 保留旧构造参数兼容：
  - `permission_service`
  - `object_storage`
- 当前仍保留 `knowledge/core.py` 作为兼容层；后续可继续把 mapping helper 迁入 presenter/mapper 或 repository 内部，进一步缩小 core。

验证：

- `ruff check apps/api/app/modules/knowledge apps/api/app/api/routes/knowledge.py tests/unit/test_knowledge_route.py` 通过。
- `pytest -q tests/unit/test_knowledge_route.py` 通过，6 passed。
- `make test` 通过，346 passed。

状态：已完成

### P2-7 拆分 PermissionService 基础能力

现状：

- `apps/api/app/modules/permissions/service.py` 约 747 行。
- `PermissionService` 同时承担用户上下文、scope 判断、SQL/Qdrant 过滤器、知识库可见性、策略校验、候选片段 gate。
- 权限能力被 query、knowledge、admin、import、indexing 多处依赖，继续堆在单 service 会拖慢后续重构。

改造：

- 拆分为：
  - `permissions/context_loader.py`：用户权限上下文加载。
  - `permissions/filter_builder.py`：SQL/Qdrant 权限过滤条件构造。
  - `permissions/policy_validator.py`：知识库/文档权限策略合法性校验。
  - `permissions/candidate_gate.py`：检索候选片段二次授权过滤。

验收：

- 所有权限过滤仍只在后端完成。
- query/knowledge/admin/import/indexing 使用统一权限 helper，不自行拼接权限条件。
- 权限收紧后，索引 payload 与查询过滤逻辑保持一致。

产出：

- 新增 `permissions/context_loader.py`：用户、部门、角色、企业版本和权限 filter hash 加载。
- 新增 `permissions/filter_builder.py`：SQL/Qdrant 权限过滤和知识库访问 SQL 构造。
- 新增 `permissions/policy_validator.py`：文档权限策略校验和权限快照 payload 构造。
- 新增 `permissions/candidate_gate.py`：候选片段二次授权 gate。
- `permissions/service.py` 收敛为 facade，保留原有对外方法和 `knowledge_base_access_where_sql` 导出。

状态：已完成（2026-05-30）

## P3 可维护性优化

### P3-1 梳理 setup/config 初始化与配置校验复用

现状：

- 初始化、配置管理、配置依赖探测有部分相似校验流程。
- 目前已有 `ConfigService.validate_config_payload`，但仍需要确认 setup 初始化链路是否完全复用统一校验。

改造：

- 确认 setup 初始化、新建配置版本、编辑配置版本均走同一校验入口。
- 如果存在重复校验，收敛到 `modules/config/validator.py` 和 `ConfigService`。

验收：

- 同一份配置在初始化和配置管理中的校验结果一致。
- 错误码和 warning 结构一致。

状态：未开始

### P3-2 收敛权限校验 helper

现状：

- `AdminService` 内部存在大量 `_ensure_actor_can_*`。
- 这些 helper 本身是必要的，但在 AdminService 拆分后需要避免复制到多个 service。

改造：

- 抽出 `admin/access_control.py` 或放入 `permissions` 模块。
- 为 admin 各 service 提供统一授权判断。

验收：

- AdminService 拆分后不复制权限校验函数。
- 知识库管理员、部门管理员、系统管理员行为保持现有测试覆盖。

产出：

- 新增 `apps/api/app/modules/admin/access_control.py`，集中管理后台 actor scope、部门作用域、知识库作用域和角色绑定作用域判断。
- `AdminService._ensure_actor_can_*` 保留兼容入口，但内部委托 `admin/access_control.py`。
- 模块级 `_has_scope`、`_actor_can_*`、`_actor_has_*` helper 改为委托公共 access control helper。

状态：已完成（2026-05-30）

### P3-3 审计日志写入统一封装

现状：

- 多个 service 内部存在 `_insert_audit_log` 或类似写入逻辑。
- 事件字段构造容易不一致。

改造：

- 提供 `AuditWriter` 或扩展 `AuditService` 写入接口。
- 各业务 service 只传业务事件参数。

验收：

- 审计日志字段结构统一。
- 高风险操作仍保留完整审计上下文。

产出：

- 新增 `apps/api/app/modules/audit/writer.py`，统一写入 `audit_logs` 的字段、request/trace 上下文和 summary JSON 序列化。
- admin、config、setup、import_pipeline、indexing、indexing ops、query 的审计写入改为委托 `AuditWriter`。
- 各业务 service 私有审计写入方法保留，避免破坏现有调用和测试 patch 入口。

状态：已完成（2026-05-30）

### P3-4 清理空模块和架构残留

现状：

- `apps/api/app/ports/__init__.py`、`apps/api/app/modules/data_model/__init__.py`、`apps/api/app/modules/org/__init__.py` 基本为空。
- 这些目录体现了架构意图，但当前没有实际职责，容易误导后续开发者以为已有端口层、组织模块或数据模型模块。

改造：

- 逐项判断是否需要保留：
  - `ports`：如果要推进端口/适配器架构，则补充对象存储、向量检索、模型调用等 Protocol；否则删除。
  - `modules/org`：如果部门/组织能力独立于 admin，则迁移部门相关 service；否则删除。
  - `modules/data_model`：如果没有领域模型层规划，则删除。
- 删除前确认没有 import 依赖。

验收：

- 空模块不再存在，或已经有明确职责和最小实现。
- `rg "app.modules.org|app.modules.data_model|app.ports"` 无无效引用。

产出：

- 删除 `apps/api/app/ports/__init__.py`，同时移除空目录 `apps/api/app/ports`。
- 删除 `apps/api/app/modules/data_model/__init__.py`，同时移除空目录 `apps/api/app/modules/data_model`。
- 删除 `apps/api/app/modules/org/__init__.py`，同时移除空目录 `apps/api/app/modules/org`。
- 保留 `docs/modules/03-组织服务设计实现文档.md` 作为未来组织服务规划，不在源码中保留未实现的空包。

验证：

- `rg "app.modules.org|app.modules.data_model|app.ports" apps tests tools docs 后端重构计划清单.md`：仅剩本清单验收项描述，无代码引用。
- `make test`：349 passed。

状态：已完成（2026-05-30）

### P3-5 梳理模块依赖方向

现状：

- 当前大体是 `api -> modules -> shared/db/adapters`。
- 但存在 `permissions route -> admin service`、admin route 借 public schema、route 直接构造 storage adapter 等依赖方向异常。

改造：

- 补一份后端模块依赖约束文档，建议放到 `docs/architecture/backend-module-boundaries.md`。
- 明确允许依赖：
  - route 可以依赖 api schema、module service、api dependencies。
  - module service 可以依赖本模块 schema/error、shared、db、明确的下游 service。
  - runtime 可以依赖 config/secrets/adapters 并组装 service。
- 明确禁止依赖：
  - public API schema 被 admin API 直接复用。
  - 业务 route 直接读取 secret 或构造外部 adapter。
  - 非 admin 模块依赖 AdminService 内部能力。

验收：

- 文档落地。
- 后续新增后端模块时有明确边界标准。

产出：

- `docs/architecture/backend-module-boundaries.md`

状态：已完成（2026-05-30）

### P3-6 拆分 AuditService 读取侧

原状：

- `apps/api/app/modules/audit/service.py` 约 575 行。
- `AuditService` 同时读取审计日志、查询日志、模型调用日志。
- 三类日志的数据结构、筛选字段和展示场景不同，长期放在一个 service 中会继续膨胀。

改造：

- 已拆分为：
  - `audit/audit_log_reader.py`：审计日志读取。
  - `audit/query_log_reader.py`：查询日志读取。
  - `audit/model_call_log_reader.py`：模型调用日志读取。
- `audit/service.py` 已收敛为 facade，保留 `list_audit_logs`、`get_audit_log`、`list_query_logs`、`get_query_log`、`list_model_call_logs`、`get_model_call_log` 原 public API。
- `audit/__init__.py` 已导出 `AuditLogReader`、`QueryLogReader`、`ModelCallLogReader`，便于后续测试和按需直接注入。

验收：

- 管理后台日志页面行为保持一致。
- 列表字段继续保持收紧，不返回 prompt、全文 chunk、内部 hash 等非展示字段。
- 详情接口继续独立权限检查。

验证：

- `ruff check apps/api/app/modules/audit tests/unit/test_audit_service.py tests/unit/test_audit_route.py` 通过。
- `pytest -q tests/unit/test_audit_service.py tests/unit/test_audit_route.py` 通过，13 passed。
- `make test` 通过，349 passed。

状态：已完成

### P3-7 收敛 models HTTP client 公共逻辑

现状：

- chat、embedding、rerank provider 中存在相似的 HTTP 请求、header、timeout、错误映射、响应解析基础逻辑。
- 当前重复还能接受，但后续接入更多 provider 时会增加错误码和鉴权处理分叉。

改造：

- 新增 `models/http.py` 或 `models/transport.py`，封装公共 HTTP 调用、认证 header、timeout、错误转换和基础日志字段。
- provider 只保留各自的 payload 构造与响应解析差异。

验收：

- LLM、Embedding、Rerank 调用行为保持一致。
- provider 密钥传递不回退，不再出现因为重构遗漏密钥导致 401 的问题。
- 错误码映射保持稳定。

产出：

- 新增 `apps/api/app/modules/models/http.py`，统一 JSON POST、SSE POST、Bearer 认证 header、timeout、HTTP 错误、网络不可用错误、JSON 解析错误和 provider 错误 body 摘要。
- `apps/api/app/modules/models/chat.py` 改为复用公共 transport，仅保留 OpenAI-compatible chat payload、SSE event 解析、choices/token usage 解析。
- `apps/api/app/modules/models/embeddings.py` 改为复用公共 transport，仅保留 TEI/OpenAI embedding payload、向量解析、维度校验和归一化。
- `apps/api/app/modules/models/rerank.py` 改为复用公共 transport，仅保留 TEI/OpenAI rerank payload、score 解析、排序和 hash 计算。
- `tests/unit/test_embedding_client.py`、`tests/unit/test_rerank_client.py` 增加 Bearer token 断言，防止模型密钥传递在重构中回退。

验证：

- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_chat_client.py tests/unit/test_embedding_client.py tests/unit/test_rerank_client.py tests/unit/test_query_runtime.py tests/unit/test_indexing_runtime.py`：14 passed。
- `make test`：349 passed。

状态：已完成（2026-05-30）

## P4 兼容 core 实迁收尾

说明：

- 本阶段不是重新设计业务，而是处理第一轮拆分后遗留的“facade/delegator 已存在，但实际实现仍堆在 core.py”的问题。
- 优先级按业务风险、文件职责混杂程度、后续改动依赖关系排序。
- 每一项应拆成小切片推进，优先迁移无数据库副作用或边界清晰的 helper，再迁移带事务和状态机的写路径。

### P4-1 ImportPipeline core 实迁收尾

现状：

- `apps/api/app/modules/import_pipeline/core.py` 约 2477 行。
- 已存在 `command_service.py`、`worker_service.py`、`stage_runner.py`、`document_writer.py`、`permission_guard.py`、`retry_service.py` 等 facade/delegator。
- 但核心实现仍集中在 `core.py`，同时承担上传校验、导入任务创建、worker 认领/心跳/失败、stage 状态推进、parse/clean/chunk/index/publish/cleanup 副作用、文档写入、权限校验、permission snapshot、audit 和 object storage 读写。

改造顺序：

1. 上传文件类型/大小校验迁出到独立 `upload_validation.py`。
2. 导入 item/request JSON 规范化 helper 迁出到 `request_items.py` 或 command 边界模块。
3. 文档和版本预创建写入从 `core.py` 下沉到 `document_writer.py`。
4. 知识库、owner department、folder 读取/校验下沉到 permission/resource guard。
5. worker claim/heartbeat/fail/succeed/cancel 状态机下沉到 `worker_service.py`。
6. stage effect 下沉到 `stage_runner.py`，阶段副作用按 parse/clean/chunk/index/publish/cleanup 再拆小模块。
7. audit 写入统一复用 `AuditWriter`，移除本模块私有 `_insert_audit_log` 分支实现。

验收：

- `core.py` 不再承载上传校验、文档写入、worker 状态机、stage 副作用的实际实现。
- facade public API 保持兼容。
- 上传、worker、重试、索引写入相关单测通过。

进度：

- 已完成第一切片：
  - 新增 `apps/api/app/modules/import_pipeline/upload_validation.py`。
  - 上传文件类型推断、允许类型归一化、大小限制和错误码构造已从 `core.py` 迁出。
  - `ImportCommandService.validate_upload_file` 直接调用上传校验模块，不再回落到 core 实现。
  - `ImportService` 继续保留原 public API；`core.py` 仅保留兼容 wrapper。
- 已完成第二切片：
  - 新增 `apps/api/app/modules/import_pipeline/request_items.py`。
  - 导入 item 规范化、metadata 解析、request_json items 读取、document id 提取、job id 去重和 JSON mapping 已从 `core.py` 迁出。
  - `core.py` 内导入创建、重试、worker stage、job 映射等路径统一调用 `request_items.py`。
  - `core.py` 行数从约 2477 行降至约 2259 行。
- 已完成第三切片：
  - `apps/api/app/modules/import_pipeline/document_writer.py` 从 delegator 改为真实文档写入组件。
  - 上传源对象存储、`documents` 预创建、`document_versions` 预创建已从 `core.py` 迁出。
  - `core.py` 的导入创建流程仅负责权限/policy/snapshot、content hash、request_json 和 import job 编排。
  - `core.py` 行数从约 2259 行降至约 2120 行。
- 已完成第四切片：
  - `apps/api/app/modules/import_pipeline/permission_guard.py` 从 delegator 改为真实导入权限与资源可用性 guard。
  - 知识库读取、导入范围校验、owner department 解析、可见性校验、父知识库权限边界校验、folder 可用性校验已从 `core.py` 迁出。
  - `service.py` 不再重复覆盖 `permission_guard`，由 core 初始化真实 guard。
  - `core.py` 行数从约 2120 行降至约 1816 行。
- 已完成第五切片：
  - `apps/api/app/modules/import_pipeline/worker_service.py` 从 delegator 改为真实 worker 状态机 service。
  - worker 任务领取、阶段推进、失败重试、心跳续租、取消、成功完成、任务企业归属读取和 worker 审计写入已从 `core.py` 迁出。
  - `core.py` 仅保留 public API wrapper 和 `_job_enterprise_id` / `_insert_worker_audit_log` 两个兼容转发点，保证既有测试 monkeypatch 路径不变。
  - `service.py` 不再重复初始化 `worker_service`，由 core 统一初始化。
  - `core.py` 行数从约 1816 行降至约 1529 行。
- 已完成第六切片：
  - `apps/api/app/modules/import_pipeline/stage_runner.py` 从 compatibility delegator 改为真实阶段副作用执行器。
  - validate、parse、clean、chunk、embed、index、publish、cleanup、permission_refresh 的阶段副作用已从 `core.py` 迁出。
  - `core.py` 仅保留 `_apply_stage_effect`、`_mark_versions_parsed`、`_write_draft_chunks` 等兼容 wrapper，真实实现统一落在 `stage_runner.py`。
  - `service.py` 保留 `build_indexing_service` 可 patch 路径，通过临时替换 `stage_runner.indexing_service_factory` 兼容现有测试和运行时注入。
  - `core.py` 行数从约 1529 行降至约 1096 行。
- 已完成第七切片：
  - 新增 `apps/api/app/modules/import_pipeline/audit_writer.py`，集中封装导入域用户审计和 worker 审计默认字段。
  - `core.py` 的 `_insert_audit_log` 不再直接拼装 `AuditWriter` 参数，改为转发到 `ImportAuditWriter.write_user_event`。
  - `worker_service.py` 的 worker 审计写入改为复用 `ImportAuditWriter.write_worker_event`。
  - 继续保留 `_insert_audit_log` / `_insert_worker_audit_log` 兼容入口，避免现有测试 monkeypatch 路径和旧调用点失效。
- 已完成第八切片：
  - `apps/api/app/modules/import_pipeline/command_service.py` 从 delegator 改为真实导入命令服务。
  - 创建导入任务、导入任务详情读取、导入任务列表分页筛选、导入任务取消逻辑已从 `core.py` 迁出。
  - `core.py` public command API 只保留兼容 wrapper，实际调用 `command_service`。
  - `core.py` 统一初始化 `command_service`，`service.py` 不再重复初始化。
  - `core.py` 行数从约 1096 行降至约 806 行。
- 已完成第九切片：
  - `apps/api/app/modules/import_pipeline/retry_service.py` 从 delegator 改为真实重试应用服务。
  - 单任务重试、批量索引重试、重试任务创建与审计编排已从 `core.py` 迁出。
  - `core.py` public retry API 和 `_create_retry_from_row` 仅保留兼容 wrapper，实际调用 `retry_service`。
  - `core.py` 统一初始化 `retry_service`，`service.py` 不再重复初始化。
  - `core.py` 行数从约 806 行降至约 715 行。
- 已完成第十切片：
  - 新增 `apps/api/app/modules/import_pipeline/repository.py`，集中封装导入任务、企业权限版本、resource policy、permission snapshot 的 PostgreSQL 读写。
  - `_load_job_by_idempotency`、`_load_permission_version`、`_replace_resource_policy`、`_insert_permission_snapshot`、`_insert_import_job`、`_load_import_job_row` 已从 `core.py` 迁出。
  - `core.py` 仅保留上述私有方法的兼容 wrapper，保证现有测试 monkeypatch 路径不变。
  - `core.py` 不再直接依赖 `PermissionService`、`stable_json_hash` 或本组 SQL 细节。
  - `core.py` 行数从约 715 行降至约 538 行。

验证：

- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- `make test` 通过，349 passed。
- 第二切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第三切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第四切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第五切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第六切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第七切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第八切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第九切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。
- 第十切片验证：
  - `ruff check apps/api/app/modules/import_pipeline tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py` 通过，33 passed。

收尾结论：

- `core.py` 当前只保留构造器、public API wrapper、以及为了兼容既有测试 monkeypatch 路径和旧内部调用点的私有 wrapper。
- 上传校验、导入命令、重试、worker 状态机、阶段副作用、文档写入、权限资源校验、repository 读写和审计默认字段均已迁出。
- 剩余 wrapper 暂不继续拆分，避免为了行数进一步增加间接层；后续如测试不再依赖私有 monkeypatch 路径，再单独做兼容层删除。

状态：已完成

### P4-2 Query core 实迁收尾

现状：

- `apps/api/app/modules/query/core.py` 约 1881 行。
- 已存在 `orchestrator.py`、`repository.py`、`retrieval_pipeline.py`、`log_writer.py`、`citation_validator.py`、`degrade_policy.py`。
- 但 `service.py` 仍从 `core.py` 导入大量私有函数，说明查询编排、检索、引用校验、降级、日志写入和 mapper 没有完全迁出。

改造顺序：

1. citation/degrade helper 从 core 私有函数切换到现有独立模块。
2. query filter、candidate mapper、hash/elapsed helper 下沉到 repository/presenter 或 query utils。
3. log/model call/audit 写入只走 `QueryLogWriter` 和 `AuditWriter`。
4. `core.py` 最终收敛为兼容数据结构或删除。

进度：

- 已完成第一切片：
  - `apps/api/app/modules/query/orchestrator.py` 从兼容转发壳改为真实查询编排边界。
  - 同步查询、流式查询计划、流式收尾不再回调 `core.QueryService.create_query` / `create_query_stream_plan` / `finalize_query_stream`。
  - 查询检索计划构建、权限上下文、知识库可查询校验、active index 读取、关键词/向量召回、融合、rerank、相关性 gate、上下文构建已由 orchestrator 统一编排。
  - 答案收尾中的引用校验、引用自动附加、展示前 source 标记清理和降级答案已直接使用 `citation_validator.py`、`degrade_policy.py`，不再依赖 core 的同名私有函数 monkeypatch。
  - `core.py` 暂保留旧 public 实现作为兼容层，下一切片继续迁移 query utils / data structures，减少 `service.py`、`repository.py`、`retrieval_pipeline.py` 对 core 私有 helper 的导入。
- 已完成第二切片：
  - `apps/api/app/modules/query/schemas.py` 承接 `QueryStreamPlan`、`_QueryAuditEvent`、`_CitationValidationResult`、`_CurrentCandidateFacts`。
  - 新增 `apps/api/app/modules/query/utils.py`，承接 query normalize、filter clause、candidate mapper、citation mapper、相关性 gate、hash、latency、数据库错误包装等纯 helper。
  - `orchestrator.py`、`repository.py`、`retrieval_pipeline.py`、`log_writer.py`、`citation_validator.py`、`service.py` 已切换到 `schemas.py` / `utils.py`，不再从 `core.py` 导入这些私有结构或 helper。
  - `core.py` 顶部重复定义的 query plan / audit / citation / candidate facts dataclass 已删除，改为复用 `schemas.py`。
  - 当前剩余 core 依赖只保留在 `service.py` 的兼容继承/monkeypatch 和 `orchestrator.py` 的 `TYPE_CHECKING` 类型提示；下一切片可继续处理 `core.py` 内部遗留重复 helper 和兼容 public 实现。
- 已完成第三切片：
  - `core.QueryService.create_query`、`create_query_stream_plan`、`finalize_query_stream` 已从完整业务实现替换为兼容 wrapper。
  - wrapper 保留旧 public 方法签名，通过 `_orchestrator()` 延迟导入 `QueryOrchestrator` 并委托执行，避免模块加载期循环依赖。
  - `core.py` 行数从约 1826 行降至约 1243 行，真实查询主编排只保留在 `orchestrator.py`。
  - `core.py` 当前仍保留 repository/log/citation/degrade 等兼容私有方法；下一切片继续删除或转发这些重复 helper。
- 已完成第四切片：
  - `core.py` 中 repository 读取、权限 gate/rerank、query/model/audit log 写入方法已改为兼容 wrapper，分别委托 `QueryRepository`、`QueryRetrievalPipeline`、`QueryLogWriter`。
  - `core.py` 底部 citation/degrade helper 重复实现已删除，保留同名兼容别名并指向 `citation_validator.py`、`degrade_policy.py`。
  - `core.py` 已不再直接持有查询 SQL、模型调用日志 SQL、审计写入实现、citation 校验实现和降级答案实现。
  - `core.py` 行数从约 1243 行降至约 397 行。
- 已完成第五切片：
  - `apps/api/app/modules/query/service.py` 不再继承 `core.QueryService`，改为独立 route-facing service。
  - `service.py` 已删除对 `query.core` 的导入、`_sync_core_policies` monkeypatch 和 public 方法中的同步调用。
  - `QueryService.__init__` 直接初始化 permission、vector retriever、reranker、fusion、context builder、answer service、repository 和 log writer。
  - `apps/api/app/modules/query/orchestrator.py` 不再为类型提示导入 `query.core`，内部变量名从 `_core_service` 收敛为 `_service`。
  - `rg` 确认 Query 运行路径不再导入 `query.core`；`core.py` 当前仅作为旧导入兼容层保留。

验证：

- `ruff check apps/api/app/modules/query/orchestrator.py` 通过。
- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。
- 第二切片验证：
  - `ruff check apps/api/app/modules/query` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
  - `make PYTHON=.venv/bin/python test` 通过，349 passed。
- 第三切片验证：
  - `ruff check apps/api/app/modules/query` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
  - `make PYTHON=.venv/bin/python test` 通过，349 passed。
- 第四切片验证：
  - `ruff check apps/api/app/modules/query` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
  - `make PYTHON=.venv/bin/python test` 通过，349 passed。
- 第五切片验证：
  - `ruff check apps/api/app/modules/query` 通过。
  - `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py` 通过，20 passed。
  - `make PYTHON=.venv/bin/python test` 通过，349 passed。

收尾结论：

- Query 主编排在 `orchestrator.py`。
- SQL 读取在 `repository.py`。
- 权限 gate/rerank 在 `retrieval_pipeline.py`。
- query/model/audit 日志写入在 `log_writer.py`。
- citation 与降级策略分别在 `citation_validator.py`、`degrade_policy.py`。
- `core.py` 不再承载真实业务实现，仅保留旧 import 路径下的兼容类和兼容别名。

状态：已完成

### P4-3 Indexing core 实迁收尾

改造前现状：

- `apps/api/app/modules/indexing/core.py` 约 1664 行。
- 已存在 `draft_service.py`、`publish_service.py`、`cleanup_service.py`、`permission_payload_service.py`。
- 但 draft 写入、keyword/vector index、publish 状态切换、pending_delete 清理、permission payload refresh、audit 仍大量堆在 core。

改造顺序：

1. vector writer protocol/noop writer 抽到 `writers.py`。（已完成）
2. draft chunk/index 写入下沉到 `draft_service.py`。（已完成）
3. publish preflight 与 activate 状态切换下沉到 `publish_service.py`。（已完成）
4. pending_delete 清理下沉到 `cleanup_service.py`。（已完成）
5. permission payload refresh 下沉到 `permission_payload_service.py`。（已完成）

产出：

- 新增 `apps/api/app/modules/indexing/writers.py`，承载 `VectorIndexWriter` 协议和 `NoopVectorIndexWriter`。
- 新增 `apps/api/app/modules/indexing/utils.py`，承载 request 解析、payload hash、vector payload 组装、错误包装等纯 helper。
- `draft_service.py` 接管 draft index version 创建、draft chunk 读取、keyword/ref 写入、draft vector 写入和 ready 标记。
- `publish_service.py` 接管 ready index 读取、发布前校验、vector 激活、active 状态切换。
- `cleanup_service.py` 接管 pending_delete ref 读取、逻辑删除、向量 payload 删除和物理删除。
- `permission_payload_service.py` 接管权限 payload refresh、access block 释放和 vector payload refresh。
- `core.py` 从约 1664 行收敛到 121 行，仅保留兼容入口、运行参数和 indexing 审计写入 helper。

验证：

- `ruff check apps/api/app/modules/indexing tests/unit/test_indexing_service.py tests/unit/test_index_ops_service.py` 通过。
- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_indexing_service.py tests/unit/test_index_ops_service.py` 通过，24 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-30）

### P4-4 PermissionAdminService 实迁收尾

现状：

- `apps/api/app/modules/permissions/admin_service.py` 约 1367 行。
- 同时承担知识库权限替换、文档权限替换、access rule 处理、resource policy、permission snapshot、access block、permission refresh job、audit、mapping 和策略 helper。

改造顺序：

1. policy/rule 规范化和可见性判断复用 `policy_validator.py` 或独立 policy 模块。（已完成）
2. resource policy/snapshot/access block 写入拆成 writer。（已完成）
3. refresh job 入队拆成 job writer。（已完成）
4. KB/document 权限替换保留在 service 编排层。（已完成）

产出：

- 新增 `apps/api/app/modules/permissions/admin_policy.py`，承载知识库可见性校验、访问规则规范化、权限收紧/扩展判断、actor 资源范围判断。
- 新增 `apps/api/app/modules/permissions/admin_writers.py`，承载 `resource_policies`、`permission_snapshots`、`access_blocks` 和 `permission_refresh` import job 写入。
- `apps/api/app/modules/permissions/admin_service.py` 从约 1367 行收敛到 872 行，保留知识库/文档权限替换编排、资源加载、越权校验和审计写入。

验证：

- `ruff check apps/api/app/modules/permissions tests/unit/test_permission_admin_service.py` 通过。
- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_permission_admin_service.py` 通过，3 passed。
- `PYTHONPATH=apps/api .venv/bin/python -m pytest -q tests/unit/test_permission_route.py tests/unit/test_permission_admin_service.py` 通过，5 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-30）

### P4-5 SetupInitialization core 实迁收尾

现状：

- `apps/api/app/modules/setup/initialize_core.py` 约 1085 行。
- 已存在 `payload_validator.py`、`organization_initializer.py`、`secret_initializer.py`、`config_publisher.py`、`recovery_service.py` 等模块。
- 但初始化校验、recovery、enterprise/admin/departments/roles/config/audit 写入仍未彻底从 core 瘦身。

改造顺序：

1. schema/rule 校验完全切到 `payload_validator.py`。
2. enterprise/admin/departments/roles 写入切到 `organization_initializer.py`。
3. secret 写入切到 `secret_initializer.py`。
4. config version/system configs 写入切到 `config_publisher.py`。
5. recovery path 切到 `recovery_service.py`。

完成记录（2026-05-31）：

- 新增 `apps/api/app/modules/setup/contracts.py`，集中承载 setup 初始化错误、结果 DTO、内置角色、模型密钥字段和角色 scope helper。
- `apps/api/app/modules/setup/initialize_service.py` 改为真实事务编排入口，不再继承或调用 `initialize_core.py`。
- `apps/api/app/modules/setup/recovery_service.py` 承接恢复初始化完整编排，包括归档旧 active config、发布新 config、清理 recovery 状态、消费 setup token 和审计。
- `payload_validator.py`、`organization_initializer.py`、`secret_initializer.py`、`repository.py` 改为依赖 `contracts.py`，解除对子模块实现的反向依赖。
- `apps/api/app/modules/setup/initialize_core.py` 瘦身为兼容导出层，从约 1085 行降至 34 行；旧导入路径仍可用。

验证：

- `ruff check apps/api/app/modules/setup tests/unit/test_setup_initialize_service.py tests/unit/test_setup_route.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py` 通过。
- `pytest -q tests/unit/test_setup_initialize_service.py tests/unit/test_setup_route.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py` 通过，24 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-31）

### P4-6 Knowledge core 实迁收尾

现状：

- `apps/api/app/modules/knowledge/core.py` 约 784 行。
- 混合知识库列表、文档列表、版本、chunk、source 读取、权限上下文和 mapper。

改造顺序：

1. 知识库/文档/版本/chunk 读取完全下沉到 reader/repository。
2. source 读取复用 `source_reader.py`。
3. mapping helper 收敛到 presenter 或 mapper。

完成记录（2026-05-31）：

- 新增 `apps/api/app/modules/knowledge/mappers.py`，集中承载普通用户知识库浏览的 DTO 映射、可选字段解析、知识库可发现 SQL 和数据库错误转换。
- `apps/api/app/modules/knowledge/repository.py` 改为依赖 `mappers.py`，不再从 `core.py` 读取 mapper/helper。
- `apps/api/app/modules/knowledge/source_reader.py` 改为复用 `mappers.py` 的 `optional/json` helper，解除 source reader 对 core 的反向依赖。
- `apps/api/app/modules/knowledge/service.py` 不再继承 `core.KnowledgeService`，改为真实 facade，组合 `KnowledgeBrowserService`、`KnowledgeDocumentReader`、`KnowledgeSourceReader`、`KnowledgeRepository` 和 `KnowledgeAccessGuard`。
- `apps/api/app/modules/knowledge/core.py` 瘦身为兼容导出层，从约 784 行降至 36 行；旧导入路径仍可用。

验证：

- `ruff check apps/api/app/modules/knowledge apps/api/app/api/routes/knowledge.py tests/unit/test_knowledge_route.py` 通过。
- `pytest -q tests/unit/test_knowledge_route.py` 通过，6 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-31）

### P4-7 QueryConversationService 拆分

现状：

- `apps/api/app/modules/query/conversations.py` 约 716 行。
- 同时包含会话 CRUD、消息生命周期、mapper、JSON citations 转换。

改造顺序：

1. SQL 读取/写入拆到 repository。
2. message lifecycle 拆到 message service。
3. mapper/JSON helper 拆到 mapper/utils。

完成记录（2026-05-31）：

- 新增 `apps/api/app/modules/query/conversation_models.py`，集中承载查询会话 summary、message、detail、list 和写入上下文数据结构。
- 新增 `apps/api/app/modules/query/conversation_mappers.py`，承载会话/消息 row 映射、citation JSON 序列化与反序列化、标题规范化、状态/置信度转换和会话错误转换。
- 新增 `apps/api/app/modules/query/conversation_repository.py`，集中承载 `query_conversations` 与 `query_messages` 的 SQL 读写。
- 新增 `apps/api/app/modules/query/conversation_messages.py`，承接用户消息创建、assistant 占位消息创建、assistant 完成和失败状态更新。
- `apps/api/app/modules/query/conversations.py` 收敛为 route-facing facade 和兼容导出层，从约 716 行降至约 235 行。

验证：

- `ruff check apps/api/app/modules/query apps/api/app/api/routes/query.py tests/unit/test_query_route.py tests/unit/test_query_service.py tests/unit/test_query_runtime.py` 通过。
- `pytest -q tests/unit/test_query_route.py tests/unit/test_query_service.py tests/unit/test_query_runtime.py` 通过，20 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-31）

### P4-8 非 admin route 收尾

现状：

- `api/routes/query.py` 仍混合 conversation、普通查询、SSE 流式查询、SSE event 格式化和失败回写。
- `api/routes/config.py` 仍混合 config item、config version、validation 和 presenter mapper。
- `api/routes/import_pipeline.py` 仍混合用户导入、admin 任务、重试、上传文件处理和 presenter mapper。

改造顺序：

1. `query.py` 按 conversation/query/query_stream 拆分。
2. `config.py` 按 config item/config version/validation 拆分。
3. `import_pipeline.py` 按 user import/admin import/upload helper 拆分。

完成记录（2026-05-31）：

- `api/routes/query.py` 收敛为路由聚合层，拆出：
  - `query_conversations.py`：查询会话 CRUD。
  - `query_execute.py`：普通查询提交与会话消息落库。
  - `query_stream.py`：SSE 流式查询、事件格式化、流式失败补偿。
  - `query_shared.py`：查询路由共享认证、request/trace、错误响应和分页常量。
- `api/routes/config.py` 收敛为路由聚合层，拆出：
  - `config_items.py`：配置项列表、详情和草稿保存。
  - `config_versions.py`：配置版本列表、创建、详情、更新、激活和归档。
  - `config_validations.py`：配置 payload 校验。
  - `config_shared.py`：系统管理员配置管理鉴权、错误响应和 DTO 映射。
- `api/routes/import_pipeline.py` 收敛为路由聚合层，拆出：
  - `import_user.py`：普通用户上传导入、元数据导入、任务读取、取消和重试。
  - `import_admin.py`：管理端导入任务列表、详情和索引任务批量重试。
  - `import_shared.py`：actor context、job DTO 映射、上传文件转 import item。
- 同步更新 `test_query_route.py`、`test_config_route.py`、`test_import_route.py` 的 patch 路径，匹配拆分后的路由模块。

验证：

- 非 admin route 拆分局部 `ruff check` 通过。
- `pytest -q tests/unit/test_query_route.py tests/unit/test_config_route.py tests/unit/test_import_route.py` 通过，27 passed。
- `make PYTHON=.venv/bin/python test` 通过，349 passed。

状态：已完成（2026-05-31）

## P5 完整模块化收尾

说明：

- 本阶段修正 P4 的一个错误判断：`core.py` 兼容层不是完整重构结果，只能视为迁移期中间产物。
- 最终目标不是把 `core.py` 变薄，而是删除中间层文件，修正调用入口，让每个模块只暴露明确的 service / runtime / repository / mapper / writer 边界。
- 删除兼容层前必须先修正测试 monkeypatch 路径，测试应 patch 稳定 public seam，不应依赖历史私有 wrapper。

### P5-1 删除 core 中间产物与兼容入口

优先级：最高。

涉及文件：

- `apps/api/app/modules/admin/core.py`
- `apps/api/app/modules/import_pipeline/core.py`
- `apps/api/app/modules/indexing/core.py`
- `apps/api/app/modules/knowledge/core.py`
- `apps/api/app/modules/query/core.py`

同类补充检查：

- `apps/api/app/modules/setup/initialize_core.py` 也是兼容导出层。虽然本次用户点名范围不包含它，但按同一原则，后续也应删除或并入真实初始化入口。

现状：

- `admin/core.py` 仍被 `AdminService` 继承，领域 service 仍通过 `_core_service` 调用共享 helper、loader、writer 和权限校验。
- `import_pipeline/core.py` 已把真实逻辑拆到 command / retry / worker / stage / repository / audit 等模块，但仍保留 public wrapper 和私有 wrapper。
- `indexing/core.py` 已瘦身，但仍保留兼容入口、运行参数和 indexing 审计 helper。
- `knowledge/core.py` 仅剩兼容导出，但旧导入路径仍存在。
- `query/core.py` 仅剩兼容类和兼容别名，但旧导入路径仍存在。

改造顺序：

1. 用 `rg "modules\\..*\\.core|import .*core|from app\\.modules\\..*\\.core"` 建立引用清单。
2. 先处理 `knowledge/core.py` 和 `query/core.py`，因为它们当前基本是兼容导出，删除风险最低。
3. 处理 `indexing/core.py`，把剩余审计 helper、默认参数和兼容导出迁到 `service.py`、`utils.py` 或专用 writer。
4. 处理 `import_pipeline/core.py`，把 `ImportService` 入口正式迁到 `service.py`，各 wrapper 调用点改为直接调用 command / retry / worker / stage / repository。
5. 最后处理 `admin/core.py`，因为它仍承担最多共享能力；需要先把共享 helper 下沉到 `access_control.py`、`resource_loaders.py`、`state_writer.py`、`permission_writer.py`、`index_job_writer.py`、`mappers.py`、`policies.py`。
6. 更新模块 `__init__.py`，只导出真实 service、schema、error 和 runtime。
7. 删除上述 core 文件，并同步更新测试 patch 路径。

验收：

- `find apps/api/app/modules -name '*core.py'` 不再出现上述兼容文件。
- 业务代码和测试不再导入 `app.modules.*.core`。
- `AdminService` 不再继承 `AdminCoreService`。
- `make PYTHON=.venv/bin/python test` 通过。

完成记录（2026-05-31）：

- 删除 `apps/api/app/modules/admin/core.py`、`import_pipeline/core.py`、`indexing/core.py`、`knowledge/core.py`、`query/core.py`、`setup/initialize_core.py`。
- `apps/api/app/modules/admin/service.py` 直接组合 admin access/resource/permission/index/audit 等 mixin，不再继承 `AdminCoreService`。
- `apps/api/app/modules/import_pipeline/service.py` 改为真实导入 facade，直接初始化 command、retry、repository、document writer、permission guard、worker、stage runner 和 audit writer，不再继承 `_ImportCore`。
- `apps/api/app/modules/indexing/service.py` 改为真实索引 facade，直接持有索引运行参数、vector writer 和 indexing 审计写入能力，不再组合 `_IndexingCore`。
- `knowledge/core.py`、`query/core.py`、`setup/initialize_core.py` 无业务引用，已直接删除。
- `rg` 确认业务代码和测试不再导入 `app.modules.*.core` 或 `setup.initialize_core`。
- `find apps/api/app/modules -name '*core.py'` 已无结果。

验证：

- `.venv/bin/python -m pytest -q tests/unit/test_admin_service.py tests/unit/test_admin_route.py tests/unit/test_import_service.py tests/unit/test_import_route.py tests/unit/test_import_runtime.py tests/unit/test_indexing_service.py tests/unit/test_index_ops_service.py tests/unit/test_knowledge_route.py tests/unit/test_query_service.py tests/unit/test_query_route.py tests/unit/test_query_runtime.py tests/unit/test_setup_initialize_service.py tests/unit/test_setup_route.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py`：208 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

剩余说明：

- admin 各领域 service 当前仍通过构造参数持有 route-facing `AdminService` 实例来复用共享 helper；这不再依赖 `admin/core.py` 文件，但仍属于“admin 领域大 service / 共享 helper 依赖过宽”的问题，归入 P5-3 继续拆分为明确 reader / writer / policy / mapper 依赖。

状态：已完成（2026-05-31）。

### P5-2 修正 shared 与 adapters 的依赖方向

优先级：高。

现状：

- `apps/api/app/shared/middleware.py` 直接依赖 `modules.setup`，违反 `shared -> modules` 禁止方向。
- `apps/api/app/adapters/qdrant.py` 直接依赖 `modules.models`、`modules.permissions.schemas`、`modules.retrieval`，违反 `adapters -> modules` 禁止方向。

改造：

- 将 `SetupGuardMiddleware` 移到 `api/middleware` 或 `modules/setup/middleware.py`；`shared` 只保留 request context 和无业务归属能力。
- 为 Qdrant adapter 提取 adapter DTO / protocol，模块层负责把 `PermissionFilter`、`RetrievalCandidate`、`DraftVectorPoint` 转换为 adapter 输入输出。

验收：

- `rg "from app\\.modules" apps/api/app/shared apps/api/app/adapters` 不再出现违反边界的业务模块依赖。
- Qdrant 搜索、draft 写入、payload 刷新、snapshot 运维相关测试通过。

完成记录（2026-05-31）：

- 新增 `apps/api/app/api/middleware/setup_guard.py`，将 `SetupGuardMiddleware` 从 `shared` 迁到 API 层；`shared/middleware.py` 只保留 `RequestContextMiddleware`。
- 新增 `apps/api/app/adapters/vector_store.py`，定义向量存储适配器自有 DTO、embedding client protocol 和 `VectorStoreEmbeddingError`。
- `apps/api/app/adapters/qdrant.py` 改为只依赖 adapter DTO / protocol，不再直接引用 `modules.models`、`modules.permissions`、`modules.retrieval`、`modules.indexing`。
- `query/runtime.py` 负责把 `PermissionFilter`、`VectorStoreCandidate` 与 `VectorSearchResult` 做双向映射。
- `indexing/runtime.py` 负责把 `DraftVectorPoint`、`VectorPayloadUpdate` 映射为 adapter 写入 DTO。
- route 单测的 setup guard monkeypatch 路径已更新到 `app.api.middleware.setup_guard.SetupService.load_state`。

验证：

- `rg "from app\\.modules|import app\\.modules" apps/api/app/shared apps/api/app/adapters`：无结果。
- `.venv/bin/python -m ruff check apps/api/app/adapters apps/api/app/api/middleware apps/api/app/shared/middleware.py apps/api/app/modules/query/runtime.py apps/api/app/modules/indexing/runtime.py tests/unit/test_qdrant_vector_retriever.py tests/integration/test_qdrant_indexing_flow.py`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_qdrant_vector_retriever.py tests/unit/test_query_runtime.py tests/unit/test_indexing_service.py tests/unit/test_import_route.py tests/unit/test_setup_route.py`：49 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

### P5-3 拆分 admin 领域大 service

优先级：高，依赖 P5-1 的 admin/core 删除工作。

现状：

- P5-1 已删除 `admin/core.py`，但 admin 各领域 service 仍通过 route-facing `AdminService` 复用共享 helper，依赖面过宽。
- `admin/documents_service.py` 约 935 行，混合文档列表、详情、版本、chunk、预览、权限更新和删除。
- `admin/users_service.py` 约 908 行，混合用户查询、部门绑定、创建、更新、删除、重置密码和解锁。
- `admin/knowledge_bases_service.py` 约 858 行，混合知识库列表、选项、详情、新建、修改和删除。
- `permissions/admin_service.py` 约 872 行，权限管理编排仍偏重。

改造：

- 文档域拆为 `documents_reader.py`、`document_versions_reader.py`、`document_chunks_reader.py`、`document_writer.py`、`document_preview_service.py`。
- 用户域拆为 `users_reader.py`、`user_department_service.py`、`user_writer.py`、`user_security_service.py`。
- 知识库域拆为 `knowledge_base_reader.py`、`knowledge_base_writer.py`、`knowledge_base_policy.py`。
- 权限 admin service 保留编排，资源读取、policy、writer、refresh job、audit 分别落到独立模块。

验收：

- 单个 admin 领域 service 不再承担读、写、权限、审计、索引任务全套职责。
- 系统管理员、部门管理员、知识库管理员三类后台权限路径测试通过。

完成记录（2026-05-31）：

- 文档管理域已拆分：
  - `documents_service.py` 保留 route-facing facade。
  - 新增 `documents_reader.py`：文档列表和详情读取。
  - 新增 `document_versions_reader.py`：文档版本和索引版本读取。
  - 新增 `document_chunks_reader.py`：chunk 列表读取。
  - 新增 `document_preview_service.py`：文档预览读取和对象存储文本读取。
  - 新增 `document_writer.py`：文档更新、删除和权限刷新任务编排。
- 用户管理域已拆分：
  - `users_service.py` 保留 route-facing facade。
  - 新增 `users_reader.py`：用户列表和详情读取。
  - 新增 `user_department_service.py`：用户部门列表和部门归属替换。
  - 新增 `user_writer.py`：用户创建、修改和删除。
  - 新增 `user_security_service.py`：密码重置和解锁。
- 知识库管理域已拆分：
  - `knowledge_bases_service.py` 保留 route-facing facade。
  - 新增 `knowledge_base_reader.py`：知识库列表、选项和详情读取。
  - 新增 `knowledge_base_writer.py`：知识库创建、修改、删除和权限策略写入编排。
- 权限管理域已拆分：
  - `permissions/admin_service.py` 保留权限替换编排入口。
  - 新增 `permissions/admin_readers.py`：权限管理资源读取和 DTO mapper。
  - 新增 `permissions/admin_guards.py`：权限管理 actor/resource 边界校验。
  - 新增 `permissions/admin_audit.py`：权限管理审计写入。
- 拆分后重点文件体量：
  - `admin/documents_service.py`：55 行。
  - `admin/users_service.py`：54 行。
  - `admin/knowledge_bases_service.py`：37 行。
  - `permissions/admin_service.py`：455 行。
  - 最大新增领域文件为 `admin/knowledge_base_writer.py`：644 行。

验证：

- `.venv/bin/python -m ruff check apps/api/app/modules/admin apps/api/app/modules/permissions/admin_service.py apps/api/app/modules/permissions/admin_readers.py apps/api/app/modules/permissions/admin_guards.py apps/api/app/modules/permissions/admin_audit.py`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_admin_service.py tests/unit/test_admin_route.py tests/unit/test_permission_admin_service.py tests/unit/test_permission_route.py`：106 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

### P5-4 拆分 IndexOpsService

优先级：中高。

现状：

- `indexing/ops_service.py` 约 563 行，同时承担 collection 健康 SQL、Qdrant 快照列表、创建快照、恢复快照、审计和问题判断。

改造：

- 拆出 `collection_health_repository.py`。
- 拆出 `collection_health_service.py`。
- 拆出 `qdrant_snapshot_service.py`。
- 拆出 `index_ops_audit.py`。

验收：

- 健康检查只读路径不依赖快照恢复操作代码。
- 高风险 Qdrant 恢复操作仍保留确认、审计和错误结构。

完成记录（2026-05-31）：

- `indexing/ops_service.py` 已收敛为 route-facing facade，仅负责把公开方法分发到健康检查和快照运维服务。
- 新增 `indexing/collection_health_repository.py`：
  - collection 健康 SQL 聚合读取。
  - collection 归属校验。
  - 健康检查分页 total 计算。
- 新增 `indexing/collection_health_service.py`：
  - collection 健康 DTO 组装。
  - Qdrant 可达性、collection 存在性、维度、points 数量和 pending cleanup 问题判断。
- 新增 `indexing/qdrant_snapshot_service.py`：
  - 快照列表、创建快照、恢复快照。
  - collection 名称归一化、恢复来源 URI 校验、高风险确认和 Qdrant 错误映射。
- 新增 `indexing/index_ops_audit.py`：
  - 快照创建和恢复相关审计写入。
- 拆分后重点文件体量：
  - `ops_service.py`：121 行。
  - `collection_health_repository.py`：175 行。
  - `collection_health_service.py`：119 行。
  - `qdrant_snapshot_service.py`：279 行。
  - `index_ops_audit.py`：39 行。

验证：

- `.venv/bin/python -m ruff check apps/api/app/modules/indexing/ops_service.py apps/api/app/modules/indexing/collection_health_repository.py apps/api/app/modules/indexing/collection_health_service.py apps/api/app/modules/indexing/qdrant_snapshot_service.py apps/api/app/modules/indexing/index_ops_audit.py`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_index_ops_service.py`：8 passed。
- `.venv/bin/python -m pytest -q tests/unit/test_admin_route.py tests/unit/test_qdrant_vector_retriever.py`：56 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

### P5-5 拆分 ConfigRepository

优先级：中。

现状：

- `config/repository.py` 约 631 行，同时承担 active config、版本详情、草稿配置、版本创建、状态切换和系统状态读取。

改造：

- 拆为 `config_read_repository.py`、`config_write_repository.py`、`config_version_repository.py`、`config_state_repository.py`。
- `ConfigService`、`ConfigReader`、`ConfigVersionService` 只依赖需要的 repository 能力。

验收：

- 配置读取路径和配置写入/激活路径依赖清晰分离。
- 配置管理、初始化、bootstrap 相关测试通过。

完成记录（2026-05-31）：

- `config/repository.py` 已收敛为兼容 facade，只聚合专用 repository；新代码不再依赖大而全的仓库类。
- 新增 `config/config_state_repository.py`：
  - 读取 `system_state` 初始化状态。
  - 读取和写入 `active_config_version`。
- 新增 `config/config_read_repository.py`：
  - 读取 active config row。
  - 按 hash 查找已有配置。
  - 查询草稿配置和草稿 section。
- 新增 `config/config_version_repository.py`：
  - 读取版本元数据、版本 payload、版本详情 payload。
  - 版本列表分页、版本总数、下一个版本号。
- 新增 `config/config_write_repository.py`：
  - 新建 config_versions / system_configs。
  - 更新版本 payload。
  - 状态切换、系统配置状态切换、旧 active config 下线。
- `ConfigReader` 改为只依赖 `ConfigReadRepository` 和 `ConfigStateRepository`。
- `ConfigVersionService` 改为依赖 `ConfigReadRepository`、`ConfigVersionRepository`、`ConfigWriteRepository`。
- `ConfigPublisher` 改为依赖 `ConfigStateRepository`、`ConfigVersionRepository`、`ConfigWriteRepository`。
- `ConfigService` 继续保留 `_load_*` 兼容委托，但委托到专用 repository。
- 拆分后重点文件体量：
  - `repository.py`：20 行。
  - `config_state_repository.py`：78 行。
  - `config_read_repository.py`：173 行。
  - `config_version_repository.py`：209 行。
  - `config_write_repository.py`：204 行。

验证：

- `.venv/bin/python -m ruff check apps/api/app/modules/config`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_config_service.py tests/unit/test_config_route.py tests/unit/test_setup_initialize_service.py tests/unit/test_setup_startup_service.py tests/unit/test_service_bootstrap.py`：50 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

### P5-6 拆分 SetupBootstrapService

优先级：中。

现状：

- `setup/bootstrap_service.py` 约 683 行，混合 bootstrap state、active config 校验、外部服务探测、模型 provider 探测和数据库状态写入。

改造：

- 拆出 `bootstrap_state_repository.py`。
- 拆出 `bootstrap_probe_service.py`。
- 拆出 `provider_probe_service.py`。
- `ServiceBootstrapStateService` 只做编排。

产出：

- 新增 `setup/bootstrap_types.py`，承载 `BootstrapCheck`、`ServiceBootstrapResult`、`ServiceBootstrapState` 等 bootstrap 数据结构。
- 新增 `setup/bootstrap_state_repository.py`，集中处理 alembic schema revision 读取、`system_state.service_bootstrap` 状态读取与写入。
- 新增 `setup/bootstrap_probe_service.py`，集中处理 secret ref、Redis、MinIO、Qdrant、关键词检索等非模型依赖探测。
- 新增 `setup/provider_probe_service.py`，集中处理 embedding、rerank、LLM provider healthcheck 和 provider auth token 读取。
- `setup/bootstrap_service.py` 保留 public import 路径和 `_http_get`、`_redis_ping` 兼容 monkeypatch 点，只负责编排 active config 加载、schema 校验和各 probe service 调用。
- `ServiceBootstrapStateService` 已改为通过 `ServiceBootstrapStateRepository` 读取缓存状态，避免自身直接拼写 `system_state` SQL。

拆分后行数：

- `bootstrap_service.py`：383 行。
- `bootstrap_state_repository.py`：99 行。
- `bootstrap_probe_service.py`：209 行。
- `provider_probe_service.py`：98 行。
- `bootstrap_types.py`：75 行。

验收：

- readiness / setup guard 不直接关心外部 provider 协议细节。
- 初始化后、active config 缺失、provider 不可用三类状态可分别测试。

验证：

- `.venv/bin/python -m ruff check apps/api/app/modules/setup tests/unit/test_service_bootstrap.py tests/unit/test_setup_startup_service.py tests/unit/test_setup_route.py`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_service_bootstrap.py tests/unit/test_setup_startup_service.py tests/unit/test_setup_route.py`：15 passed。
- `.venv/bin/python -m pytest -q tests/unit/test_config_service.py tests/unit/test_config_route.py tests/unit/test_setup_initialize_service.py tests/unit/test_setup_service.py tests/unit/test_setup_startup_service.py tests/unit/test_service_bootstrap.py`：56 passed。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

### P5-7 拆分 admin API schema

优先级：中低。

现状：

- `api/schemas/admin.py` 约 660 行，用户、部门、角色、知识库、文档、索引运维 DTO 聚合在一个文件中。

改造：

- 拆为 `admin_users.py`、`admin_departments.py`、`admin_roles.py`、`admin_knowledge.py`、`admin_documents.py`、`admin_index_ops.py`。
- 保留 `api/schemas/admin.py` 聚合导出或直接改 route 导入路径；最终以 OpenAPI 生成稳定为准。

产出：

- 新增 `api/schemas/admin_common.py`，承载管理后台通用 accepted 响应。
- 新增 `api/schemas/admin_departments.py`，承载部门 DTO。
- 新增 `api/schemas/admin_roles.py`，承载角色、可分配角色和角色绑定 DTO。
- 新增 `api/schemas/admin_users.py`，承载用户、用户部门和密码重置 DTO。
- 新增 `api/schemas/admin_knowledge.py`，承载知识库、知识库权限规则和文件夹 DTO。
- 新增 `api/schemas/admin_documents.py`，承载文档、文档版本、chunk 和文档预览 DTO。
- 新增 `api/schemas/admin_index_ops.py`，承载索引版本、collection 健康、快照、恢复和重建任务 DTO。
- `api/schemas/admin.py` 已改为聚合导出入口，继续兼容 `from app.api.schemas.admin import ...`。
- 聚合入口会把拆分后的 Pydantic 模型 `__module__` 归一回 `app.api.schemas.admin` 并强制 rebuild，避免 OpenAPI 同名 schema 组件名从旧路径漂移到拆分文件路径。

拆分后行数：

- `admin.py`：26 行。
- `admin_common.py`：19 行。
- `admin_departments.py`：75 行。
- `admin_roles.py`：101 行。
- `admin_users.py`：90 行。
- `admin_knowledge.py`：167 行。
- `admin_documents.py`：135 行。
- `admin_index_ops.py`：126 行。

验收：

- DTO 拆分不改变 OpenAPI 字段契约。
- admin route typecheck / 单测通过。

验证：

- `.venv/bin/python -m ruff check apps/api/app/api/schemas/admin.py apps/api/app/api/schemas/admin_common.py apps/api/app/api/schemas/admin_departments.py apps/api/app/api/schemas/admin_roles.py apps/api/app/api/schemas/admin_users.py apps/api/app/api/schemas/admin_knowledge.py apps/api/app/api/schemas/admin_documents.py apps/api/app/api/schemas/admin_index_ops.py apps/api/app/api/routes/admin_shared.py apps/api/app/api/routes/admin_users.py apps/api/app/api/routes/admin_departments.py apps/api/app/api/routes/admin_knowledge.py apps/api/app/api/routes/admin_documents.py apps/api/app/api/routes/admin_index_ops.py apps/api/app/api/presenters/admin.py tests/unit/test_admin_route.py`：通过。
- `.venv/bin/python -m pytest -q tests/unit/test_admin_route.py tests/unit/test_admin_service.py`：101 passed。
- `PYTHONPATH=apps/api .venv/bin/python -c "from app.main import create_app; comps=create_app(run_startup_checks=False).openapi().get('components',{}).get('schemas',{}); required=['UserResponse','DepartmentListResponse','KnowledgeBaseResponse','app__api__schemas__admin__DocumentListResponse','IndexHealthResponse','RoleBindingListResponse']; missing=[x for x in required if x not in comps]; print('schemas', len(comps), 'missing', missing)"`：`schemas 152 missing []`。
- `make PYTHON=.venv/bin/python test`：349 passed。

状态：已完成（2026-05-31）。

## 暂不合并的合理重复

### public/admin DTO 不直接合并

原因：

- public DTO 和 admin DTO 的字段暴露边界不同。
- public 接口应最小化返回字段，admin 接口可以返回管理所需字段。

处理：

- 可以重命名为 `PublicDocumentData`、`AdminDocumentData` 提升可读性。
- 不建议为了减少重复把字段模型合并。

### 列表 DTO 和详情 DTO 不直接合并

原因：

- 列表接口必须避免返回详情字段。
- 详情接口必须重新做权限检查。

处理：

- 继续保留 `ListItemData` 和 `Data` 分离。
- 只抽公共字段 mixin 或 presenter，不改变接口暴露边界。

## 推荐执行顺序

说明：

- 下面顺序按依赖关系排序，不等同于 P0/P1/P2/P3 编号。
- P0/P1/P2/P3 表示风险等级和业务优先级；实际执行先处理会影响后续拆分的公共边界，再拆核心 service。

排序原则：

- 先固定模块依赖方向、错误模型、路由公共能力和分页结构。
- 再抽取对象存储、审计、权限等跨模块公共能力。
- 再收紧接口边界和 schema 边界，避免重构时继续扩大前后端耦合。
- 再拆 config、auth、indexing、import、setup、query 等核心 service。
- 最后处理 admin 大模块、日志读取侧、provider 公共 HTTP 和空模块清理。

1. P3-5 梳理模块依赖方向。
2. P0-3 统一 ServiceError 基类。
3. P0-2 统一 API 错误响应构造。
4. P0-1 统一路由层认证与 request_id 处理。
5. P2-1 抽离 PaginationData。
6. P1-5 建立模块化 route 注册层。
7. P0-4 统一 ObjectStorage 运行时构造。（已完成）
8. P3-3 审计日志写入统一封装。（已完成）
9. P3-2 收敛权限校验 helper。（已完成）
10. P2-7 拆分 PermissionService 基础能力。（已完成）
11. P2-4 清理 admin/public schema 交叉引用。（已完成）
12. P2-2 删除 public document preview 废弃接口。（已完成）
13. P2-3 配置列表分页下沉 service。（已完成）
14. P1-6 拆分 ConfigService。（已完成）
15. P2-5 拆分 AuthService。（已完成）
16. P1-8 拆分 IndexingService。（已完成）
17. P1-7 拆分 ImportService。（已完成）
18. P1-10 拆分 SetupInitializationService。（已完成）
19. P1-9 拆分 QueryService。（已完成，真实环境回归待验收）
20. P2-6 拆分 KnowledgeService。（已完成）
21. P1-4 解除 permissions 路由对 AdminService 的反向依赖。（已完成）
22. P1-1 拆分 AdminService。（已完成）
23. P1-2 拆分 admin route。（已完成）
24. P1-3 抽离 DTO presenter/mapper。（已完成）
25. P3-6 拆分 AuditService 读取侧。（已完成）
26. P3-7 收敛 models HTTP client 公共逻辑。（已完成）
27. P3-4 清理空模块和架构残留。（已完成）
28. P4-1 ImportPipeline core 实迁收尾。（已完成）
29. P4-2 Query core 实迁收尾。（已完成）
30. P4-3 Indexing core 实迁收尾。（已完成）
31. P4-4 PermissionAdminService 实迁收尾。（已完成）
32. P4-5 SetupInitialization core 实迁收尾。（已完成）
33. P4-6 Knowledge core 实迁收尾。（已完成）
34. P4-7 QueryConversationService 拆分。（已完成）
35. P4-8 非 admin route 收尾。（已完成）
36. P5-1 删除 core 中间产物与兼容入口。（已完成）
37. P5-2 修正 shared 与 adapters 的依赖方向。（已完成）
38. P5-3 拆分 admin 领域大 service。（已完成）
39. P5-4 拆分 IndexOpsService。（已完成）
40. P5-5 拆分 ConfigRepository。（已完成）
41. P5-6 拆分 SetupBootstrapService。（已完成）
42. P5-7 拆分 admin API schema。（已完成）

## 验证要求

每完成一项至少执行：

- `make test` 或对应单元测试。
- 受影响模块的接口测试。
- `ruff check`。
- 前端相关接口变化需要执行对应前端 build。

高风险项额外要求：

- 权限相关重构必须覆盖系统管理员、部门管理员、知识库管理员、普通员工四类账号。
- 错误响应重构必须验证 401、403、404、409、428、500 场景。
- ObjectStorage 重构必须验证导入、文档预览、citation source 读取。
