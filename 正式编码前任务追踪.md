# 正式编码前任务追踪

> 目标：在进入正式编码前，把当前设计文档收敛为可执行、可验证、可追踪的工程契约，降低后续返工、权限绕过、接口不一致和数据模型反复调整的风险。

## 1. 编码准入标准

只有当以下 P0 项全部完成后，才建议进入正式业务编码：

- [ ] MVP 闭环范围已冻结。
- [ ] RESTful API 已落成 OpenAPI 契约。
- [ ] 核心数据库 schema 已明确字段、约束、索引和枚举。
- [ ] 接口权限矩阵已明确到 endpoint 级别。
- [ ] 核心状态机已明确允许迁移和失败处理。
- [ ] P0 测试计划已覆盖初始化、认证、权限、导入、索引和查询闭环。
- [ ] 本地开发环境和最小依赖启动方式已明确。

## 2. P0 任务

### 2.1 冻结 MVP 闭环范围

状态：待评审

产物：

- [x] `docs/MVP范围说明.md`
- [x] P0 功能列表
- [x] P0 暂不实现列表
- [x] 第一阶段端到端用户路径

建议 P0 闭环：

1. 初始化系统。
2. 创建首个系统管理员。
3. 登录并获取 access/refresh JWT。
4. 创建组织、用户和角色绑定。
5. 创建知识库。
6. 上传文档并创建导入任务。
7. Worker 解析、切块、向量化、写 draft 索引。
8. 发布 active index。
9. 用户按权限查询。
10. 返回带 citation 的答案。
11. 写查询、导入、权限和管理员操作审计。

完成标准：

- P0 范围可以支持“文档导入到可权限过滤查询”的完整闭环。
- P0 不包含复杂审批流、多租户运营后台、外部组织同步、高级评测平台、复杂多 Agent 编排等非首阶段能力。
- 每个 P0 功能都能映射到 API、数据表、状态机和测试用例。

### 2.2 生成 OpenAPI 契约

状态：未开始

依据文档：

- `docs/modules/15-前端与管理后台API设计实现文档.md`
- `docs/modules/00-公共实现约束.md`

产物：

- [ ] `openapi.yaml`
- [ ] API 分组标签
- [ ] request schema
- [ ] response schema
- [ ] error schema
- [ ] 分页 schema
- [ ] SSE 事件 schema
- [ ] multipart/form-data 上传 schema
- [ ] 鉴权声明
- [ ] HTTP status 规范

必须覆盖：

- [ ] 初始化 API
- [ ] 会话与当前用户 API
- [ ] 查询问答 API
- [ ] 普通知识库浏览 API
- [ ] 文档导入与任务 API
- [ ] 管理后台知识库 API
- [ ] 管理后台文件夹 API
- [ ] 管理后台文档 API
- [ ] 管理后台用户 API
- [ ] 管理后台组织 API
- [ ] 管理后台角色与权限 API
- [ ] 管理后台配置 API
- [ ] 管理后台审计与可观测 API
- [ ] healthcheck API
- [ ] 内部模型网关 API

完成标准：

- 后端实现、前端联调和 API contract 测试都以 `openapi.yaml` 为准。
- 每个接口都有明确 request、response、错误码和权限要求。
- 不再存在旧 RPC 风格路径。

### 2.3 落地数据库 Schema

状态：未开始

依据文档：

- `docs/modules/14-核心数据模型设计实现文档.md`
- `docs/modules/01-初始化服务设计实现文档.md`
- `docs/modules/02-认证服务设计实现文档.md`
- `docs/modules/03-组织服务设计实现文档.md`
- `docs/modules/04-权限服务设计实现文档.md`
- `docs/modules/05-配置服务设计实现文档.md`
- `docs/modules/06-导入服务与工作进程设计实现文档.md`
- `docs/modules/07-索引服务设计实现文档.md`
- `docs/modules/11-审计与可观测性设计实现文档.md`

产物：

- [ ] `database-schema.md`
- [ ] 首批 Alembic migration 草案
- [ ] 枚举定义清单
- [ ] 唯一约束清单
- [ ] 外键约束清单
- [ ] 查询索引清单
- [ ] 软删除和 access block 约束说明

必须明确：

- [ ] `system_state`
- [ ] `setup_tokens` 或等价 token 状态表
- [ ] `users`
- [ ] `user_credentials`
- [ ] `jwt_token_states`
- [ ] `enterprises`
- [ ] `departments`
- [ ] `department_members`
- [ ] `roles`
- [ ] `role_bindings`
- [ ] `resource_policies`
- [ ] `permission_snapshots`
- [ ] `config_items`
- [ ] `config_versions`
- [ ] `secrets`
- [ ] `knowledge_bases`
- [ ] `folders`
- [ ] `documents`
- [ ] `document_versions`
- [ ] `chunks`
- [ ] `index_versions`
- [ ] `chunk_index_refs`
- [ ] `access_blocks`
- [ ] `import_jobs`
- [ ] `audit_logs`
- [ ] `query_logs`
- [ ] `model_call_logs`
- [ ] `query_cache_entries`

完成标准：

- 所有表都有 `enterprise_id`、主键、时间戳和必要状态字段。
- 所有跨租户查询都有可用索引。
- 文档、chunk、索引、权限快照之间可对账。
- draft index、deleted document、blocked resource 无法被查询命中。

### 2.4 制定接口权限矩阵

状态：未开始

产物：

- [ ] `权限矩阵.md`

矩阵字段：

| 字段 | 说明 |
| --- | --- |
| endpoint | API 路径 |
| method | HTTP 方法 |
| user_type | anonymous、setup、user、admin、service |
| required_scope | 需要的 scope |
| resource_check | 是否需要资源范围校验 |
| risk_level | low、medium、high、critical |
| audit_required | 是否必须审计 |
| confirmation_required | 是否需要二次确认 |
| rollback_strategy | 失败或误操作回滚策略 |

高风险操作必须覆盖：

- [ ] 初始化提交。
- [ ] setup JWT 签发和轮换。
- [ ] 系统管理员创建、禁用、删除。
- [ ] 高风险角色绑定。
- [ ] 文档可见性从 department 改为 enterprise。
- [ ] 权限收紧。
- [ ] 配置发布。
- [ ] 配置回滚。
- [ ] 文档删除。
- [ ] 知识库删除。
- [ ] 索引重建和发布。

完成标准：

- 每个写接口都有 scope、资源范围和审计要求。
- 前端菜单权限与后端真实权限校验可以对应。
- 管理员接口不能只依赖前端隐藏菜单。

### 2.5 固化核心状态机

状态：未开始

产物：

- [ ] `状态机设计.md`

必须覆盖：

- [ ] setup 初始化状态机。
- [ ] setup JWT 状态机。
- [ ] access/refresh token 状态机。
- [ ] import_job 状态机。
- [ ] document lifecycle 状态机。
- [ ] document_version 状态机。
- [ ] index_version 状态机。
- [ ] chunk_index_ref visibility 状态机。
- [ ] config_version 状态机。
- [ ] access_block 生命周期。

每个状态机必须明确：

- [ ] 状态枚举。
- [ ] 允许迁移。
- [ ] 禁止迁移。
- [ ] 幂等行为。
- [ ] 失败重试。
- [ ] 取消行为。
- [ ] 回滚或补偿。
- [ ] 审计事件。

完成标准：

- Worker、API 和管理后台不能绕过状态机直接改状态。
- `PATCH status` 类接口有明确的允许状态迁移。
- 任务重试、取消、恢复不会破坏数据一致性。

### 2.6 制定 P0 测试计划

状态：未开始

依据文档：

- `docs/modules/00-公共实现约束.md`

产物：

- [ ] `测试计划.md`

测试分层：

- [ ] policy 单元测试。
- [ ] service 用例测试。
- [ ] repository 数据库测试。
- [ ] adapter 集成测试。
- [ ] API contract 测试。
- [ ] 端到端测试。
- [ ] 权限绕过测试。
- [ ] 超时、重试、熔断和降级测试。

P0 必测链路：

- [ ] 首次初始化成功。
- [ ] setup JWT 每次签发新 token，旧 token 失效。
- [ ] 未初始化时普通业务 API 返回 `SETUP_REQUIRED`。
- [ ] 本地账号登录成功和失败锁定。
- [ ] refresh token rotation。
- [ ] 管理员创建用户和绑定角色。
- [ ] 文档上传创建导入任务。
- [ ] Worker 处理导入任务。
- [ ] draft index 查询不可见。
- [ ] active index 查询可见。
- [ ] 权限过滤下推到向量检索和关键词检索。
- [ ] 权限收紧后旧用户无法查询。
- [ ] 文档删除后无法通过缓存或旧索引命中。
- [ ] 查询返回 citation。
- [ ] strict citation 模式不会输出未经引用校验的最终答案。
- [ ] 查询缓存不会跨用户、跨租户、跨权限过滤条件复用。

完成标准：

- 每条 P0 链路都有自动化测试或明确手工验收步骤。
- 高风险权限场景必须自动化。
- 测试数据可重复构造和清理。

### 2.7 准备本地开发环境

状态：未开始

产物：

- [ ] `本地开发环境.md`
- [ ] `.env.example`
- [ ] `docker-compose.yml` 草案
- [ ] `Makefile` 或等价任务入口草案

最小依赖：

- [ ] PostgreSQL
- [ ] Redis
- [ ] MinIO
- [ ] Qdrant
- [ ] 模型网关 mock
- [ ] Embedding mock
- [ ] Rerank mock
- [ ] LLM mock

必须明确：

- [ ] 启动配置只允许数据库连接和进程参数。
- [ ] 业务配置通过初始化接口写入 active config。
- [ ] 未初始化时只开放 setup-state、setup-config-validations、setup-initialization。
- [ ] ready 检查必须覆盖数据库、初始化状态、active config、ServiceBootstrap 状态和迁移版本。

完成标准：

- 新开发者可以按文档启动最小环境。
- 可以完成一次初始化、登录、导入 mock 文档、查询 mock 答案。
- 不需要真实大模型即可跑通 P0 自动化测试。

## 3. P1 任务

### 3.1 管理后台交互契约

状态：未开始

- [ ] 页面路由清单。
- [ ] 菜单与 scope 映射。
- [ ] 表格筛选字段。
- [ ] 表单字段和校验规则。
- [ ] 高风险操作确认文案和二次确认机制。
- [ ] 审计详情展示脱敏规则。

### 3.2 配置 Schema 与默认值

状态：未开始

- [ ] active config JSON schema。
- [ ] 初始化配置 JSON schema。
- [ ] 配置项默认值。
- [ ] 配置版本 diff 规则。
- [ ] 高风险配置列表。
- [ ] 配置热更新失败策略。

### 3.3 审计事件字典

状态：未开始

- [ ] 审计事件名称。
- [ ] actor 字段。
- [ ] resource 字段。
- [ ] before/after 摘要。
- [ ] risk_level。
- [ ] retention 策略。
- [ ] 脱敏策略。

### 3.4 模型与检索评测基线

状态：未开始

- [ ] 最小离线评测集。
- [ ] 检索 recall@k 指标。
- [ ] citation 命中率。
- [ ] 无答案拒答率。
- [ ] 延迟和成本指标。
- [ ] 模型不可用降级验收。

### 3.5 部署与发布检查清单

状态：未开始

- [ ] migration 执行策略。
- [ ] 初始化和恢复初始化流程。
- [ ] 备份与恢复演练。
- [ ] 配置回滚演练。
- [ ] 索引重建演练。
- [ ] 权限收紧演练。
- [ ] 日志、指标、Trace 验收。

## 4. 建议编码顺序

1. 工程骨架：FastAPI、错误结构、request_id、trace_id、日志中间件。
2. 数据库基础：SQLAlchemy、Alembic、事务管理、repository 基类。
3. Setup Service：初始化状态、setup JWT、初始化配置校验、active config v1。
4. Config Service：active config 读取、缓存、版本、热更新通知。
5. Auth Service：用户、密码、登录、refresh token rotation、JWT 状态。
6. Org Service：企业、部门、成员关系、组织版本。
7. Permission Service：角色、绑定、权限上下文、资源权限策略。
8. KnowledgeBase / Document 元数据。
9. Import Service 与 Worker：任务创建、领取、解析、切块、embedding。
10. Indexing Service：draft 写入、校验、active 发布、回滚和删除阻断。
11. Query Service：查询入口、权限过滤、召回、rerank、上下文、答案和 citation。
12. Audit / Observability：审计表、查询日志、模型调用日志、指标聚合。
13. 管理后台 API 完整补齐。

## 5. 风险清单

| 风险 | 影响 | 编码前动作 |
| --- | --- | --- |
| API 只有路径，没有 schema | 前后端联调反复返工 | 先完成 OpenAPI |
| 数据模型缺少约束和索引 | 查询慢、状态错乱、权限绕过 | 先完成 schema 和 migration 草案 |
| 权限矩阵不明确 | 管理后台越权风险 | endpoint 级权限矩阵先行 |
| 状态机没有固化 | Worker 重试、取消、回滚行为不一致 | 状态机设计先行 |
| 真实模型依赖过早接入 | 开发和测试不稳定 | 先做模型 mock 和 adapter |
| 配置和 secret 处理不清晰 | 部署后难以恢复和审计 | 初始化配置 schema 和 Secret Store 策略先行 |
| 缓存 key 设计不完整 | 跨用户、跨权限串权 | 查询缓存测试和 key 规范先行 |

## 6. 当前文档待清理项

- [ ] `docs/modules/15-前端与管理后台API设计实现文档.md` 开头仍有“如果模块文档中仍存在早期 RPC 风格路径，应以后续本文定义为准并逐步同步”的历史表述。当前处于设计阶段，建议删除，避免暗示存在旧路径兼容。
- [ ] 检查所有模块文档中的 API 片段，确保与 `15-前端与管理后台API设计实现文档.md` 完全一致。
- [ ] 检查 `docs/README.md` 中关于 setup token 的表述，统一为 setup JWT。

## 7. 每日任务追踪模板

| 日期 | 任务 | 状态 | 阻塞 | 下一步 |
| --- | --- | --- | --- | --- |
|  |  | 未开始 |  |  |

状态枚举：

- 未开始
- 进行中
- 待评审
- 已完成
- 阻塞
