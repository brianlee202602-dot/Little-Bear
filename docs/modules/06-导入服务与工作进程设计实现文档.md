# Import Service 与 Workers 设计实现文档

## 1. 模块目标

Import Service 负责文档导入入口和任务创建，Import Workers 负责异步执行解析、清洗、切块、向量化、索引写入和发布，保证大批量导入不阻塞线上查询。

## 2. 模块职责

Import Service：

- 校验导入权限。
- 校验文件大小、类型、幂等键。
- 创建 `import_jobs`、`documents`。
- 原始文件写入对象存储。
- 返回 `job_id`。
- 提供任务状态查询。

Import Workers：

- 领取导入任务。
- 执行阶段状态机。
- 解析、清洗、切块。
- 调用 embedding。
- 写向量索引和关键词索引。
- 校验索引可用性。
- 发布索引版本。
- 失败重试和补偿清理。

## 3. 非职责

- 不直接决定用户权限，导入权限由 Permission Service 校验。
- 不直接调用 Qdrant、MinIO、模型 SDK，必须通过 ports/adapters。
- 不在 HTTP 请求内执行长耗时解析和 embedding。

## 4. 建议包结构

```text
app/modules/import_pipeline/
  service.py
  job_service.py
  state_machine.py
  parsers/
  cleaners/
  chunkers/
  worker.py
  stages/
    validate.py
    parse.py
    clean.py
    chunk.py
    embed.py
    index.py
    publish.py
  repositories.py
  schemas.py
  errors.py
  events.py
```

## 5. 核心表

`import_jobs`：

| 字段 | 说明 |
| --- | --- |
| id | 任务 ID |
| enterprise_id | 企业 ID |
| kb_id | 知识库 ID |
| created_by | 创建人 |
| job_type | upload、api_batch、connector_sync |
| status | queued、running、retrying、partial_success、success、failed、cancelled |
| stage | validate、parse、clean、chunk、embed、index、publish |
| total_items | 总数 |
| succeeded_items | 成功数 |
| failed_items | 失败数 |
| error_summary | 错误摘要 |
| idempotency_key | 幂等键 |
| priority | 优先级 |
| locked_by | Worker ID |
| locked_until | 锁过期时间 |
| retry_count | 重试次数 |
| next_retry_at | 下次重试时间 |

`documents`、`document_versions`、`chunks`、`index_versions` 和 `chunk_index_refs` 由核心数据模型统一维护，字段契约详见 `14-核心数据模型设计实现文档.md`。

## 6. 导入 API

```http
POST /internal/v1/knowledge-bases/{kb_id}/documents:upload
POST /internal/v1/knowledge-bases/{kb_id}/documents:import
GET  /internal/v1/import-jobs/{job_id}
POST /internal/v1/import-jobs/{job_id}:cancel
POST /internal/v1/import-jobs/{job_id}:retry
```

HTTP 导入响应：

```json
{
  "job_id": "job_001",
  "status": "queued",
  "document_ids": ["doc_001"]
}
```

上传/导入请求必须支持文档可见范围：

```json
{
  "owner_department_id": "dept_rnd",
  "visibility": "department"
}
```

可见范围语义：

- `owner_department_id` 默认取上传人的主部门，也可以由有权限的管理员指定。
- `visibility=department` 表示仅 `owner_department_id` 对应部门成员可检索，是成员上传文档的默认值。
- `visibility=enterprise` 表示企业公开，当前企业内所有部门成员都可检索。
- 文档权限设置界面只展示“部门可见”和“企业公开”两个选项，不展示上级部门、子部门、递归共享或自定义组织 ID 列表。
- 普通成员默认只能上传到自己的主部门；只有具备部门管理或系统管理权限的操作者可以指定其他 `owner_department_id`。
- 企业公开属于扩大可见范围，必须记录审计；如业务需要，可配置为需要审批。

示例：

```text
企业
  研发部
  人事部
```

研发部成员上传文档时：

- 默认 `visibility=department`：只有研发部成员可检索。
- 选择 `visibility=enterprise`：企业内所有部门成员均可检索。

```json
{
  "owner_department_id": "dept_rnd",
  "visibility": "enterprise",
  "org_version": 18
}
```

查询时不得递归组织树或遍历数据库计算共享范围。权限过滤只使用文档 `visibility`、`owner_department_id` 和查询用户的部门 ID；数据库回源只作为召回后的二次 gate。

## 7. 总流程

```text
1. API Gateway 做认证、限流、请求大小检查
2. Permission Service 校验 document:import 和目标知识库写权限
3. Permission Service 校验 `owner_department_id` 和 `visibility`，生成 permission_snapshot
4. Import Service 校验文件类型、大小、幂等键
5. 写 import_job、document 和权限快照引用
6. 原始文件写入 ObjectStorage
7. Worker 领取任务
8. validate
9. parse
10. clean
11. chunk
12. embed
13. index_vector(draft)，写入 `owner_department_id` 和 `visibility`
14. index_keyword(draft)，写入同一套权限字段
15. validate_index，确认 draft 索引对查询不可见
16. publish，激活 active index version
17. 写审计和指标
```

## 8. Worker 领取任务

基于 PostgreSQL 任务表：

```text
1. 查询 queued/retrying 且 next_retry_at <= now 的任务
2. 使用 SKIP LOCKED 或乐观锁领取
3. 设置 locked_by、locked_until、status=running
4. 执行当前 stage
5. 成功后推进 stage
6. 失败可重试则 retry_count + 1，status=retrying，设置 next_retry_at
7. 不可重试则 failed
```

Worker 必须定期续约 `locked_until`。如果 Worker 崩溃，锁过期后其他 Worker 可接管。

状态推进约束：

- `queued -> running -> success` 表示首次执行成功。
- `running -> retrying -> running` 表示可重试失败后的再次领取。
- `running -> failed` 只用于不可重试错误或超过最大重试次数。
- `cancelled` 只能由 API 或管理员动作触发，Worker 领取和阶段推进前必须检查取消状态。
- 发布阶段成功前，`document.index_status` 不能变为 `indexed`，`chunk_index_refs.visibility_state` 必须保持 `draft`。

## 9. 解析阶段

统一解析输出：

```json
{
  "doc_id": "doc_001",
  "title": "销售合同审批制度",
  "sections": [
    {
      "heading": "审批材料",
      "level": 2,
      "text": "销售合同审批需提交...",
      "page_start": 3,
      "page_end": 4,
      "tables": [],
      "images": [],
      "source_offsets": {"start": 1200, "end": 1980}
    }
  ],
  "metadata": {
    "author": "alice",
    "department": "dept_sales"
  }
}
```

解析要求：

- 保留页码、标题层级、表格、图片说明。
- 扫描件走 OCR，并记录 OCR 置信度。
- 代码、配置、公式、合同条款不能被普通清洗破坏。
- 失败记录解析器版本和错误码。

## 10. 清洗与切块

清洗原则：提高检索质量，不改写业务事实。

切块默认策略：

- 按标题、段落、语义边界切分。
- chunk 大小 300 到 800 tokens。
- overlap 50 到 150 tokens。
- 保留 heading_path、页码、文档版本、来源 URI。
- FAQ 问答对、合同条款、表格尽量不拆散。

## 11. Embedding 阶段

要求：

- 使用 Model Gateway。
- document embedding 使用离线 batch 池。
- 向量 ID 确定性生成：`chunk_id + embedding_model + doc_version`。
- 记录 `embedding_model`、`model_version`、`dimension`。
- 模型失败可重试，超过阈值进入 failed 或 dead-letter。

## 12. 并发控制

建议值：

| 维度 | 建议 |
| --- | --- |
| 全局导入任务并发 | 3 到 5 |
| 单部门导入任务并发 | 1 到 2 |
| 单用户导入任务并发 | 1 |
| 单任务文件并发 | 2 到 4 |
| Embedding batch size | 16 到 64 |
| index batch size | 100 到 500 chunks |

并发策略由 active config 管理。

## 13. 错误码

| 错误码 | 场景 |
| --- | --- |
| `IMPORT_FILE_TOO_LARGE` | 文件过大 |
| `IMPORT_FILE_TYPE_UNSUPPORTED` | 文件类型不支持 |
| `IMPORT_IDEMPOTENCY_CONFLICT` | 幂等键冲突 |
| `IMPORT_PARSE_FAILED` | 文档解析失败 |
| `IMPORT_CLEAN_EMPTY` | 清洗结果为空 |
| `IMPORT_CHUNK_FAILED` | 切块失败 |
| `IMPORT_EMBED_FAILED` | 向量化失败 |
| `IMPORT_CANCELLED` | 用户取消 |

## 14. 审计事件

- `import.job.created`
- `import.job.cancelled`
- `import.stage.started`
- `import.stage.succeeded`
- `import.stage.failed`
- `import.job.completed`

## 15. 测试点

- HTTP 上传只创建任务，不执行长耗时导入。
- 同一幂等键重复提交返回同一任务。
- Worker 崩溃后任务可被其他 Worker 接管。
- 解析失败可定位文件和解析器版本。
- embedding 服务不可用不会影响查询链路。
- 导入失败不会发布未完成或未校验的索引。
- draft 索引写入后在发布前不会被查询命中。
- 成员上传文档默认 `visibility=department`，仅归属部门成员可检索。
- 选择 `visibility=enterprise` 后，企业内所有部门成员可检索。
- 文档权限设置不支持递归部门、上级部门、子部门或自定义组织 ID 共享。
- 查询不会先遍历数据库筛选可见文档，权限过滤必须下推到向量索引和关键词索引。
