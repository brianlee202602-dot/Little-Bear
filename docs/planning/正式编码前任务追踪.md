# 正式编码前任务追踪

> 目标：在进入正式编码前，把当前设计文档收敛为可执行、可验证、可追踪的工程契约，降低后续返工、权限绕过、接口不一致和数据模型反复调整的风险。

## 1. 编码准入标准

只有当以下 P0 项全部完成后，才建议进入正式业务编码：

- [x] MVP 闭环范围已冻结。
- [x] 初始化配置与 `active_config v1` schema 已明确。
- [x] RESTful API 已落成 OpenAPI 契约。
- [x] 核心数据库 schema 已明确字段、约束、索引和枚举。
- [x] 接口权限矩阵已明确到 endpoint 级别。
- [x] 核心状态机已明确允许迁移和失败处理。
- [x] P0 审计事件字典和脱敏规则已明确。
- [x] P0 测试计划已覆盖初始化、认证、权限、导入、索引、查询、删除阻断和外部模型 provider 降级。
- [x] 本地开发环境和最小依赖启动方式已明确。

## 2. P0 任务

### 2.0 当前实现进度快照

状态：持续维护

最近更新：2026-05-16 21:01

当前代码实现已经完成控制面、导入索引最小链路、Permission Service、非流式查询、Query Stream 第一版、普通用户 Web 查询工作区和管理后台知识库运营产品面。Permission Service 已接入知识库浏览、导入索引和查询链路，可生成统一过滤条件并执行候选准入 gate。导入链路已接入 MinIO/S3 对象存储交接和 PDF / DOCX / UTF-8 文本 / Markdown parse-clean-chunk 第一版。文档详情、版本、chunk、普通用户文档预览，以及知识库 / 文档独立权限变更 API 已补齐。

当前主要缺口集中在 RAG 数据面的真实生产能力：

- 复杂版式 PDF、扫描件 OCR、表格结构保真和解析质量评估仍需要补齐。
- 管理后台仍缺文档全文预览、页内定位、文档移动 / 生命周期变更和权限收紧后的索引 payload 刷新闭环。
- Query Stream 当前是服务端缓冲式 SSE，尚未升级为 provider token 级流式输出。
- 查询改写、回归数据集和真实业务验收集仍未落地。
- P0 smoke 和真实 MinIO + Worker + API 联调还需要在真实初始化环境中形成固定执行记录。

进度入口：

- 根目录 `开发进度追踪.md`

本轮已完成的进度追踪改进：

- [x] README 当前状态已更新为“控制面已初步落地，RAG 数据面待实现”。
- [x] 新增实际 FastAPI routes 与 `docs/contracts/openapi.yaml` 的契约差异测试。
- [x] 补齐 `DELETE /internal/v1/admin/config-versions/{version}` 在 OpenAPI、权限矩阵、管理后台契约和审计事件字典中的登记。
- [x] Permission Service 核心实现已新增，包含权限上下文、过滤条件、候选准入 gate、P0 可见性策略校验和单元测试。
- [x] P0 查询闭环、流式查询、普通 Web 查询工作区、P0 smoke 入口和 employee 知识库浏览权限兼容迁移已更新到根目录进度追踪。
- [x] 对象存储 runtime、文本 parse-clean-chunk 执行器和 Worker 失败落库已更新到根目录进度追踪。
- [x] 根目录进度追踪已清理旧状态表述。
- [x] `make test` 已修复为执行全量 ruff 和单元测试；全量 ruff 当前通过。
- [x] `make smoke-p0` 已改为显式要求 smoke 用户名和密码，避免硬编码默认凭据造成 `401` 假失败。
- [x] PDF / DOCX parser 第一版已接入，上传文件类型和大小限制改为读取 `active_config.import`。
- [x] 文档详情、文档版本、文档 chunk、普通用户文档预览，以及知识库 / 文档独立权限变更 API 已补齐。

### 2.1 冻结 MVP 闭环范围

状态：已完成

产物：

- [x] `docs/MVP范围说明.md`
- [x] P0 功能列表
- [x] P0 暂不实现列表
- [x] 第一阶段端到端用户路径

建议 P0 闭环：

1. 初始化系统。
2. 创建首个系统管理员。
3. 登录并获取 access/refresh JWT。
4. 创建组织、用户和角色绑定。
5. 创建知识库。
6. 上传文档并创建导入任务。
7. Worker 解析、切块、向量化、写 draft 索引。
8. 发布 active index。
9. 用户按权限查询。
10. 返回带 citation 的答案。
11. 写查询、导入、权限和管理员操作审计。

完成标准：

- P0 范围可以支持“文档导入到可权限过滤查询”的完整闭环。
- P0 不包含复杂审批流、多租户运营后台、外部组织同步、高级评测平台、复杂多 Agent 编排等非首阶段能力。
- 每个 P0 功能都能映射到 API、数据表、状态机和测试用例。

### 2.2 固化初始化配置与 active_config v1 Schema

状态：待评审

依据文档：

- `docs/MVP范围说明.md`
- `docs/modules/01-初始化服务设计实现文档.md`
- `docs/modules/05-配置服务设计实现文档.md`
- `docs/modules/10-模型网关设计实现文档.md`

产物：

- [x] `docs/contracts/config-schema.md`
- [x] `docs/contracts/config.schema.json`
- [x] 初始化配置 JSON schema
- [x] `active_config v1` JSON schema
- [x] Secret 引用格式和校验规则
- [x] 外部 embedding、rerank、LLM provider 配置 schema
- [x] ServiceBootstrap 依赖清单和 ready 判定规则
- [x] 配置默认安全边界和禁止从环境变量读取的业务配置清单
- [x] 初始化配置校验错误码

完成标准：

- `setup-config-validations` 可以按 schema 校验完整初始化配置。
- `setup-initialization` 能一次性写入首个可运行的 `active_config v1`。
- 本地 P0 流程依赖外部 embedding、rerank、LLM 服务或企业模型代理，不再依赖内置模型模拟服务。
- Secret value 不进入 active config、API 响应、普通日志或审计摘要。

### 2.3 生成 OpenAPI 契约

状态：待评审

依据文档：

- `docs/modules/15-前端与管理后台API设计实现文档.md`
- `docs/modules/00-公共实现约束.md`

产物：

- [x] `docs/contracts/openapi.yaml`
- [x] API 分组标签
- [x] request schema
- [x] response schema
- [x] error schema
- [x] 分页 schema
- [x] SSE 事件 schema
- [x] multipart/form-data 上传 schema
- [x] 初始化配置和 active config 摘要 schema
- [x] 鉴权声明
- [x] HTTP status 规范

必须覆盖：

- [x] 初始化 API
- [x] 会话与当前用户 API
- [x] 查询问答 API
- [x] 普通知识库浏览 API
- [x] 文档导入与任务 API
- [x] 管理后台知识库 API
- [x] 管理后台文件夹 API
- [x] 管理后台文档 API
- [x] 管理后台用户 API
- [x] 管理后台组织 API
- [x] 管理后台角色与权限 API
- [x] 管理后台配置 API（P0 仅配置查看、校验和发布；不含完整审批/回滚工作流）
- [x] 管理后台审计与可观测 API（P0 覆盖审计日志、查询日志和模型调用日志；指标汇总和告警可标 P1）
- [x] healthcheck API
- [x] 外部模型 provider adapter API

完成标准：

- 后端实现、前端联调和 API contract 测试都以 `docs/contracts/openapi.yaml` 为准。
- 每个接口都有明确 request、response、错误码和权限要求。
- 不再存在旧 RPC 风格路径。

### 2.4 落地数据库 Schema

状态：待评审

依据文档：

- `docs/modules/14-核心数据模型设计实现文档.md`
- `docs/modules/01-初始化服务设计实现文档.md`
- `docs/modules/02-认证服务设计实现文档.md`
- `docs/modules/03-组织服务设计实现文档.md`
- `docs/modules/04-权限服务设计实现文档.md`
- `docs/modules/05-配置服务设计实现文档.md`
- `docs/modules/06-导入服务与工作进程设计实现文档.md`
- `docs/modules/07-索引服务设计实现文档.md`
- `docs/modules/11-审计与可观测性设计实现文档.md`

产物：

- [x] `docs/contracts/database-schema.md`
- [x] 首批 Alembic migration 草案
- [x] 枚举定义清单
- [x] 唯一约束清单
- [x] 外键约束清单
- [x] 查询索引清单
- [x] 软删除和 access block 约束说明

必须明确：

- [x] `system_state`
- [x] `setup_tokens`
- [x] `users`
- [x] `user_credentials` 或并入 `users` 的凭证字段
- [x] `jwt_tokens`
- [x] `enterprises`
- [x] `departments`
- [x] `user_department_memberships`
- [x] `roles`
- [x] `role_bindings`
- [x] `resource_policies`
- [x] `permission_snapshots`
- [x] `system_configs`
- [x] `config_versions`
- [x] `secrets`
- [x] `knowledge_bases`
- [x] `folders`
- [x] `documents`
- [x] `document_versions`
- [x] `chunks`
- [x] `index_versions`
- [x] `chunk_index_refs`
- [x] `keyword_index_entries`
- [x] `access_blocks`
- [x] `import_jobs`
- [x] `audit_logs`
- [x] `query_logs`
- [x] `model_call_logs`
- [x] `query_cache_entries`

完成标准：

- 所有租户业务表都有 `enterprise_id`、主键、时间戳和必要状态字段；全局表必须明确 global scope 和访问边界。
- 所有跨租户查询都有可用索引。
- 文档、chunk、索引、权限快照之间可对账。
- draft index、deleted document、blocked resource 无法被查询命中。

### 2.5 制定接口权限矩阵

状态：待评审

产物：

- [x] `docs/contracts/权限矩阵.md`

矩阵字段：

| 字段 | 说明 |
| --- | --- |
| endpoint | API 路径 |
| method | HTTP 方法 |
| user_type | anonymous、setup、user、admin、service |
| required_auth | none、access_token、refresh_token、setup_token、service_token |
| required_scope | 需要的 scope |
| resource_check | 是否需要资源范围校验 |
| risk_level | low、medium、high、critical |
| audit_required | 是否必须审计 |
| confirmation_required | no、yes、conditional |
| confirmation_condition | 二次确认条件 |
| compensation_strategy | 失败、误操作或异步任务失败时的补偿/阻断策略 |

额外必须明确：

- [x] 初始化生命周期门禁：未初始化时仅开放 setup 和 healthcheck，初始化后关闭 setup 写接口。
- [x] 普通文档列表必须在知识库可见基础上逐文档执行 `department` / `enterprise` 可见性过滤。
- [x] P0 资源权限策略仅支持 `visibility` 和 `owner_department_id`，拒绝指定用户、项目组、自定义组织 ACL。
- [x] OpenAPI `x-required-scope`、token 类型、审计声明和权限矩阵一致。

高风险操作必须覆盖：

- [x] 初始化提交。
- [x] setup JWT 签发和轮换。
- [x] 系统管理员创建、禁用、删除。
- [x] 高风险角色绑定。
- [x] 文档可见性从 department 改为 enterprise。
- [x] 权限收紧。
- [x] 越界权限策略拒绝。
- [x] 配置发布（P0 仅 active config 发布，不含完整审批/回滚工作流）。
- [x] 文档删除。
- [x] 知识库删除。
- [x] 索引重建和发布。

完成标准：

- 每个写接口都有 scope、资源范围和审计要求。
- 初始化生命周期门禁不依赖前端或业务 handler 自行判断。
- 前端菜单权限与后端真实权限校验可以对应。
- 管理员接口不能只依赖前端隐藏菜单。

### 2.6 固化核心状态机

状态：待评审

产物：

- [x] `docs/contracts/状态机设计.md`

必须覆盖：

- [x] setup 初始化状态机。
- [x] setup JWT 状态机。
- [x] access/refresh token 状态机。
- [x] 组织、账号与角色绑定状态机。
- [x] import_job 状态机。
- [x] knowledge_base 与 folder 状态机。
- [x] document lifecycle 状态机。
- [x] document_version 状态机。
- [x] index_version 状态机。
- [x] chunk_index_ref visibility 状态机。
- [x] config_version 状态机（P0 仅覆盖 draft、active、archived 等基础生命周期，不含完整审批/回滚流程）。
- [x] access_block 生命周期。

每个状态机必须明确：

- [x] 状态枚举。
- [x] 允许迁移。
- [x] 禁止迁移。
- [x] 幂等行为。
- [x] 失败重试。
- [x] 取消行为。
- [x] 补偿或阻断策略（P0 不要求完整配置回滚）。
- [x] 审计事件。

完成标准：

- Worker、API 和管理后台不能绕过状态机直接改状态。
- `PATCH status` 类接口有明确的允许状态迁移。
- 任务重试、取消、恢复不会破坏数据一致性。

### 2.7 固化 P0 审计事件字典与脱敏规则

状态：待评审

依据文档：

- `docs/MVP范围说明.md`
- `docs/modules/00-公共实现约束.md`
- `docs/modules/11-审计与可观测性设计实现文档.md`

产物：

- [x] `docs/contracts/审计事件字典.md`

必须覆盖：

- [x] 初始化 validate / initialize。
- [x] setup JWT 签发、轮换、使用和失效。
- [x] 登录成功、登录失败、refresh token rotation。
- [x] 用户创建、禁用、软删除、重置密码、解锁。
- [x] 角色绑定、替换和撤销。
- [x] 文档导入、导入失败、导入重试、导入取消。
- [x] 文档可见性扩大和权限收紧。
- [x] 文档删除、知识库删除、access block 创建。
- [x] 查询、候选阻断、citation 校验失败、降级响应。
- [x] 模型调用、外部模型 provider 失败和降级。

每个事件必须明确：

- [x] event_name。
- [x] actor_type / actor_id。
- [x] resource_type / resource_id。
- [x] result。
- [x] risk_level。
- [x] summary_json 字段。
- [x] 禁止记录字段：密码、token、secret value、完整 prompt、未脱敏敏感原文。

完成标准：

- 所有 P0 高风险写接口都有审计事件定义。
- 查询日志、模型调用日志和审计日志的字段边界清楚。
- 审计摘要能支持 P0 追踪和排障，但不会泄露敏感信息。

### 2.8 制定 P0 测试计划

状态：待评审

依据文档：

- `docs/MVP范围说明.md`
- `docs/modules/00-公共实现约束.md`
- `docs/contracts/openapi.yaml`
- `docs/contracts/database-schema.md`
- `docs/contracts/权限矩阵.md`
- `docs/contracts/状态机设计.md`
- `docs/contracts/审计事件字典.md`
- `docs/contracts/config-schema.md`
- `docs/contracts/config.schema.json`

产物：

- [x] `docs/testing/测试计划.md`

测试分层：

- [x] policy 单元测试。
- [x] service 用例测试。
- [x] repository 数据库测试。
- [x] adapter 集成测试。
- [x] API contract 测试。
- [x] CLI contract 测试。
- [x] 端到端测试。
- [x] 权限绕过测试。
- [x] 超时、重试、熔断和降级测试。

P0 必测链路：

- [x] 首次初始化成功。
- [x] 初始化生命周期门禁：未初始化时普通业务、管理后台和服务间 API 返回 `SETUP_REQUIRED`，初始化后 setup 写接口返回 `SETUP_CLOSED`。
- [x] setup JWT 每次签发新 setup JWT，旧 setup JWT 失效。
- [x] setup JWT 不能访问普通业务 API、管理员 API 或服务间 API。
- [x] 本地账号登录成功和失败锁定。
- [x] refresh token rotation。
- [x] 管理员创建用户、部门和角色绑定。
- [x] Config Service 读取、校验、发布和 active version 切换。
- [x] 当前用户信息查询、改密和登出。
- [x] 管理员禁用、软删除用户，重置密码和解锁用户。
- [x] 知识库、文件夹、文档元数据和文档版本创建。
- [x] 文档上传创建导入任务。
- [x] URL 或批量元数据创建导入任务。
- [x] Worker 阶段化处理导入任务。
- [x] Worker 取消、重试、非法迁移和 crash 后 `locked_until` 接管。
- [x] draft index 查询不可见。
- [x] active index 查询可见。
- [x] 发布前校验 chunk 数量、向量维度、payload hash、权限快照和 draft 不可见。
- [x] 权限过滤下推到向量检索和关键词检索。
- [x] 越界权限策略拒绝。
- [x] 部门用户 Bob 查询销售部门文档被阻断。
- [x] 文档可见性从 `department` 扩大到 `enterprise` 后新权限用户可见。
- [x] 权限收紧后旧用户无法通过旧缓存、旧索引或旧 permission_version 查询。
- [x] 文档删除后无法通过缓存或旧索引命中，物理删除失败时 access block 保持 active。
- [x] 查询返回 citation。
- [x] strict citation 模式不会输出未经引用校验的最终答案。
- [x] 查询缓存不会跨用户、跨租户、跨权限过滤条件、跨权限版本或跨索引版本复用。
- [x] Embedding、Rerank 或 LLM provider 不可用时按策略降级。
- [x] 审计和脱敏覆盖初始化、登录、用户管理、角色绑定、权限变更、导入、删除、查询降级和模型调用。
- [x] 审计日志、查询日志和模型调用日志查询与筛选。
- [x] OpenAPI contract 回归覆盖 request、response、error schema、鉴权声明、分页、SSE 和 multipart 上传。

必须明确：

- [x] P0 最小测试环境和外部模型 provider 依赖。
- [x] 基础测试数据：管理员、Alice、Bob、销售部门、销售知识库和销售政策文档。
- [x] P0 必测场景矩阵和关键断言。
- [x] 场景矩阵包含 gate、automation_required 和 manual_allowed 字段。
- [x] 状态机测试要求。
- [x] API contract 测试要求。
- [x] 权限绕过测试要求。
- [x] 降级和故障注入测试要求。
- [x] 自动化门禁：fast、db、contract、cli-contract、integration、e2e、security。
- [x] 手工验收边界。
- [x] 缺陷回归要求。
- [x] 失败定位要求。

完成标准：

- 每条 P0 链路都有自动化测试或明确手工验收步骤。
- 高风险权限、删除阻断、token 越权、缓存串权和敏感信息泄露场景必须自动化。
- 测试数据可重复构造和清理。
- 本地和 CI 均可使用外部 embedding、rerank、LLM 服务或企业模型代理跑通初始化、认证、权限、导入、索引、查询、删除阻断和外部模型 provider 降级。
- 测试计划能映射到 `docs/contracts/openapi.yaml`、`docs/contracts/database-schema.md`、`docs/contracts/权限矩阵.md`、`docs/contracts/状态机设计.md` 和 `docs/contracts/审计事件字典.md`。

### 2.9 准备本地开发环境

状态：待评审

产物：

- [x] `docs/development/本地开发环境.md`
- [x] `.env.example`
- [x] `docker-compose.yml` 草案
- [x] `Makefile` 或等价任务入口草案
- [x] `docs/examples/setup-initialization.local.p0.json`

最小依赖：

- [x] PostgreSQL
- [x] Redis
- [x] MinIO
- [x] Qdrant
- [x] TEI Embedding provider
- [x] TEI Rerank provider
- [x] OpenAI-compatible LLM provider

必须明确：

- [x] 启动配置只允许数据库连接和进程参数。
- [x] 业务配置通过初始化接口写入 active config。
- [x] 未初始化时只开放 setup-state、setup-config-validations、setup-initialization。
- [x] ready 检查必须覆盖数据库、初始化状态、active config、ServiceBootstrap 状态和迁移版本。
- [x] API、Worker 和 setup JWT CLI 的本地启动/调用入口契约。
- [x] 完整本地初始化 payload 覆盖 `active_config v1` 必填分组。
- [x] Qdrant ready 语义：初始化后无文档也可 ready，collection 不兼容时 fail closed。

完成标准：

- 新开发者可以按文档启动最小环境。
- 可以完成一次初始化、登录、导入测试文档、查询带 citation 的模型答案。
- 需要可控外部模型服务或企业模型代理跑通 P0 自动化测试。

## 3. P1 任务

### 3.1 管理后台交互契约

状态：待评审

产物：

- [x] `docs/admin/管理后台交互契约.md`

完成项：

- [x] 页面路由清单。
- [x] 菜单与 scope 映射。
- [x] 表格筛选字段。
- [x] 表单字段和校验规则。
- [x] 高风险操作确认文案和二次确认机制。
- [x] 审计详情展示脱敏规则。

### 3.2 配置变更与热更新策略

状态：待评审

产物：

- [x] `docs/operations/配置变更与热更新策略.md`

完成项：

- [x] 配置版本 diff 规则。
- [x] 高风险配置列表和审批规则。
- [x] 配置热更新策略。
- [x] 配置热更新失败策略。
- [x] 配置回滚策略（阶段 2）。
- [x] department、knowledge_base、model_pool 等作用域覆盖规则。

### 3.3 审计查询与展示扩展

状态：待评审

产物：

- [x] `docs/observability/审计查询与展示扩展.md`

完成项：

- [x] 基于 P0 审计事件字典补充 before/after 摘要。
- [x] 审计详情查询字段。
- [x] 审计导出边界。
- [x] retention 策略。
- [x] 脱敏策略。

### 3.4 模型与检索评测基线

状态：待评审

产物：

- [x] `docs/evaluation/模型与检索评测基线.md`
- [x] `docs/examples/eval-dataset.local.p1.jsonl`

完成项：

- [x] 最小离线评测集。
- [x] 检索 recall@k 指标。
- [x] citation 命中率。
- [x] 无答案拒答率。
- [x] 延迟和成本指标。

### 3.5 部署与发布检查清单

状态：待评审

产物：

- [x] `docs/operations/部署与发布检查清单.md`

完成项：

- [x] migration 执行策略。
- [x] 初始化和恢复初始化流程。
- [x] 备份与恢复演练。
- [x] 配置恢复/回滚演练（阶段 2）。
- [x] 索引重建演练。
- [x] 权限收紧演练。
- [x] 日志、指标、Trace 验收。

## 4. 建议编码顺序

1. 工程骨架：FastAPI、错误结构、request_id、trace_id、日志中间件。
2. 数据库基础：SQLAlchemy、Alembic、事务管理、repository 基类。
3. 配置契约：初始化配置 schema、`active_config v1` schema、Secret 引用、外部模型 provider 配置。
4. Setup Service：初始化状态、setup JWT、初始化配置校验、active config v1。
5. Config Service：active config 读取、缓存、版本和发布；P0 不实现完整审批/回滚。
6. Auth Service：用户、密码、登录、refresh token rotation、JWT 状态。
7. Org Service：企业、部门、成员关系、组织版本。
8. Permission Service：角色、绑定、权限上下文、资源权限策略。
9. KnowledgeBase / Document 元数据。
10. Import Service 与 Worker：任务创建、领取、解析、切块、embedding。
11. Indexing Service：draft 写入、校验、active 发布、兼容性校验和删除阻断。
12. Query Service：查询入口、权限过滤、召回、rerank、上下文、答案和 citation。
13. Audit / Observability：P0 审计事件、查询日志、模型调用日志、基础指标。
14. P0 管理后台 API 按 OpenAPI 补齐。

## 5. 风险清单

| 风险 | 影响 | 编码前动作 |
| --- | --- | --- |
| API 只有路径，没有 schema | 前后端联调反复返工 | 先完成 OpenAPI |
| 数据模型缺少约束和索引 | 查询慢、状态错乱、权限绕过 | 先完成 schema 和 migration 草案 |
| 权限矩阵不明确 | 管理后台越权风险 | endpoint 级权限矩阵先行 |
| 状态机没有固化 | Worker 重试、取消、补偿行为不一致 | 状态机设计先行 |
| 外部模型依赖不稳定 | 开发和测试受下载、延迟、成本和供应商限流影响 | 固定 provider profile，设置超时、降级、调用日志和独立健康检查 |
| 配置和 secret 处理不清晰 | 部署后难以恢复和审计 | 初始化配置 schema、active_config v1 schema 和 Secret Store 策略先行 |
| 缓存 key 设计不完整 | 跨用户、跨权限串权 | 查询缓存测试和 key 规范先行 |

## 6. 当前文档待清理项

- [ ] 检查所有模块文档中的 API 片段，确保与 `15-前端与管理后台API设计实现文档.md` 完全一致。
- [ ] 检查所有文档中的 setup token、system token 表述，统一为 setup JWT；数据库字段可保留 jti/token 状态语义。
- [ ] 检查配置审批、配置回滚、连接器同步、真实 vLLM/TEI 部署、离线评测平台等内容，明确标注为 P1/P2，不得作为 P0 编码门禁。
- [ ] 统一数据库表名：`jwt_tokens`、`user_department_memberships`、`system_configs` 等名称应和最终 schema 保持一致。

### 6.1 需要人工审核的收口项

以下项目暂不自动处理，由人工审核后统一决定是否修改：

- [ ] OpenAPI 与权限矩阵的 healthcheck 契约差异：`/health/live` 和 `/health/ready` 在权限矩阵中为 anonymous / none，OpenAPI 当前未声明 `x-token-type: anonymous`。`docs/testing/测试计划.md` 已明确 contract 测试的两种收口规则；仍需人工决定是否补 OpenAPI 声明。
- [ ] 历史文档中的表述清理：`setup token`、`system token`、配置审批、配置回滚、真实模型、离线评测等旧表述仍分布在总设计文档和部分模块文档中。需人工判断哪些属于历史背景保留，哪些需要标注为 P1/P2 或改为 setup JWT。
- [ ] 当前工作区变更确认：`docs/contracts/database-schema.md`、`docs/contracts/openapi.yaml`、`docs/contracts/权限矩阵.md`、`docs/planning/正式编码前任务追踪.md`、`docs/contracts/状态机设计.md` 有未提交修改，`docs/contracts/审计事件字典.md`、`docs/testing/测试计划.md` 为未跟踪文件。进入 2.9 前需人工审核、拆分或提交这些文档变更。

## 7. 每日任务追踪模板

| 日期 | 任务 | 状态 | 阻塞 | 下一步 |
| --- | --- | --- | --- | --- |
|  |  | 未开始 |  |  |

状态枚举：

- 未开始
- 进行中
- 待评审
- 已完成
- 阻塞
