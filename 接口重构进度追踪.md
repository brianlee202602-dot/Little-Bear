# Little Bear 接口重构进度追踪

更新时间：2026-05-30

本文用于追踪前后端交互接口的收紧重构。目标不是简单减少字段，而是把分页、筛选、权限过滤、ID 映射和详情加载边界重新收敛到后端，避免前端拉取过量数据后自行过滤或裁剪。

## 1. 重构标准

后端接口按以下标准重构：

- 所有列表接口必须支持后端分页：`page`、`page_size`、`total`。
- 所有业务查询条件必须传给后端处理：`keyword`、`status`、`department_id`、`kb_id`、`date_range` 等。
- 所有权限过滤只能在后端完成，前端隐藏菜单或隐藏字段不构成安全边界。
- 列表接口不得返回详情字段、权限策略字段、内部 hash、trace、完整配置、全文 chunk。
- 详情接口必须重新做权限检查，不能因为列表可见就默认详情可见。
- ID 显示应由后端转换为可读名称，或通过最小化 lookup 接口转换。
- 管理后台可以看到更多，但不应默认看到所有内部字段。

## 2. 状态标记

- `[x]` 已符合当前标准。
- `[~]` 部分符合，需要局部收紧。
- `[ ]` 不符合，需要重构。
- `[!]` 高风险，优先处理。
- `[L]` 低风险或可豁免，但需要明确记录原因。

## 3. 审计结论

当前后端大部分业务列表已经具备 `page/page_size/total`，但仍有三类问题：

1. 列表接口复用详情模型，返回了前端列表不需要的内部字段。
2. 前端为了展示名称或做局部选择，拉取完整用户、知识库、角色、chunk 等对象。
3. 普通用户侧接口暴露了管理或策略语义，例如 scope、policy version、owner department、trace id 等。

优先级最高的接口是：

- `GET /internal/v1/admin/config-versions`
- `GET /internal/v1/admin/query-logs`
- `GET /internal/v1/admin/model-call-logs`
- `GET /internal/v1/users/me`
- `GET /internal/v1/knowledge-bases`
- `GET /internal/v1/knowledge-bases/{kb_id}/documents`
- `GET /internal/v1/documents/{doc_id}/chunks`
- `GET /internal/v1/admin/documents/{doc_id}/chunks`
- 查询诊断页依赖的 `GET /internal/v1/admin/users?page_size=200`
- 查询诊断页依赖的 `GET /internal/v1/admin/knowledge-bases?page_size=200`

## 4. 前后端交互接口清单

### 4.1 健康检查、初始化与认证

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /health/live` | Web 健康检查 | 非业务展示接口，无过量字段 | 保持 |
| [x] | `GET /health/ready` | 运维 / 启动检查 | 非业务展示接口 | 保持 |
| [x] | `GET /internal/v1/setup-state` | 管理后台初始化页 | 已返回有限初始化状态，不返回 token/secret 明文 | 保持 |
| [x] | `POST /internal/v1/setup-config-validations` | 初始化配置校验 | 写入前校验接口，不属于列表展示 | 保持 |
| [x] | `PUT /internal/v1/setup-initialization` | 初始化提交 | 写接口，已有确认头和生命周期门禁 | 保持 |
| [x] | `POST /internal/v1/sessions` | 管理后台 / Web 登录 | 返回 token 所需字段 | 保持 |
| [x] | `POST /internal/v1/token-refreshes` | 管理后台 / Web 刷新会话 | 返回 token 所需字段 | 保持 |
| [x] | `DELETE /internal/v1/sessions/current` | 管理后台 / Web 登出 | 无展示数据 | 保持 |
| [x] | `GET /internal/v1/users/me` | Web 当前用户 | 已收紧为最小身份响应，只返回 `id/username/name/status` | 管理后台改用 `GET /internal/v1/admin/users/me/capabilities` |
| [x] | `GET /internal/v1/admin/users/me/capabilities` | 管理后台当前用户能力 | 管理后台专用 capability 接口，返回菜单和操作判断所需的 roles/scopes/departments；普通员工无管理能力时返回 403 | 保持后端管理入口校验 |
| [x] | `PUT /internal/v1/users/me/password` | 管理后台修改本人密码 | 写接口，无过量展示 | 保持 |

### 4.2 普通用户知识库与查询端

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/knowledge-bases` | Web 查询页知识库选择 | 已收紧为普通用户知识库摘要，只返回 `id/name/status`；Web 端改为分页加载，不再固定一次拉取 100 条 | 保持后端权限过滤和分页 |
| [x] | `GET /internal/v1/knowledge-bases/{kb_id}/documents` | Web 文档列表能力 | 已拆为普通用户文档列表摘要，只返回 `id/title/lifecycle_status/index_status/updated_at/can_view/can_cite`；Web client 改为分页参数，不再固定一次拉取 100 条 | 保持后端权限过滤和分页 |
| [x] | `GET /internal/v1/documents/{doc_id}` | 普通用户文档详情 | 已收紧为普通用户最小文档详情，只返回 `id/title/lifecycle_status/index_status/updated_at/can_view/can_cite` | Web client 已移除未使用详情封装；后端详情仍保留权限校验 |
| [x] | `GET /internal/v1/documents/{doc_id}/versions` | Web client 已封装 | 已增加后端分页 `page/page_size/total`，普通用户文档版本不再一次返回全部版本 | 保持详情权限校验和分页边界 |
| [x] | `GET /internal/v1/documents/{doc_id}/chunks` | Web 来源窗口 fallback | 已增加后端分页 `page/page_size/total`、`keyword/status` 查询和 `ordinal`；Web fallback 改为第一页加载，可继续加载更多 | 普通用户仍优先使用 citation source，chunk fallback 不再全量拉取 |
| [x] | `GET /internal/v1/documents/{doc_id}/preview` | Web client 原有封装未使用 | Web client 已移除未使用预览封装；后端 preview 标记 deprecated，当前查询页继续使用 citation source 与 chunk 分页 | 后续若需要文档阅读器，再重建分页预览接口 |
| [x] | `GET /internal/v1/documents/{doc_id}/sources/{source_id}` | Web citation 精确来源 | source 级读取，权限校验后只返回命中来源 | 保持，必要时增加邻近片段窗口参数 |
| [~] | `GET /internal/v1/query-conversations` | Web 左侧会话列表 | 后端有分页，前端固定拉第一页 50 条；无滚动翻页 | 前端增加分页/滚动加载 |
| [x] | `GET /internal/v1/query-conversations/{conversation_id}` | Web 会话详情 | 已改为消息分页响应，默认只返回最近 50 条并返回 `messages_pagination`；Web 端可继续加载更早消息 | 保持详情权限校验和消息分页边界 |
| [x] | `POST /internal/v1/queries` | Web 非流式查询 | 已移除普通用户响应中的 `request_id/trace_id`，改为不可逆短排障码 `debug_id`；诊断链路仍在后台日志保留内部 ID | 保持内部审计字段不透出到普通用户响应 |
| [x] | `POST /internal/v1/query-streams` | Web SSE 查询 | 已将 metadata/done/error 事件中的内部追踪字段替换为 `debug_id` | 与非流式响应保持同一脱敏模型 |

### 4.3 管理后台配置管理

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/admin/configs` | client 已封装，当前页面主要使用版本接口 | 已拆为配置项摘要列表，列表不再返回完整 `value_json` | 完整配置项只通过 `GET /internal/v1/admin/configs/{key}` 按需读取 |
| [x] | `GET /internal/v1/admin/configs/{key}` | client 已封装 | 详情接口，按 key 获取完整配置项合理 | 保持权限校验 |
| [x] | `PUT /internal/v1/admin/configs/{key}` | client 已封装 | 写接口，已有确认语义 | 保持 |
| [x] | `GET /internal/v1/admin/config-versions` | 管理后台配置版本列表 | 已改为后端分页；列表只返回版本摘要，不返回完整 `config` | 已完成，完整配置只通过详情接口按需读取 |
| [x] | `GET /internal/v1/admin/config-versions/{version}` | 管理后台查看/编辑配置版本 | 详情接口返回完整 config 合理 | 保持，继续做权限校验 |
| [x] | `POST /internal/v1/admin/config-versions` | 管理后台新建配置版本 | 写接口，已有确认头 | 保持 |
| [x] | `PUT /internal/v1/admin/config-versions/{version}` | 管理后台编辑配置版本 | 写接口，已有确认头 | 保持 |
| [x] | `PATCH /internal/v1/admin/config-versions/{version}` | 管理后台激活配置版本 | 写接口，已有确认头 | 保持 |
| [x] | `DELETE /internal/v1/admin/config-versions/{version}` | 管理后台归档配置版本 | 写接口，已有确认头 | 保持 |
| [x] | `POST /internal/v1/admin/config-validations` | 管理后台配置校验 | 校验接口，不属于列表展示 | 保持 |

### 4.4 管理后台用户、部门与角色

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/admin/users` | 用户管理列表；诊断 lookup | 已拆为列表摘要，只返回 `id/username/name/status/department_names/role_names`；编辑、部门、角色、密码、删除弹窗打开时再调用用户详情接口 | 后续若多页面需要用户选择器，再补最小化 user option/lookup 接口 |
| [x] | `GET /internal/v1/admin/users/{user_id}` | 用户详情 | 详情接口返回用户完整管理信息合理 | 保持，详情权限单独校验 |
| [x] | `POST /internal/v1/admin/users` | 创建用户 | 写接口 | 保持 |
| [x] | `PATCH /internal/v1/admin/users/{user_id}` | 修改用户 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/users/{user_id}` | 删除用户 | 写接口，有确认 | 保持 |
| [x] | `PUT /internal/v1/admin/users/{user_id}/password` | 重置密码 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/users/{user_id}/lock` | 解锁用户 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/departments` | 部门管理列表 | 已拆为部门列表摘要，只返回 `id/name/status/is_default`；列表默认不返回 `code` | 详情接口仍按权限返回完整部门信息 |
| [x] | `GET /internal/v1/admin/department-options` | 部门 selector | 新增最小化部门选择器接口，只返回 `id/name/status/is_default`，前端用户、角色、知识库、文档权限选择改用该接口 | 已将默认 option 拉取量降为 20，并在相关弹窗接入 keyword 搜索 |
| [x] | `GET /internal/v1/admin/departments/{department_id}` | 部门详情 | 详情接口合理 | 保持 |
| [x] | `POST /internal/v1/admin/departments` | 创建部门 | 写接口 | 保持 |
| [x] | `PATCH /internal/v1/admin/departments/{department_id}` | 修改部门 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/departments/{department_id}` | 删除部门 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/users/{user_id}/departments` | 用户部门绑定弹窗 | 已增加后端分页 `page/page_size/total`；管理后台用户部门弹窗接入分页控件，保存部门时仍以用户详情中的完整部门集合初始化表单，避免分页隐藏项被误删 | 保持详情权限校验和分页边界 |
| [x] | `PUT /internal/v1/admin/users/{user_id}/departments` | 替换用户部门 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/roles` | 角色管理列表 | 已改为后端分页角色摘要，只返回 `id/code/name/scope_type/is_builtin/status`，不再返回完整 `scopes` | 角色详情仍通过 `GET /internal/v1/admin/roles/{role_id}` 按需读取 |
| [x] | `GET /internal/v1/admin/assignable-role-options` | 用户创建初始角色、角色绑定 selector | 新增可分配角色 option 接口，只返回 `id/code/name/scope_type/status/risk_level`；前端用 `risk_level` 做高风险确认，不再读取 `scopes` | 已将默认 option 拉取量降为 20，并在用户创建/角色绑定弹窗接入 keyword 搜索 |
| [x] | `GET /internal/v1/admin/roles/{role_id}` | 角色详情 | 详情接口返回 scopes 合理 | 保持 |
| [x] | `GET /internal/v1/admin/users/{user_id}/role-bindings` | 用户角色绑定弹窗 | 已增加后端分页 `page/page_size/total`；管理后台当前角色列表接入分页控件 | 保持详情权限校验和分页边界 |
| [x] | `POST /internal/v1/admin/users/{user_id}/role-bindings` | 添加角色绑定 | 写接口 | 保持 |
| [x] | `PUT /internal/v1/admin/users/{user_id}/role-bindings` | 替换角色绑定 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/users/{user_id}/role-bindings/{binding_id}` | 删除角色绑定 | 写接口 | 保持 |

### 4.5 管理后台知识库、文档与权限

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/admin/knowledge-bases` | 知识库管理列表 | 已拆为知识库列表摘要，只返回列表展示和操作入口需要的 `id/name/status/owner_department_name/kb_visibility/default_document_visibility` 等字段，不再返回 `access_rules/config_scope_id/policy_version` | 完整策略和配置作用域只通过详情接口读取 |
| [x] | `GET /internal/v1/admin/knowledge-base-options` | 角色作用域、导入任务筛选等 selector | 新增最小化知识库选择器接口，只返回 `id/name/status`，前端 selector 不再复用完整知识库列表 DTO | 已将默认 option 拉取量降为 20，并在角色作用域和任务筛选接入 keyword 搜索 |
| [x] | `GET /internal/v1/admin/knowledge-bases/{kb_id}` | 知识库详情/编辑 | 详情接口返回策略字段合理 | 保持详情权限校验 |
| [x] | `POST /internal/v1/admin/knowledge-bases` | 新建知识库 | 写接口 | 保持 |
| [x] | `PATCH /internal/v1/admin/knowledge-bases/{kb_id}` | 修改知识库 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/knowledge-bases/{kb_id}` | 删除知识库 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/knowledge-bases/{kb_id}/folders` | 文件夹管理 | 后端已有分页，管理后台文件夹列表已接入独立分页状态和分页控件；文件夹下拉继续使用最小化 folder option DTO | 文件夹 option 默认拉取量降为 20，并在上传、创建文件夹、编辑文件夹弹窗接入 keyword 搜索 |
| [x] | `GET /internal/v1/admin/folders/{folder_id}` | 文件夹详情 | 详情接口合理 | 保持 |
| [x] | `POST /internal/v1/admin/knowledge-bases/{kb_id}/folders` | 新建文件夹 | 写接口 | 保持 |
| [x] | `PATCH /internal/v1/admin/folders/{folder_id}` | 修改文件夹 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/folders/{folder_id}` | 删除文件夹 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/knowledge-bases/{kb_id}/documents` | 文档管理列表 | 已拆 `AdminDocumentListItem` 摘要响应；列表只返回标题、文件夹名、状态、可见性部门名、当前版本号和是否可重建；管理后台文档列表接入 `page/page_size/total` 分页 | 详情字段继续通过 `GET /internal/v1/admin/documents/{doc_id}` 按需读取 |
| [x] | `GET /internal/v1/admin/documents/{doc_id}` | 文档详情/编辑 | 详情接口合理 | 保持 |
| [x] | `GET /internal/v1/admin/documents/{doc_id}/versions` | 文档版本弹窗 | 后端已支持分页并返回 `pagination`；管理后台文档版本区域已接入分页控件 | 保持详情权限校验和分页边界 |
| [x] | `GET /internal/v1/admin/documents/{doc_id}/chunks` | Chunk 预览 | 已增加后端分页 `page/page_size/total`、`keyword/status` 查询和 `ordinal`；管理后台文档详情弹窗改为分页展示 | 后续可继续增加 keyword 输入和 ordinal window 定位 |
| [x] | `GET /internal/v1/admin/documents/{doc_id}/preview` | 管理端文档预览 | 已增加 `page/page_size/total`，不再一次返回全部全文 chunk；当前管理后台已不在弹窗初始化时调用该接口 | 后续可视需要废弃或改成片段窗口读取 |
| [x] | `GET /internal/v1/admin/documents/{doc_id}/index-versions` | 文档索引版本 | 已增加后端分页 `page/page_size/total`；管理后台索引版本区域已接入独立分页控件 | 保持详情权限校验和分页边界 |
| [x] | `POST /internal/v1/admin/documents/{doc_id}/index-jobs` | 单文档重建索引 | 写接口，有确认 | 保持 |
| [x] | `PATCH /internal/v1/admin/documents/{doc_id}` | 修改文档元数据 | 写接口 | 保持 |
| [x] | `DELETE /internal/v1/admin/documents/{doc_id}` | 删除文档 | 写接口 | 保持 |
| [x] | `PUT /internal/v1/knowledge-bases/{kb_id}/permissions` | 知识库权限修改 | 写接口，返回修改后的权限策略合理 | 保持 |
| [x] | `PUT /internal/v1/documents/{doc_id}/permissions` | 文档权限修改 | 写接口，返回修改后的权限策略合理 | 保持 |
| [x] | `POST /internal/v1/knowledge-bases/{kb_id}/documents` | 文件上传 | 写接口，返回导入任务 | 保持 |

### 4.6 导入任务、索引运维与任务重试

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/import-jobs/{job_id}` | client 已封装 | 详情接口合理 | 保持 |
| [x] | `PATCH /internal/v1/import-jobs/{job_id}` | 取消任务 | 写接口 | 保持 |
| [x] | `GET /internal/v1/admin/import-jobs` | 导入/索引任务列表 | 已改为列表摘要，返回 `document_count`，不再返回 `document_ids`；管理后台任务列表和失败索引任务块均接入后端分页 | 详情接口 `GET /internal/v1/admin/import-jobs/{job_id}` 继续返回具体 `document_ids` |
| [x] | `GET /internal/v1/admin/import-jobs/{job_id}` | 任务详情 | 详情接口合理 | 保持 |
| [x] | `POST /internal/v1/admin/index-jobs` | 知识库/文档批量重建 | 写接口，有确认 | 保持 |
| [x] | `POST /internal/v1/admin/index-jobs/retries` | 失败任务重试 | 写接口，有确认 | 保持 |
| [x] | `POST /internal/v1/admin/index-versions/cleanup-jobs` | 索引版本清理 | 写接口，有确认 | 保持 |
| [x] | `GET /internal/v1/admin/index-health` | 索引运维页 | 已增加后端分页 `page/page_size/total`；管理后台索引运维 collection 列表已接入分页控件，不再一次性探测全部 collection | 保持后端权限过滤和分页 |
| [x] | `GET /internal/v1/admin/index-collections/{collection}/snapshots` | Qdrant 快照列表 | 已增加后端分页 `page/page_size/total`；管理后台 Qdrant 快照列表已接入分页控件 | 保持后端权限过滤和分页 |
| [x] | `POST /internal/v1/admin/index-collections/{collection}/snapshots` | 创建快照 | 写接口，有确认 | 保持 |
| [x] | `PUT /internal/v1/admin/index-collections/{collection}/snapshot-recoveries` | 恢复快照 | 写接口，有确认 | 保持 |
| [x] | `POST /internal/v1/admin/index-collections/{collection}/rebuild-jobs` | collection 重建索引 | 写接口，有确认 | 保持 |

### 4.7 审计、查询诊断与模型调用日志

| 状态 | 接口 | 前端调用 | 当前问题 | 重构动作 |
| --- | --- | --- | --- | --- |
| [x] | `GET /internal/v1/admin/audit-logs` | 配置审计/审计列表 | 已拆为列表摘要和详情；列表不再返回 request_id、trace_id、summary_json、index_version_hash | 详情字段只通过 `GET /internal/v1/admin/audit-logs/{audit_id}` 返回 |
| [x] | `GET /internal/v1/admin/audit-logs/{audit_id}` | 审计详情 | 详情接口合理 | 保持 |
| [x] | `GET /internal/v1/admin/query-logs` | 查询诊断列表 | 已拆为列表摘要和详情；列表不再返回 request_id、trace_id、query_hash、permission_filter_hash、model_route_hash；用户/知识库名称由后端返回 | 已完成，详情字段只通过 `GET /internal/v1/admin/query-logs/{query_log_id}` 返回 |
| [x] | `GET /internal/v1/admin/query-logs/{query_log_id}` | 查询日志详情弹窗 | 详情接口合理 | 保持详情权限校验 |
| [x] | `GET /internal/v1/admin/model-call-logs` | 模型调用日志列表 | 已拆为列表摘要和详情；列表不再返回 request_id、trace_id、route hash、prompt/input/output hash、token JSON | 已完成，详情字段只通过 `GET /internal/v1/admin/model-call-logs/{id}` 返回 |
| [x] | `GET /internal/v1/admin/model-call-logs/{id}` | 模型调用日志详情弹窗 | 新增详情接口，保留必要诊断字段并重新做企业范围权限校验 | 保持详情权限校验 |

## 5. 需新增的最小化 DTO / 接口

### 5.1 列表摘要 DTO

- `ConfigVersionListItem`
- `AdminUserListItem`
- `AdminDepartmentListItem`
- `AdminKnowledgeBaseListItem`
- `AdminDocumentListItem`
- `ImportJobListItem`
- `AuditLogListItem`
- `QueryLogListItem`
- `ModelCallLogListItem`
- `PublicKnowledgeBaseListItem`
- `PublicDocumentListItem`

### 5.2 详情 DTO

- `ConfigVersionDetail`
- `AdminUserDetail`
- `AdminKnowledgeBaseDetail`
- `AdminDocumentDetail`
- `AuditLogDetail`
- `QueryLogDetail`
- `ModelCallLogDetail`
- `PublicDocumentDetail`

### 5.3 选择器 / 映射 DTO

- `DepartmentOption`
- `UserOption`
- `KnowledgeBaseOption`
- `FolderOption`
- `AssignableRoleOption`
- `DiagnosticLookupResult`

建议优先使用后端在列表中直接返回可读名称。只有多个页面共用且数据量可控时，再提供批量 lookup 接口，例如：

```text
POST /internal/v1/admin/lookups
```

请求只提交需要转换的 ID，响应只返回最小可读名称，不返回完整用户、知识库或部门对象。

## 6. 重构计划

### P0：立刻处理的泄漏与过量返回

- [x] `GET /internal/v1/admin/config-versions` 增加分页，列表移除完整 `config`。
- [x] 管理后台配置页改为先加载版本摘要，查看/编辑时再调用 `GET /config-versions/{version}`。
- [x] `GET /internal/v1/admin/query-logs` 拆列表/详情，列表增加 `user_display_name` 和 `knowledge_base_names`。
- [x] 移除诊断页对 `GET /admin/users?page_size=200` 的 lookup 依赖。
- [x] 移除诊断页对 `GET /admin/knowledge-bases?page_size=200` 的 lookup 依赖。
- [x] `GET /internal/v1/admin/model-call-logs` 拆列表/详情，新增 `GET /internal/v1/admin/model-call-logs/{id}`。
- [x] 普通用户 `GET /internal/v1/users/me` 收紧为最小身份响应；管理后台另取 capability。
- [x] 普通用户 `GET /internal/v1/knowledge-bases` 收紧字段。
- [x] 普通用户 `GET /internal/v1/knowledge-bases/{kb_id}/documents` 收紧字段。
- [x] 普通用户查询响应移除或脱敏 `request_id/trace_id`。
- [x] 公共和管理端 chunk 接口增加分页，禁止一次返回全部 chunk。

### P1：管理后台列表和 selector 收敛

- [x] `GET /internal/v1/admin/users` 拆列表摘要和详情。
- [x] `GET /internal/v1/admin/departments` 拆列表摘要和 selector option，默认不返回 `code`。
- [x] `GET /internal/v1/admin/knowledge-bases` 拆列表摘要、详情和 option。
- [x] `GET /internal/v1/admin/roles` 改为分页角色列表；新增可分配角色 option 接口。
- [x] `GET /internal/v1/admin/import-jobs` 列表将 `document_ids` 改为 `document_count`。
- [x] `GET /internal/v1/admin/audit-logs` 拆列表摘要和详情。
- [x] 管理后台所有下拉选择改为 option 接口，不再复用完整列表 DTO。

### P2：增长型子资源分页

- [x] `GET /internal/v1/query-conversations/{conversation_id}` 消息分页或最近 N 条限制。
- [x] `GET /internal/v1/documents/{doc_id}/versions` 增加分页或明确 bounded list 豁免。
- [x] `GET /internal/v1/admin/documents/{doc_id}/versions` 增加分页或明确 bounded list 豁免。
- [x] `GET /internal/v1/admin/documents/{doc_id}/index-versions` 增加分页。
- [x] `GET /internal/v1/admin/knowledge-bases/{kb_id}/folders` 前端接入分页或 lazy tree。
- [x] `GET /internal/v1/admin/index-health` 视 collection 数量决定是否分页。
- [x] `GET /internal/v1/admin/index-collections/{collection}/snapshots` 视快照数量决定是否分页。
- [x] 用户部门绑定、角色绑定列表已补分页，不再按 bounded list 豁免处理。

## 7. 前端一致性改造要求

- 前端不得再通过大 `page_size` 拉完整列表做 ID 映射。
- 前端列表页只消费列表摘要 DTO。
- 前端详情弹窗打开时再请求详情接口。
- 前端 selector 只消费 option DTO。
- 前端分页状态必须与后端 `pagination.total` 同步。
- 前端不得依赖隐藏字段完成权限判断；权限判断只能用于 UI 提示，最终以后端为准。
- 普通用户端不得展示或依赖 scope、policy version、trace id、request id、内部 hash。

## 8. 后端验收标准

每完成一个接口重构，至少验证：

- 列表接口返回 `pagination.page/page_size/total`。
- 列表响应不包含详情字段、内部 hash、完整配置、全文 chunk。
- 详情接口保留必要字段，并重新做权限校验。
- 当前用户无权限访问详情时返回明确错误，不因列表可见而放行。
- 后端返回可读名称或最小 lookup 数据，不要求前端拉完整对象映射。
- 相关 OpenAPI / schema / TypeScript client 类型同步更新。
- 管理后台和普通 Web build 通过。
- 单元测试覆盖权限过滤、分页边界、字段脱敏。

## 9. 当前落地记录

- 2026-05-25：已完成 P0 第一项，`GET /internal/v1/admin/config-versions` 改为后端分页摘要响应，列表不再读取或返回完整 `config`；管理后台配置页改为先加载版本摘要，创建/编辑时再按需调用详情接口读取完整配置。
- 2026-05-25：已完成 P0 第二项，`GET /internal/v1/admin/query-logs` 改为列表摘要响应，详情级 request/trace/hash 字段只保留在查询日志详情接口；后端列表直接返回 `user_display_name` 和 `knowledge_base_names`，管理后台不再额外拉取用户列表和知识库列表做名称映射。
- 2026-05-25：已完成 P0 第三项，`GET /internal/v1/admin/model-call-logs` 改为列表摘要响应，列表不再返回 request/trace、route hash、prompt/input/output hash 和 token JSON；新增 `GET /internal/v1/admin/model-call-logs/{id}` 供管理后台详情弹窗按需读取。
- 2026-05-25：已完成 P0 第四项，`GET /internal/v1/users/me` 改为普通用户最小身份响应；新增 `GET /internal/v1/admin/users/me/capabilities` 供管理后台读取 roles/scopes/departments，且普通员工缺少管理能力时后端返回 403。
- 2026-05-25：已完成 P0 第五项，`GET /internal/v1/knowledge-bases` 改为普通用户摘要响应，只返回 `id/name/status`；Web 查询页改为分页加载知识库，不再依赖权限策略字段或固定 `page_size=100`。
- 2026-05-25：已完成 P0 第六项，`GET /internal/v1/knowledge-bases/{kb_id}/documents` 改为普通用户文档列表摘要响应，列表不再返回 `kb_id/owner_department_id/visibility/current_version_id`；Web client 改为显式分页参数。
- 2026-05-25：已完成 P0 第七项，普通用户查询响应、SSE metadata/done/error 事件和会话消息不再返回 `request_id/trace_id`，统一改为不可逆短排障码 `debug_id`；内部 service、数据库和管理后台诊断日志继续保留原始追踪字段。
- 2026-05-25：已完成 P0 第八项，公共和管理端 chunk 列表接口统一增加 `page/page_size/total` 分页响应，管理端全文预览接口也增加分页；Web 来源 fallback 改为按页加载更多，管理后台文档详情弹窗改为 chunk 分页展示。
- 2026-05-25：已完成 P1 第一项，`GET /internal/v1/admin/users` 改为用户列表摘要响应，不再返回 email/phone/departments/roles/scopes；管理后台用户表格只展示部门名和角色名，打开用户操作弹窗时再调用 `GET /internal/v1/admin/users/{user_id}` 读取完整详情。
- 2026-05-26：已完成 P1 审计日志项，`GET /internal/v1/admin/audit-logs` 改为审计列表摘要响应，列表不再返回 request/trace、summary JSON、index hash、actor/resource ID；详情字段继续通过 `GET /internal/v1/admin/audit-logs/{audit_id}` 按需读取。
- 2026-05-26：已完成 P1 selector 收敛项，新增 `GET /internal/v1/admin/knowledge-bases/{kb_id}/folder-options`，文件夹下拉改为消费最小 option DTO；部门、知识库、角色和文件夹选择器均不再复用完整列表 DTO。
- 2026-05-26：已完成 P2 第一项，`GET /internal/v1/query-conversations/{conversation_id}` 增加消息分页参数 `page/page_size` 和响应字段 `messages_pagination`；默认只返回最近 50 条消息，Web 查询页增加“加载更早消息”，避免长会话一次性返回全部消息。
- 2026-05-26：已完成 P2 第二项，`GET /internal/v1/documents/{doc_id}/versions` 增加分页参数和响应 `pagination`；由于公共端和管理端复用文档版本响应模型，管理端版本接口也同步返回分页信息，后续还需补管理后台版本翻页控件。
- 2026-05-29：已完成 P2 第三项，管理后台文档详情弹窗的“文档版本”接入独立分页状态和分页控件；版本翻页只刷新版本列表，不再强制重载 chunk 与索引版本。
- 2026-05-29：已完成 P2 第四项，`GET /internal/v1/admin/documents/{doc_id}/index-versions` 增加分页参数和响应 `pagination`；管理后台文档详情弹窗的“索引版本”接入独立分页状态和分页控件，清理选择仅作用于当前页可见的 pending_delete 索引版本。
- 2026-05-29：已完成 P2 第五项，管理后台知识库文档管理弹窗的“文件夹管理”接入 `GET /internal/v1/admin/knowledge-bases/{kb_id}/folders` 分页响应；切换知识库、权限不足和读取失败时同步清理分页状态，不再固定 `page_size=100` 拉取文件夹列表。
- 2026-05-29：已完成 P2 第六项，`GET /internal/v1/admin/index-health` 增加分页参数和响应 `pagination`；索引运维页 collection 健康列表接入分页控件，当前页 collection 才执行 Qdrant 健康探测。
- 2026-05-29：已完成 P2 第七项，`GET /internal/v1/admin/index-collections/{collection}/snapshots` 增加分页参数和响应 `pagination`；索引运维页 Qdrant 快照列表接入分页控件，切换 collection、权限不足和读取失败时同步清理分页状态。
- 2026-05-29：已完成最新审计 P0 第一项，管理后台文档列表改为摘要 DTO + 后端分页，列表不再暴露 `owner_department_id/current_version_id/content_hash` 等详情或内部字段；权限弹窗只读取文档详情，不再连带拉取版本、chunk 和索引版本。
- 2026-05-29：已完成最新审计 P1 第一项，管理后台“失败索引任务”块接入独立分页状态和分页控件，不再固定 `page_size=100` 拉取失败任务；批量重试只作用于当前页选中的失败索引任务。
- 2026-05-30：已完成最新审计 P1 第二项，`GET /internal/v1/admin/users/{user_id}/departments` 和 `GET /internal/v1/admin/users/{user_id}/role-bindings` 增加分页参数与响应 `pagination`；管理后台部门归属弹窗和角色绑定弹窗接入分页控件。
- 2026-05-30：已完成最新审计 P2 selector 项，部门、知识库、可分配角色、文件夹 option 接口与前端 client 默认 `page_size` 从 100 降为 20；管理后台用户、角色、知识库、文档权限、上传和文件夹弹窗接入 keyword 搜索，并保留当前已选项避免搜索分页导致选项丢失。
- 2026-05-30：已完成最新审计 P2 配置遗留列表项，`GET /internal/v1/admin/configs` 改为 `ConfigItemListItem` 摘要响应，不再返回完整 `value_json`；`GET /internal/v1/admin/configs/{key}` 继续作为详情接口返回具体配置项。
- 2026-05-30：已完成最新审计 P2 普通用户文档详情项，`GET /internal/v1/documents/{doc_id}` 收紧为普通用户最小详情 DTO，不再返回 `kb_id/folder_id/owner_department_id/visibility/current_version_id`；Web client 移除未使用的 `getDocument/getDocumentPreview`，普通用户 preview 后端接口标记 deprecated。

后续应继续按 P0 -> P1 -> P2 顺序逐项落地，每完成一项在本文对应清单中更新状态。

## 10. 最新审计遗留项

2026-05-29 复查前后端交互后，仍需跟踪以下问题：

| 优先级 | 状态 | 问题 | 涉及接口 / 页面 | 处理建议 |
| --- | --- | --- | --- | --- |
| P0 | [x] | 管理后台文档列表仍固定拉取 `page_size=100`，且列表复用详情 DTO，携带 `owner_department_id`、`current_version_id` 等详情字段 | `GET /internal/v1/admin/knowledge-bases/{kb_id}/documents`；知识库管理 / 文档管理弹窗 | 已拆 `AdminDocumentListItem`；列表接入分页控件；列表字段改为可读名称和可重建布尔值 |
| P0 | [x] | 文档权限弹窗打开时复用“版本与片段”详情刷新，额外拉取版本、chunk、索引版本 | 管理后台文档权限弹窗 | 已拆文档 metadata 刷新函数，权限弹窗只读取文档详情 |
| P1 | [x] | 失败索引任务批量重试固定拉取 `page_size=100`，任务会随时间增长 | `GET /internal/v1/admin/import-jobs`；失败索引任务块 | 已增加独立分页，批量重试只处理当前页选中任务 |
| P1 | [x] | 用户部门绑定、角色绑定仍无分页；若豁免，需要明确业务上限 | `GET /internal/v1/admin/users/{user_id}/departments`、`GET /internal/v1/admin/users/{user_id}/role-bindings` | 已补分页；部门归属保存表单仍以用户详情完整部门集合初始化，避免分页导致误删 |
| P2 | [x] | 多个 selector 仍一次性拉取 100/200 条 option；数据量大时会过量预取 | 部门、知识库、角色、文件夹 option 接口 | 已将 option 默认拉取量降为 20；相关弹窗改为 searchable selector，并在前端合并保留当前已选项 |
| P2 | [x] | `GET /internal/v1/admin/configs` 仍返回完整 `value_json`，当前主要是遗留 client 封装 | 配置管理遗留接口 | 已拆为摘要列表与详情接口；列表不再返回 `value_json` |
| P2 | [x] | 普通用户文档详情 / 预览 client 有封装但当前未使用，后端详情 DTO 仍包含管理语义字段 | `GET /internal/v1/documents/{doc_id}`、`GET /internal/v1/documents/{doc_id}/preview` | 已移除 Web client 未使用封装；普通文档详情 DTO 已收紧；preview 后端接口标记 deprecated |
